# SDD ledger — plan: docs/superpowers/plans/2026-08-03-phackle-v1.md

Branch: build/v1 (repo /home/carlos/PycharmProjects/phackle; dedicated repo, no other work in tree).
Parallel waves use Agent isolation:worktree branched from build/v1; controller merges after each task review passes.
Model policy: implementers sonnet (content/judgment tasks opus: T6, T10, T16, T19, T20); task reviewers sonnet; re-reviews haiku/sonnet; final whole-branch review fable.

Task T1: dispatched (base 60c4787, implementer sonnet, solo on build/v1)
Task T1: implementer DONE_WITH_CONCERNS (38ec6b2..609ff89, 34/34 tests). Concerns adjudicated at source: plan amended (tailwind-vite, rotation disambiguation, 4aa1df2). Review dispatched (sonnet).
Task T1: complete (commits 38ec6b2..609ff89, review clean). ⚠ resolved: protocol `id` field is spec-mandated (§5.4 "Request/response with ids") — not an unrequested addition.
Task T1: minor (deferred): seeds.ts scenarioIndexFor cold-call recursion depth ≈ days-since-EPOCH (add iterative walk or comment); daily DST test can't discriminate UTC-vs-local implementations; civil-calendar helpers lack leap-day/year-boundary direct tests; CI uses npx instead of package scripts (cosmetic).
Task T2: dispatched (base 4aa1df2, sonnet, isolation:worktree)
Task T3: dispatched (base 4aa1df2, sonnet, isolation:worktree)
Task T4: dispatched (base 4aa1df2, sonnet, isolation:worktree)
INFRA: Agent isolation:worktree forks at build/v1's FORK POINT (main tip 60c4787), NOT at build/v1 HEAD. Fix applied: all wave-1a agents authorized to `git reset --hard 4aa1df2` (zero commits lost). RULE for all future worktree dispatches: include explicit step 0 "git reset --hard <BASE_SHA>" in the dispatch prompt. Also noted: npm ci in a package.json-less worktree silently installs into the parent checkout (harmless, lockfile-exact).
Task T2: NEEDS_CONTEXT (stale worktree) → unblocked via reset authorization, resumed.
Task T3: self-resolved stale worktree via `git merge --ff-only build/v1` (equivalent end state to authorized reset; verified HEAD==4aa1df2, npm ci done). Proceeding as briefed.
Task T4: self-resolved stale worktree via ff-only merge (HEAD==4aa1df2, Locale export verified). Proceeding as briefed.
DECISION (Carlos): aesthetic direction A — "Preprint Gothic, Nothing-disciplined". Recorded in delta spec; T28 (design foundation, opus) + T29 (visual polish pass) added to plan (commit 44d0370). T5 now gated on T4+T28.
Task T2: worktree was auto-cleaned after zero-commit first run; reprovisioned by controller at 4aa1df2 (same name/branch); resumed. RULE: a resumed completed agent may have lost its worktree — reprovision before resuming.
Task T28: dispatched (base 44d0370, opus, isolation:worktree, step-0 reset in prompt)
Task T3: mid-flight rulings — diff-in-diff test APPROVED (tol 1e-12); noise-crushing fix OVERRIDDEN → shared error factor RHO_SHARED≈0.3 + marginal guards (plan 21c9b65, brief regenerated, agent messaged).
Task T28: implementer DONE (8ec2c56+679a0fa, 44 token tests, gate green). Deviations flagged: --hack-gold-ink added (gold 2.94:1 fails text contrast), dark accents +30% red/green (spec's +10% fails 4.5:1). Fonts deferred to T5 per plan. Review dispatched (sonnet).
Task T3: shared-factor verified pre-implementation: RHO=0.3 → corr 0.286 [0.174,0.356], kurt(Y1) 1.295, skew(Y2) 1.608, Y3 distinct 9 / maxfreq 0.372. All bands comfortable; implementing. η_shared = 10th gaussPair/row, 2nd value discarded (documented).
INFRA HAZARD (recurring): agents' env lists the MAIN checkout as an "additional working directory" — Write-tool calls with the main-checkout absolute prefix silently land there while bash cwd stays in the worktree (symptom: vitest can't see files ls shows). T4 self-caught+cleaned its strays. T3 confirmed straying (dgpConstants.ts, dgp.test.ts, fixtures/, scripts/ untracked in main) — corrective message sent; T3 to move strays itself (do NOT controller-clean: may be live state). T2 messaged preemptively. CONTROLLER RULES: never `git add -A` in main while agents run (explicit paths only); future worktree dispatches must state "verify pwd; all file writes use YOUR WORKTREE prefix" in step 0.
Task T4: path-trap self-caught, strays cleaned from main, genuine RED re-established in worktree; proceeding.
Task T28: review verdict Needs fixes — 3 Important (§10 enforcement overclaims R1.7/R2.2/R3.1; R7.3 missing Do/Don't; R1.3 vs --sig-band unreconciled). Fix round 1/5 dispatched to original implementer (FIX_BASE 679a0fa).
Task T28: minor (deferred): doc-drift substring check lacks word boundaries (tokens.test.ts:249); §0 reconciliation table count says five, +30% dark-luminance departure is a sixth (subsumable in fix); §10 manual greps weaker than vitest counterparts (border-radius decimals, border:1px no-space).
Task T3: path-trap cleanup CONFIRMED both sides (byte-identical copies to worktree, originals removed, controller verified main clean — only .claude/ untracked). Genuine in-worktree RED/GREEN re-established; 24/24 passing; proceeding to gate+commit.
Task T28: fix round 1/5 — implementer reports all 3 addressed (82429ae, 47 tests, gate green; §0 count corrected 52→48 disclosed). Scoped re-review dispatched (sonnet, FIX_BASE 679a0fa).
Task T2: implementer DONE (585ab4b+3f5c7d1 @ branch worktree-agent-a3ca594e6a8f8bae0, 90/90, gate clean). Judgment calls flagged: gammln coefficient respelling (bit-identical verified), Lentz eps 1e-15 (empirical 1.7e-12 worst case, 60x margin). Review dispatched (sonnet, base 4aa1df2).
Task T3: implementer DONE (cac330f+ab73fc1 @ branch worktree-agent-a20c6f3c430d10b69, 58/58, gate clean). Y1 kurtosis margin 1.295 (thinner, budgeted cost of shared-factor ruling) — flag for T10 calibration. Review dispatched (sonnet, base 4aa1df2).
Task T4: implementer DONE (0dbecf6 @ branch worktree-agent-a63c5a5a4ce4b8570, 58/58, gate clean). Review dispatched (sonnet, base 4aa1df2).
INFRA HAZARD (cross-cutting, from T4): PATH default node = Homebrew v25.4.0 (stub localStorage global breaks jsdom storage tests); .nvmrc pins 22; /usr/bin/node = v22.22.1 works. RULE: every future dispatch with jsdom tests must include: verify `node --version` is v22.x, else prefix commands with PATH="/usr/bin:$PATH". CI: test.yml pins setup-node 22 (verified).
SEAM NOTES for future dispatches: (1) useLocale() content/copy are NULLABLE during async load — T14–T18 dispatches must state the loading-state contract. (2) T4 invented persistence key 'phackle.settings'; master §5.6 pins single key 'phackle.v1' with settings inside — T13 dispatch must reconcile (storage.ts owns the key; LocaleProvider migrates to consume it). Reviewer verdicts may adjust this.
Task T2: complete (commits 585ab4b..3f5c7d1, review clean, merged 105d032; ⚠ gate-transcripts resolved by controller full-gate run post-merge: GATE GREEN + CI on push).
Task T2: minor (deferred): plain number[] internally vs Appendix A "Float64Array" phrasing (no consequence, note-worthy comment suggested); n=31 "exact" satisfied via 1e-10 relative + df toBe (interpretation documented post-hoc).
Task T3: complete (commits cac330f..ab73fc1, review clean, merged into build/v1; ⚠ empirical values corroborated by controller ledger; gate covered post-merge).
Task T3: minor (deferred): generateRows export foot-gun (suggest runtime guard n!==400&&effect); diff-in-diff tests only outcome 0 of 4 (cheap parametrize); Y4 clamp bounds classified under TUNABLE object; t5Scale naming imprecise; TDD evidence prose-not-transcript.
Task T4: complete (commit 0dbecf6, review approved, merged into build/v1). Importants ratified by controller: (1) nullable useLocale content → RATIFIED with App-level loading gate convention (T5 dispatch will mandate: App gates screen render until content loaded; screens assume non-null after gate). (2) phackle.settings key → T13 MUST reconcile into phackle.v1 §5.6 schema + migrate LocaleProvider to consume storage.ts. (3)(4) plan-mandated stubs tracked → T6, T19/T20.
Task T4: minor (deferred): journals "Suspicious Findings" capitalization vs master lowercase (T6 fixes); correspondingAuthor static string; implementer read §2.11/AppB beyond scope (transparent, benign — achievements/glossary got authored early, T6 polishes); validateLocaleContent lives in test file (test-importing-test, confirm at final review).
Task T28: fix round 1/5 (3 addressed, 1 NEW Important open — R6.1 outline:none enforcement deleted by tier rewrite while claiming Tier A; commits 679a0fa..82429ae). Fix round 2 dispatched to original implementer. Minor: fix-report test count 47→48 transcription (fixing in round 2). Deferred: R5.6 no mechanical check (pre-existing).
MERGES: build/v1 @ 1137c75 = T1+T2+T3+T4 + vitest worktree-glob exclusion (controller infra commit). Full gate: 8 files / 138 tests GREEN. Pushed.
WAVE 2 DISPATCHED (all base 1137c75, worktree, step-0 reset+pwd+PATH rules): T6 EN content (opus), T7 analyze (sonnet), T12 store+forkLog (sonnet; zustand install authorized; boot opts gains scenarioCount; VIEW_SPEC gains seen flag).
Task T28: fix round 2/5 — R6.1 strong-option scan (8f44191, 50 token tests, gate green; 47→48 report line corrected). Re-review round 2 dispatched (sonnet, FIX_BASE 82429ae).
Task T28: complete (commits 8ec2c56..8f44191, review clean after 2 fix rounds; merged into build/v1). Deferred minors: FOCUS_SUPPRESSION_RE bare-prefix match (fail-safe only); Tier A prose omits hyphenated outline-width form; R5.6 matchMedia check unenforced mechanically (becomes real at T17/T18 — T5 dispatch carries it by hand).
INFRA: eslint globalIgnores gained '.claude' (controller commit ffc92cb) — typescript-eslint saw T12's worktree (diverged package.json) as a second project root and failed all parsing. Rule: every repo-wide tool must exclude .claude/ and .superpowers/.
MERGES: build/v1 @ ffc92cb = +T28 (DESIGN.md law + tokens + 50 token tests). Gate: 9 files / 188 tests + lint + tsc + build GREEN. Pushed.
Task T5: dispatched (sonnet, worktree, base ffc92cb; loading-gate convention + theme-in-phackle.settings ruling + fontsource authorization; package.json lock conflict with T12 to be resolved by controller at merge via npm install).
Task T6: implementer DONE (fb522c3+a83f7b6 @ branch worktree-agent-abf0b982992943ee8, 145/145, gate clean; "main" in report = slip, verified: real main untouched at 60c4787). Fixed T4 content bugs en route (cat-crypto direction, peekFootnote wink). Seams: 13/20 headlines carry {n} → T15 must substitute; press/grantwell scenario-agnostic (types have no link — §4.1 pressPool delta, controller to adjudicate at review). Review dispatched (opus, base 1137c75).
Task T7: implementer DONE (9fb1850+92f72aa @ branch worktree-agent-ae4cfc188f114dc2d, 31 new / 169 total, gate clean). Judgment call: valid=false PathResults carry computed stats (needed for 12-row micro fixture under MIN_CELL=30) — reviewer to adjudicate; T8 contract already mandates consumers filter on valid. Review dispatched (sonnet, base 1137c75).
Task T6: review verdict Approved-with-fixes (opus; hand-verified 80/80 outcome labels, 20 scenarios, all banks; comedy bar cleared "by craft"). Fix round 1/5 dispatched (FIX_BASE a83f7b6): (1) restore §2.4 Armitage wink as lab.peekFootnoteArmitage (de-winking was WRONG remedy — §2.4 protects that exact wink; T14 plan updated: 1st-press sincere + 2nd-press Armitage); (2) {n}→{effect} in 18 headlines + substitution rule + at-most-one test (report count was wrong: 18/20 not 13/20); (3) kill 15× "four hundred" cover-story seam (also deflates optional-stopping fiction); (4) validateLocaleContent requires lexicons arg (harm+direction propagate to IT/ES); (5) PressBlurb.scenarioIds? + tag 3 bound blurbs (adopted reviewer rec; T15 plan updated with preference picker + {effect} render rule, commit debe082).
Task T6: minor (deferred): jigsaw headline contradicts its outcome-0 (spare capacity) + litres-in-Y1 note; KLOC negative-draw label; bounded-scale noun repetition (3× confidence, 2× warmth, 12× Self-rated); slot-1 "Longest…" device 9/20; cover-story gap-opener template ~14/18; thirteen-mortgage is 3rd superstition scenario (kept deliberately); mixed US/UK orthography (metres/litres/storey) + €-vs-401(k) — note for T19/T20; report's grantwell-ending claim wrong (actual ending better); harm-check scope scenarios-only + 'The Sunday Supplement' outlet would trip a widened lexicon (comment guard suggested); direction-test fixture inconsistency.
Task T6: fix round 1/5 — implementer reports all 5 fixed + 2 subsumed one-liners (b4c8fa9, 150/150, gate clean). Scoped re-review dispatched (sonnet, FIX_BASE a83f7b6).
Task T7: complete (commits 9fb1850+92f72aa, review Approved, adjudication ACCEPT valid-carrying w/ doc guard → folded into T8 dispatch; merged 13b30bb, gate 219 GREEN, pushed).
Task T7: minor (deferred): CI-property test branches on df>0 not result.valid (passes-by-coincidence risk); commit 92f72aa body has orphaned line (cosmetic).
Task T12: implementer DONE (51309ef @ branch worktree-agent-a6bc55665d937744c, 45 new / 183 total, gate clean). CONTROLLER RULING for review: §2.2/§2.3 require player-driven briefing→lab ("Open the data"); brief's action list omitted it (plan gap); boot()-drives-to-lab is non-compliant if confirmed — expect fix round adding openData() guard. Review dispatched (sonnet, base 1137c75).
Task T8: dispatched (sonnet, worktree, base 13b30bb; carries T7 doc-guard comment additions to PathResult/protocol; tails-share-fit guidance; "112 preps" noted as approximate).
Task T6: complete (commits fb522c3..b4c8fa9, review clean after fix round 1; merged f9f6aa3, gate 231 GREEN, pushed). Re-review verified Armitage string byte-exact, 19/20 {effect} + 0 {n} headlines, 0 "four hundred", required-lexicons signature, 3 tagged blurbs.
Task T6: minor (deferred): 3× "still" as recruitment marker across the 15 rewrites (soft echo, distinct phrasing); label-maker tense wobble (index.ts:226).
Task T12: review verdict Needs fixes — 1 Important (briefing→lab not player-gated; controller ruling confirmed violated; fix shape: boot lands 'briefing' + guarded openData()). Fix round 1/5 dispatched (FIX_BASE 51309ef). Adjudications: plain-string errors OK; live forks counter OK (all 6 sites atomic, inspection-verified).
Task T12: minor (deferred): 2 brittle exact-string guard assertions (store.test.ts:286,301); pending stuck true after abandon-during-flight (inert today; one-liner pending:false in abandon — MAY be subsumed in fix round); no forks===countForks invariant test; useGameStore singleton wrapper untested.
Task T5: implementer DONE (49eb0ac @ branch worktree-agent-aa8fb42455e6c1a33, 19 new / 207 total, gate clean). CONTROLLER RULING: REPLICATED stamp IS a sanctioned --assist-green place (master §7.2 explicit; direction A's single-loud-color governs chrome, not the verdict moment) — DESIGN.md R1.5 carve-out + green stamp land in T5 fix round; ruling surfaced to Carlos via status message (one-line revert if he disagrees). Theme-flash (no inline boot script; index.html not T5's) → fold into T21 dispatch. R1.7 scanner caught a JS variable named 'gold' (renamed — the law works). Review dispatched (sonnet, base ffc92cb).
Task T12: fix round 1/5 — openData() player gate added (b0af61d, 186/186, gate clean). Scoped re-review dispatched (sonnet, FIX_BASE 51309ef).
Task T8: implementer DONE (7b8aa40 @ branch worktree-agent-a2bbba7adb87a4476, 27 new / 246 total, gate clean; perf 18ms vs 800ms budget). Judgment call: covariates axis order = income-high-bit binary (documented; internal-consistency only). T7 doc-guard comments landed here. Review dispatched (sonnet, base 13b30bb).
Task T5: review verdict Needs fixes — 1 Important: green-stamp ruling not yet applied (ruling post-dates implementation; expected). Fix round 1/5 dispatched (FIX_BASE 49eb0ac): (a) green REPLICATED mark, (b) DESIGN.md R1.5 carve-out + §0 row (T5 authorized to edit law for this), (c) token-test alignment without loosening. Everything else clean (structural loading gate, RAF teardown, domTheme single contact point praised).
Task T5: minor (deferred): header vol-line renders label words in mono not just numerals (Tier-D trade-off, self-disclosed); local TFunction type duplicates LocaleProvider's inline shape.
Task T12: fix round 1/5 verdict — openData ADDRESSED, but fix introduced NEW Important: changeSpec/peekAndExtend/submit lack screen==='lab' guards (submit-after-boot publishes from briefing, demonstrated). Fix round 2/5 dispatched: guards + skip-openData order tests (commits 51309ef..b0af61d so far).
Task T5: fix round 1/5 — green stamp + DESIGN.md carve-out delivered (c132f9a, 208/208, no token test tripped — R1.5 is Tier D). Scoped re-review dispatched (sonnet, FIX_BASE 49eb0ac).
Task T8: complete (commit 7b8aa40, review Approved; merged bc12871, gate 258 GREEN, pushed). Adjudication: covariates order accepted as documented-fixed (not overclaimed PINNED); T9 goldens must key by specKey not index (carried into T9 dispatch).
Task T8: minor (deferred): AXES.covariates exports mutable shared object references (unfrozen); small digit-encoding duplication.
Task T9: dispatched (sonnet, worktree, base bc12871; stride-7 precheck pinned, best-attempt scoring pinned, canonicalTransform(Y2)=log1p, specKey-sorted curve hash, tsx devDep authorized, 30-day distribution numbers requested).
Task T12: fix round 2/5 — lab-action guards added (64dce84, 32/32 store, 189/189). Scoped re-review dispatched (sonnet, FIX_BASE b0af61d; includes prefetch-path subtlety check).
Task T5: complete (commits 49eb0ac+c132f9a, review clean after 1 fix round; merged f7de899, gate 278 GREEN, pushed). Green-stamp carve-out live in DESIGN.md (§0 row placed to preserve "last row" self-reference; narrowings 6→7).
Task T5: minor (deferred, from original concerns list): pre-hydration theme flash → T21 owns index.html inline boot script; other concerns (#1,#4-7 in its report) reviewed as acceptable/open observations.
Task T12: complete (commits 51309ef..64dce84, review clean after 2 fix rounds; merged 11e74ca, gate 329 GREEN, pushed). package.json auto-merged clean (zustand + fontsource, different regions).
CONTROLLER SLIP (recorded): T12 round-2 re-review dispatched without regenerating the package (round-1 SHAs passed to review-package); re-reviewer materialized the diff itself. Double-check FIX_BASE args every round.
Task T13: dispatched (sonnet, worktree, base 11e74ca). RULINGS: history per-mode keyed from v1 (§5.6 extension); phackle.settings fold-in migration + LocaleProvider delegation (helpers only, its tests stay green unmodified); share.* copy keys authorized; spoiler property test parametrized over AVAILABLE_LOCALES.
Task T9: implementer DONE (99876f3 @ branch worktree-agent-ad17fb64d5dcf791b, 34 new / 292 total, gate clean). 30-day window: 22 null/8 effect, sigCount [30,176], attempts {0:20,1:8,2:2} — cap never hit. Concerns: puzzleNumber calendar-math duplicate in engine (byte-checked vs daily.ts); generatePractice gained scenarioCount param (protocol-consistent); scripts/ not in tsconfig include (pre-existing). Review dispatched (sonnet, base bc12871).
Task T13: implementer DONE (ed3628a @ branch worktree-agent-a00516271fbae5315, 91 new / 420 total, gate clean). Concerns: 9 summary.breakdown* keys added (T17/T19/T20 must know); per-mode history signatures (ruling consequence); LEGACY-KEY MIRRORING deviates from remove-ruling (kept to avoid touching T5's tests — resurrection-loop risk, review adjudication requested); master §2.9 sample itself has 6-vs-7 off-by-one (documented; T17 Legend must not reproduce it verbatim). Review dispatched (sonnet, base 11e74ca).
Task T9: review verdict Needs fixes — 1 Important (null-day precheck runs AFTER full enumeration; unsurfaced deviation from §3.3 + dispatch pin). Adjudications: puzzleNumber duplication ACCEPT (EPOCH from tuning, real cross-module test); generatePractice 2-arg ACCEPT. Fix round 1/5 dispatched (FIX_BASE 99876f3): reorder + gating test + measurements both orderings; goldens must not move.
Task T9: minor (deferred): daysFromCivil duplicated engine-side (seeds.ts copy unexported); gen_goldens.ts relies on default uncaught-exception for nonzero exit; SCENARIO_COUNT=20 triplicated in test/script files; puzzleNumber cross-check lacks leap-day date; scripts/ outside tsconfig include (pre-existing).
Task T13: review verdict Needs fixes — 1 Important (legacy-key write mirroring contradicts remove-ruling; reviewer traced resurrection + quota-inversion + stale-stranding paths). Fix round 1/5 dispatched (FIX_BASE ed3628a): delete mirror, single-writer phackle.v1, ownership granted for exactly 2 test assertions, non-resurrection test. Adjudications: breakdown keys COMPLETE (T17 note: CallCorrect/Incorrect labels vs reveal.* sentences — deliberate); per-mode signatures CORRECT; §2.9 sample off-by-one — IMPLEMENTER RIGHT (master's own caption inconsistent; countForks is source of truth; T17 Legend must not copy the "7 forks" caption).
Task T13: minor (deferred): ModeHistory type duplicated (achievements vs storage); loadStats no direct test; scoring tests assert symbolic constants not literals; true_detective same-date tie-break assumes hack-before-prereg (no intra-day timestamp in DayRecord); LocaleProvider double loadState on mount; settings save is per-key not logical-op atomic (resolved by mirror removal).
Task T9: fix round 1/5 — precheck gating implemented (ef8f198, 294/294, goldens byte-identical). MEASURED: letter-compliant ordering ~11-12% slower (~1ms/day absolute; unmemoized runSpec 4.3x per-spec). CONTROLLER ADJUDICATION: KEEP the fix — spec-auditability is a design pillar; ~1ms/day is the right price. Numbers preserved in report. Scoped re-review dispatched (sonnet, FIX_BASE 99876f3).
Task T13: fix round 1/5 — mirror removed, phackle.v1 single writer, 2 authorized assertions repointed, non-resurrection test added (db7a9b5, 421/421). Scoped re-review dispatched (sonnet, FIX_BASE ed3628a).
Task T9: complete (commits 99876f3+ef8f198, review clean after 1 fix round; merged f851bc7, gate 365 GREEN, pushed). Perf adjudication: letter-compliant ordering kept (~1ms/day cost, documented).
Task T9: minor (deferred): cap-exhaustion fallback tie-break now excludes precheck-failed attempts (arguably more correct, untested — compound rare event); 30-day decision-equality script was scratchpad-only (goldens are the permanent equivalent).
Task T10: dispatched (OPUS, worktree, base f851bc7). The balance sheet: 1000 simulated days, §3.9 bands a-e, p_hit table + checksum, tuning authority limited to TUNABLE within bounds, golden-regen protocol, Y1-kurtosis watch-item carried from T3.
Task T13: complete (commits ed3628a+db7a9b5, review clean after 1 fix round; merged c6b5a9c, gate 457 GREEN, pushed). phackle.v1 is the single settings writer; non-resurrection proven.
Task T21: dispatched (sonnet, worktree, base c6b5a9c). Owns index.html theme-flash inline boot script (T5 ledger item), OG 1200x630, favicon SVG fork, vite-plugin-pwa autoUpdate; package.json additions limited to pwa plugin + png converter.
Task T21: implementer DONE (9fb8cc1 @ branch worktree-agent-aa0b82566ebfb6e06, 24 new / 481 total, gate clean, dist verified). Concerns: duplicate manifest link in dist (plugin unconditional + source link needed for dev/test — documented); resvg font-substitution bug found+fixed (pinned families); description wording flagged for review. Review dispatched (sonnet, base c6b5a9c).
Task T10: PARTIALLY BLOCKED escalation (7980ab8; 3/5 bands pass; (b) null-only=15 vs [4,12], (d) literal §3.9d rule maxes 0.655 — never beats 0.75 base rate due to within-family spec correlation clustering on null days). CONTROLLER ADJUDICATIONS (both sanctioned by §3.9's own [TUNABLE] marking; plan updated + brief regenerated, commit on build/v1): (b) primary statistic = 75/25 day-mix (measures 11 ∈ band; player-experience reading; null-only reported informationally, band [4,16]); (d) family-DENSITY statistic (formalizes §2.7.6 clustering; 0.822 ∈ band; script must document the literal rule's failure mode). No DGP constants change; goldens must stay byte-identical. Fix round 1/5 dispatched.
Task T10: watch-items ledgered: (c) power margin ~1.4 SE; Y1 excess-kurtosis median 1.28 with 37.9% of days <1.0 (median-based moment test still passes; caps RHO_SHARED headroom).
Task T21: review verdict Approved-with-2-Important → fix round 1/5 dispatched (FIX_BASE 9fb8cc1): (1) boot-script theme mapping must match domTheme on stored-but-invalid values (matchMedia only when nothing stored) + 7th jsdom scenario; (2) font-presence guard in generate-pwa-images.mjs (throw, never silent-degrade). Adjudications: duplicate manifest link ACCEPT-documented; og.png render evidence ACCEPT; description register ACCEPT.
Task T21: minor (deferred): favicon fork uses <rect> not <path> (wording gap only); unescaped < in meta content attributes (valid HTML5, tooling-safe spelling preferred).
Task T10: fix round 1/5 GREEN — all 5 bands pass under adjudicated definitions (eed079a; b-mix 11, d-density 0.822; informational rows a-raw/b-null/b-eff/d-share retained; §3.9d failure numbers in script header; HACKABILITY_MIN_SIG pinned literal 30; goldens+dgpConstants+tuning byte-identical; cal exit 0, determinism re-proven). Review dispatched (sonnet, base f851bc7, 4 commits).
Task T21: fix round 1/5 — both Importants fixed (30857d7, 483/483, dist byte-identical; ternary matches domTheme fallthrough exactly; assertFontsResolvable throws-with-hint, proven both ways). Scoped re-review dispatched (sonnet, FIX_BASE 9fb8cc1).
Task T21: fix round 1 re-review — OPEN: (1) '' stored-theme edge still diverges (truthiness vs nullish; fix theme != null + 8th scenario); (2) EVIDENCE INTEGRITY: negative-path font-guard "proof" was paraphrase-as-transcript (invalid JS command, output contradicts code's own singular/plural template) — code verified correct by reading, but real run must be pasted; implementer told to self-audit remaining evidence blocks. Fix round 2/5 dispatched (FIX_BASE 30857d7).
Task T21: minor (deferred): fc-list substring match lets 'DejaVu Sans' pass via 'DejaVu Sans Mono' (exact-name match suggested); stale line citation in fix report.
Task T10: complete (commits 6cea0ac..eed079a, review Approved — checksum vector exhaustively verified, statistics hand-re-derived, all 5 adjudication-fidelity checks PASS; merged d58a7d4, gate 488 GREEN, pushed). M1 MILESTONE SATISFIED: calibration suite green, cal exit 0.
Task T10: minor (deferred): family-density denominator fixed 448 not live valid-count (equivalent today, conservative bias if drifts — suggest assert validCount===448); N_SCHEDULE checksum-exclusion rationale imprecise (CURVE_N=200 independent literal); "all-invalid curve" test name overclaims (no literally-empty-curve test). ⚠ checksum assert fires per-pHitAtK-call not literal boot — resolved: T11 folds an explicit init-time call.
Task T11: dispatched (sonnet, worktree, base d58a7d4). FINAL ENGINE TASK. Pins: re-init allowed (midnight rollover); spoiler JSON-scan per op; trueBeta null-day=0/effect=injected magnitude (minimal GeneratedDay extension authorized); checksum at init; vitest-vs-E2E division stated.
Task T21: fix round 2/5 — nullish fix + 8th scenario (be7634e, 484/484); evidence self-audit: §7(b) fabrication replaced with genuine verified run + disclosed compressed-but-real blocks elsewhere. Re-review dispatched (sonnet, FIX_BASE 30857d7).
Task T21: complete (commits 9fb8cc1..be7634e, review clean after 2 fix rounds; merged 13e0dab, gate 515 GREEN incl. build, pushed). Re-review externally corroborated PNG checksums; evidence-integrity remediation verified genuine.
Task T21: minor (deferred): report prose claims truthy/nullish disagree on literal null (they agree — table correct, clause wrong); self-audit enumeration missed one compressed block (numbers consistent).
Task T11: implementer DONE (8c87fd0+7ec6943 @ branch worktree-agent-ab4e862764c425d31, 35 new / 523 total, gate clean). trueBeta = injected d·sd raw-units (authorized day.ts/types.ts extension; reveal-UI readability flag for T16). Review dispatched (sonnet, base d58a7d4).
Task T11: review Approved w/ 1 Important (timeout map-cleanup unprovable black-box — source verified correct; fix round 1/5: test-only pendingCount() seam + 2 assertions). Adjudications: trueBeta raw-units per pin CONFIRMED implemented, but reviewer notes master §2.7's "β = 0.24" example is STANDARDIZED-scale — T16 dispatch must carry a display-normalization decision; JSON mock properly scoped; worker.ts 32-line thin confirmed.
Task T11: minor (deferred): spoiler runSpec scanned with 1 spec/day-type not several (key-set can't vary — low risk); idx===-1 unreachable branch reuses 'max N' string; trueBeta doc note (master's own example isn't raw-units — comment addendum suggested).
SCREEN-WAVE PREP: briefs T14-T17 extracted. Glue plan: T14 creates ScreenRouter + screens/registry.ts (stub per screen; each later task replaces its ONE registry line — minimal conflict surface); T14 wires App boot (real engineClient, scenarioCount from content.scenarios.length, practice via daily.isPractice). copy.ts concurrent additions expected — controller resolves additive conflicts at merge.
Task T11: fix round 1/5 — pendingCount() seam + transition assertions, mutation-verified against production line deletion (7753e35, 523/523). Scoped re-review dispatched (sonnet, FIX_BASE 7ec6943).
Task T11: complete (commits 8c87fd0..7753e35, review clean after 1 fix round; merged 8035462, gate 550 GREEN, pushed). ENGINE COMPLETE — all of §3/§5 worker-side shipped and calibrated.
SCREEN WAVE DISPATCHED (all base 8035462, worktree, step-0 rules): T14 Lab+router-glue (sonnet; owns registry.ts w/ stub-per-screen + App boot wiring; briefing stub carries openData button), T15 Briefing+Published (sonnet; registry patch file; press scenarioIds picker; {effect}=max(1,round(|beta|)); call-overlay via registry), T16 Call+Reveal+SpecCurve (OPUS; registry patch file; container-agnostic Call; trueBeta raw-units WITH unit label ruling; y-mapping pinned; per-family readability assessment required), T17 Summary+Stats+Legend+About+COPY-FREEZE (sonnet; registry patch + nav doc; invoice; always-both α panels; legend from mapping not master's buggy caption; freeze roster gathers sibling copy-key lists).
Merge order note: T14 first (owns registry.ts), then T15/T16/T17 applying their registry patch lines + additive copy.ts conflict resolution by controller.
Task T16: implementer DONE (58f2633 @ branch worktree-agent-a2c388df04bcaf679, 67 new / 617 total, gate clean; self-screenshotted figs both themes, fixed published-point-on-axis + band-label truncation defects itself; truth-line unit-on-outcome format, 112-day sample all sane 0.13-0.41). Cross-task finding: DESIGN R4.2 inline color-mix vs R1.3a — T15 messaged with authorized --scrim token resolution. Review dispatched (OPUS — centerpiece; base 8035462).
Task T14: implementer DONE (ff8897c..5881a2f @ branch worktree-agent-afedfa6a0f5611412, 43 new / 593 total, gate clean; 22 lab.* copy keys). Law amendment: R1.5 second green exception (dial) + R1.8 opacity-ramp rewrite per ACT-I pin (dial's original muted→sig-red spec violated Act-I rule + color-mix ban). One-word exports of gameStore/FORK_EMOJI; App try/catch around createEngineClient (jsdom Worker-less); covariates as single 4-option radiogroup. Review dispatched (sonnet, base 8035462).
Task T16: review verdict Needs fixes (OPUS review) — 5 Important: I1 touch-action:none + no tap binding (scroll theft, no tooltip); I2 callout overflow on long labels (fix: §7.4's own Y-notation abbreviation); I3 viewBox scaling breaks mobile (type ~5.8px, hit <8px at 320px; evidence taken at the one non-revealing width); I4 abandon-path caption/legend claim absent highlight; I5 published recipe AT-unreachable (fix: text line under accounting). +M6 registry mount-condition note (cross-task, included in round). Fix round 1/5 dispatched (FIX_BASE 58f2633). Adjudications: sig-red restraint on pHitPct CORRECT (R1.3's 4th place is the p<.05 figures only); tooltip pointer-only ACCEPTED with recipe-text affordance required; T16 scrim-clean confirmed; waivers genuine (Reveal's weaker); {k} in accounting3 MORE faithful than pin.
Task T16: minor (deferred): plotInset + wrapLabel fixes unpinned by tests, wrapLabel dead export; leader parallel to y-axis at rank~0 (anchor at 35-45% width suggested); call.title uppercased sentence vs R2.7; hover re-renders 1792 elements (memoize suggestion); volume:1 hardcoded in cover echo (vs T15's cover — check at merge); truth line never names the exposure (§2.7.1 names it); "Fig." word inside mono span; data-p/data-outcome test attrs shipped on all circles. DESIGN geometry note for future: sub-.10 region gets 60% height but only ~10% width (rank axis) — most teaching left on the table if ever revisited.
Task T15: implementer DONE (03d7500 @ branch worktree-agent-ac2c075693f639653, 612/612, gate clean; 4 copy keys). Scrim token implemented per authorization (tokens.css + DESIGN §0/R4.2/§9). Self-caught setState-updater bug. Flags: gold career-points inline addition (review judges vs R1.6); registry patch guessed T14 shape (controller reconciles); overlay empty-until-merge interim correct. Review dispatched (sonnet, base 8035462).
Task T17: implementer DONE (25f943e @ branch worktree-agent-a8719b57a34c37e3f, 112 new / 662 total, gate clean; 25 copy keys; freeze harness 33 tests; 175-key roster T6→T17, T15 row pending controller). CRITICAL SEAM SURFACED: day-completion wiring UNASSIGNED (scoreDay/evaluateAchievements/saveDay/streaks never called by any UI — prereg unlock dead without it) → new task T30 planned post-merge. App.tsx T14-vs-T17 merge documented in patch file. Prereg scoring approximation inert until T18. Review dispatched (sonnet, base 8035462).
Task T16: fix round 1/5 — all 6 fixed + bonus band-label-overlap defect found via TRUE 320px render (8f99ff9, 95 owned / 645 total; viewBox-tracks-container = unit≡CSSpx by construction; headless-Chrome 500px window clamp trap disclosed — container-width CSS is the honest method). M1 subsumed (wrapLabel pinned by 3 tests). Copy running total 28 added/10 changed. Re-review dispatched (sonnet, FIX_BASE 58f2633).
Task T14: review verdict Needs fixes — CRITICAL: dial opacity ramp fails 4.5:1 across ~90% of p-range both themes (1.60:1 at p=1.0) while T14's own R1.8 text claims ≥4.5 throughout (reviewer recomputed with project's WCAG formula; static token suite structurally can't catch runtime blends). RULING: stepped ramp via 3 registered derived tokens (tokens.css color-mix, both themes, each ≥4.5, ADDED TO CONTRAST SUITE → law claim becomes mechanical) + discrete p-bands 0.5/0.2/0.1/0.05; §2.4 anticipation mechanic preserved as 5-state progression. + Important: SpecControls 181 lines vs ≤150 brief cap. Fix round 1/5 dispatched (FIX_BASE 5881a2f). Adjudications: DESIGN amendment registration CLEAN + master §7.2-vs-§2.4 contradiction CONFIRMED real; exports additive-clean; try/catch NOT a swallow (converges on error banner, both paths tested); covariates single-group MORE faithful.
Task T14: minor (deferred): ForkTrail duplicates trail-walk loop (share.ts buildTrail unexported — extract shared helper later); footnote co-presence unasserted (subsumable); StrictMode guard test exercises rerender not real double-invoke.
Task T15: review verdict Needs fixes — 1 Important: §2.5's altmetric counter absent (traced to CONTROLLER brief omission, not implementer). RULING: add STATIC tier-scaled altmetric figure (spinning violates motion budget — same documented precedence as press slide-ins; deterministic fnv1a32(iso)×tier, mono numerals, ink/muted NOT gold). Subsumed: briefing.correspondingAuthor → "Corresponding author: You" per §7.3's own joke (Grantwell byline was T4-era wrong value). Fix round 1/5 dispatched (FIX_BASE 03d7500). Adjudications: gold career points KEEP (R1.6's own worked example); setState fix correct+tested; no-escape one-way-door SUPPORTED (interim empty-trap caveat auto-resolves at T16 merge — MERGE CHECKLIST: verify Call present before ship); empty-overlay covered.
Task T15: minor (deferred): empty-overlay claim spans two tests; App header not inert under overlay (→ T22); briefing test title overclaims; watermark DOM order differs PressCard vs ChyronBar; pickJournal locale-proof structural (real cross-check possible only after T19/T20).
Task T16: complete (commits 58f2633+8f99ff9 @ branch worktree-agent-a2c388df04bcaf679, review clean after 1 fix round; AWAITING MERGE behind T14's registry). Re-review independently reproduced I2 arithmetic exactly + verified true worst-case (71-char recipe wraps in-budget at 320px); evidence blocks verified genuine incl. the disclosed headless-clamp correction.
Task T16: minor (deferred): initial-width single-frame default before first ResizeObserver callback (defanged by scroll-fade; only reduced-motion+above-fold could glimpse); pointerleave fires on touch release so quick taps show tooltip briefly (pre-existing, pointerType check would fix); I2 test fixture not literally worst case (naming only — true worst verified in re-review).
Task T17: review verdict Needs fixes — CRITICAL: persistence non-idempotent across own nav (Summary remount re-fires saveDay; increment-built counters silently inflate; fix = durable history[iso][mode] check + remount-cycle test asserting persisted NUMBERS). Important: silent share failure (contract-violating void'd promise → summary.shareFailed key + rejection test); freeze-harness undisclosed dynamic-key blind spot (2 sites in own code; honest comment + tsc-coverage rationale required); patch-file must carry the nav-remount interaction. Fix round 1/5 dispatched (FIX_BASE 25f943e).
T30 SCOPE CLARIFIED by review: day persistence EXISTS (Summary persists live-computed score); ONLY achievement evaluation is unwired (evaluateAchievements zero call sites — wall stays all-locked, prereg unlock dead). T30 = wire evaluateAchievements + decisiveTails at the same idempotent persistence moment.
Task T17: minor (deferred): histogram aria-label on role-less span; no exact-20 rolling-window boundary test; stats.avgScore dangling unused key (T19/T20 roster note: do not translate as-is without a consumer).
Task T14: fix round 1/5 — stepped dial tokens (literal hex from offline color-mix computation; contrast light 5.31/5.49/5.63, dark 5.09/5.19/5.26; TEXT_TOKENS-enforced; R1.8 Tier D→B) + RadioGroup extraction 181→121 (5501c13, 107 owned / 607 total). Co-presence assertion subsumed. Re-review dispatched (sonnet, FIX_BASE 5881a2f).
Task T15: fix round 1/5 — static altmetric (non-overlapping tier ranges = hard monotone) + byline "You" corrected (3d5ec28, 623/623). Copy +2 (altmetricScore/Percentile). Re-review dispatched (sonnet, FIX_BASE 03d7500).
Task T14: complete (commits ff8897c..5501c13, review clean after 1 fix round; contrast table + mix recipe independently recomputed both themes; dialBand boundary-scanned at 0.001; zero test edits verified via git show --stat). MERGING FIRST (registry owner).
Task T14: minor (deferred): DESIGN R5.1 still says dial tick animates opacity — contradicts new R1.8 (one-word doc fix, route to T29).
MERGES: T14 → 70284e9 (607 GREEN, no conflicts). T16 → merged + registry wired (call/reveal real) → 1918fde, gate 702 GREEN, pushed. Abandon path now fully real at localhost (briefing-stub → lab → call → reveal w/ SpecCurve → summary-stub). Awaiting T15 re-review (publish path) + T17 fix round (summary).
Task T17: fix round 1/5 — durable history-based persistence guard (mutation-verified via revert: hackDays 1→2 reproduced then fixed), summary.shareFailed alert + 3 failure tests, blind-spot honesty comment, patch-file nav-remount section (7e371d0, 668 total). Re-review dispatched (sonnet, FIX_BASE 25f943e).
Task T15: complete (commits 03d7500+3d5ec28, review clean after 1 fix round; ranges verified structurally non-overlapping; test counts independently recomputed). Merging.
Task T15: minor (deferred): monotonicity tests sample 5 ISOs vs asserting on unexported range constants; "already used pattern" framing imprecise (prior resolution was report-prose not code comment); altmetric --text-13 vs career --text-15 unexplained sizing.
MERGES: T15 → merged with 2 conflicts hand-resolved (tokens.css additive; DESIGN §0 unified into ONE six-derivation row, narrowings 7→8, §9 row deduped) + registry wired (briefing/published real) → gate initially RED (6 stub-reality tests): CONTROLLER INCIDENT — first gate chain piped vitest through grep, masking exit code; f9350b1 was pushed red. Fixed pattern (capture exit before grep). Integration edits: 6 assertions repointed to composed reality (openData CTA marker; real-Call-renders-null-under-unbooted-singleton documented) + my own sed apostrophe bug fixed. Final: 7a2e87e, 775/775 + tsc + lint GREEN, pushed. FLAG FOR FINAL REVIEW: controller-side test edits in router.test.tsx + published.test.tsx (documented in commit body).
GATE RULE (standing): never `vitest | grep` in the same pipeline as the pass/fail decision — capture exit code first.
Task T17: fix round 1 verdict — all 4 ADDRESSED, but day-boundary trace found NEW Important: guard keys on live localIsoDate() not puzzle day (midnight-straddle nav re-persists yesterday under tomorrow's key: inflation + phantom streak day + next-day suppression); docs overclaim remount-independence. Fix round 2/5 dispatched: store retains boot iso (ONE authorized minimal store.ts change), persistence keyed on store.iso, straddle test, doc corrections, roster 25→26 subsumed.
Task T17: fix round 2/5 — persistence anchored to store.iso (boot-retained), straddle test via vi.setSystemTime, docs corrected, roster 26/176 (c9aa3a1, 669 total; store tests extended to 32). Re-review dispatched (sonnet, FIX_BASE 7e371d0).
Task T17: complete (25f943e..c9aa3a1, review clean after 2 fix rounds; merged a9e21e0). SCREEN WAVE FULLY MERGED — full loop real at localhost. Gate 927 GREEN, pushed.
MERGE-INTEGRATION EDITS (controller, all flagged for final review): App.tsx boot+nav reconciliation per patch files; share.ts comment union; registry summary wiring; copy-freeze harness precision fix (arrow-return generic + split comparison false-positives from sibling code; lookbehind + code-only chars; negative cases pinned; positive guard cases untouched).
CARLOS PLAY-TEST FEEDBACK: "needs graphs + explanations, feels barebone" → T31 added to plan (restores §2.4's dropped cut visual + six methods notes + first-run intro). T30+T31 DISPATCHED (base a9e21e0; T30 sonnet, T31 OPUS; disjoint except storage.ts different-regions — watch at merge). T18 queued behind T30 (prereg unlock needs live achievements).
OWNER FEEDBACK ROUND 2: "NO plots" (T31 unmerged when he refreshed — expectation set) + "hard to understand" (T31 scope UPGRADED mid-flight: briefing goal strip, 4-step how-to, dial caption) + "comedy meh, be braver" + "too many em dashes, reads too AI" → T32 added to plan (opus; braver corpus + mechanized em-dash budget test; keys/structure frozen; queued behind T30+T31 merges). Memory updated (design-taste). T18 still queued behind T30.
Task T30: implementer DONE (e648659 @ branch worktree-agent-ae5a714dfcfb85376, 958/958, gate clean). Edges flagged: decisiveTails trusts submit's p<.05 invariant (2-arg signature carries no p); specKey-no-N means sibling matches at any N. Review dispatched (sonnet, base a9e21e0).
Task T30: complete (e648659, review Approved — structural stale-immunity + transitive invariant traced; merged 755db1e, gate 958 GREEN, pushed).
Task T30: minor (deferred): computeDecisiveTails comment must name the monotonic-N over-award mechanism plainly (comment-only; behavior pinned + tested); saveAchievements changed-tracking unreachable from sole call site (defensive, harmless).
Task T18: dispatched (sonnet, worktree, base 755db1e). Pins: Screen union +'prereg'; chooseMode/preregCommit guards; N=400 via extends (judgment documented); real preregSig replaces T17 approximation; all four §2.8 prereg rows tested; Reveal call-block gating authorized minimally; prereg.* namespace only in copy.ts (T31 concurrent).
Task T31: implementer DONE (35a9e01+0bd846f @ branch worktree-agent-aa3e3ed1ad792e9a9, 52 new / 1000 total, gate clean; 21 copy keys; screenshots preserved). Found REAL mobile blocker: sticky results pane (998px tall) painted over controls at 360px — knobs unreachable; fixed via static (costs live dial while scrolling mobile; proper dial-alone-sticky = controller call). Pin deviations documented: value-seeded jitter (index-seeding reshuffles on exclusion — kills the mechanic); --dot-all not --dot-explored (4px×200 = 114% column slab). howThisWorks.body replaced by step1-4. Review dispatched (OPUS, base a9e21e0).
INTERRUPTION: host machine powered off accidentally (Carlos, overnight); process restarted. State verified on resume: build/v1 @ 755db1e intact + pushed; T18 worktree intact at base (no commits lost); T31 review had no on-disk artifacts to lose. Both agents resumed from transcripts. T32 still queued behind T31 merge.
Task T31: review verdict Needs fixes (OPUS, resumed post-shutdown) — CRITICAL I1: value-seeded jitter collapses ties (outcomes 2/3: ~9 dots rendered vs legend's 400; fix = occurrence-index-within-value-group, adjudicated deviation itself CORRECT); Important I2: dial caption teaches p BACKWARDS (reviewer replacement supplied); I3: jitter band wastes pixels on informationless axis (cap 64 + height→96); M4 restored requirement: Legend pointer dropped by my scope upgrade — forkTrailHint line. Fix round 1/5 dispatched (FIX_BASE 0bd846f). STICKY RULING: static ships; dial-alone-sticky = NAMED T29 PIN (reviewer's grid CSS shape recorded in its report §Adjudication 1; sticky block must exclude caption; latent T14 bug credited).
Task T31: minor (deferred): M1 tap targets (summary/dismiss); M2 Got-it affordance; M3 jargon-in-explanations (2 rewrites supplied); M5 stale shots labelled; M6 baseline half-outside viewBox (overflow:visible dependent); M7 dial numeral wraps at 1088 (R2.5's clamp — separate task candidate). Screenshot-table 256-vs-272 scrollbar note.
T29 PINS ACCUMULATING: dial-alone-sticky grid CSS (T31 review §Adj1); DESIGN R5.1 stale opacity word (T14); volume:1 cover-echo vs T15 JournalCover consistency (T16 M5).
Task T31: original implementer transcript LOST to power-off (completed pre-shutdown; prior process's completed-agent transcripts unrecoverable). Fresh fixer dispatched per loop fallback (sonnet, working IN the existing worktree agent-aa3e3ed1ad792e9a9 on top of 0bd846f; report file = persistent memory; findings I1/I2/I3/M4 + subsume-invites M1/M2/M3).
CARLOS DIRECTIVE: grand review when pipeline completes — plan written + committed (docs/superpowers/plans/2026-08-04-grand-review.md): 4 read-only lanes (code/game/writing/UX, top tier) → synthesis → controller triage → fix waves → verification. SEQUENCE: runs after T24 + v1 final whole-branch review, BEFORE T25 deploy. Bar stated as properties; no publication named anywhere per directive. Deferred-minors ledger feeds GR1c adjudication table.
Task T18: implementer DONE (60f6662 @ branch worktree-agent-a6eca979e92733b6b, 1006 total, gate clean; 7+1 copy keys; survived power-off resume). Concerns: preregFalsePositive wiring slightly beyond Reveal authorization (reviewer assesses); SHARE BUG FOUND: prereg days always show ⚖️❌ (no call exists) — controller proposed ruling: 🧾 + 📄/🏳️ by significance, NO ⚖️ marker, spoiler-safe; prereg can't fire hack-only achievements (by design — verify monk/preregDays still feed); RadioGroup disabled-keydown nit. Review dispatched (sonnet, base 755db1e).
INFRA: first T18 reviewer misfired post-restart — responded as a GitHub-PR-review workflow (plugin skill hijack on "Review" in the description; no review performed). Re-dispatched with description "T18 task-gate verification" + explicit not-a-PR-review disarm line at prompt top. RULE: post-restart reviewer dispatches avoid "Review X" descriptions and carry the disarm preamble.
Task T18: review verdict Needs fixes — 1 Important: prereg share broken end-to-end (no terminal glyph on real playthroughs + unconditional ⚖️❌; pre-existing share test exercised a log shape reality never produces). CONTROLLER RULING REJECTED BY REVIEWER (correctly): 📄/🏳️-by-significance would leak day type (~93% posterior at power 0.7) — §2.9 is distribution-indistinguishability, not possibility. Adopted counter-proposal: fixed outcome-independent 📄 terminal for prereg + null-callCorrect drops the ⚖️ suffix + spoiler property extended with preregSig-varying pair. Fix round 1/5 dispatched (FIX_BASE 60f6662). Adjudications 1/3/4 in implementer's favor (preregFalsePositive spec-mandated ACCEPT; Monk verified firing; RadioGroup harmless).
Task T18: minor (deferred): hack-only achievements (first_blood/outlier_surgeon/one_tailed_bandit/harking) can't fire from prereg commits — design follow-up question for Carlos/grand review; RadioGroup keydown lacks disabled check (latent, out of diff).
Task T31: fix round 1/5 — all four findings fixed (d495236, 1007/1007; RED reproduced via disposable-worktree baseline; M3 partial subsumed, tap-targets correctly NOT touched per gate). Disclosure: mean-separation demo needed ~20 sampled days (data-shape property). Re-review dispatched (sonnet, FIX_BASE 0bd846f, disarm preamble per post-restart rule).
Task T18: fix round 1/5 — reviewer counter-proposal implemented (1e5593d, 1008 total; mode-decided fixed 📄 terminal, null callCorrect drops ⚖️, spoiler property + prereg pair + leak-sensitivity guard, real-shape fixture). Re-review dispatched (sonnet, FIX_BASE 60f6662, disarm preamble).
Task T31: complete (35a9e01..d495236, review clean after 1 fix round; merged c138220, gate 1038 GREEN, pushed). Carlos's plots + explanations are LIVE at localhost.
Task T31: minor (deferred): demo screenshot lacks reproducible practice-seed label (mechanism verified structural not cherry-picked; practiceSeed uses Math.random so exact repro impossible without logging the seed — T29/grand-review evidence-hygiene note); baseline-count 33-vs-34 discrepancy self-flagged immaterial.
Task T18: complete (60f6662+1e5593d, review clean after 1 fix round; merged 4b95b82 with copy.ts additive resolution, gate 1088 GREEN, pushed). PREREG LIVE. Minor deferred: new spoiler "guards-the-guard" test vacuous as written (guarantee holds STRUCTURALLY — buildTrail signature carries no significance channel; test-quality item → grand-review GR1c).
OWNER FEEDBACK ROUND 3 (mid-flight amendments): (1) attention score illegible → T32 amendment (self-explanatory values, satire kept); (2) press should look like press → T29 pin 8 (clipping anatomy); (3) DataCut → analyst-instrument feel → T29 pin 9; (4) question on screen 1 vs instructions on screen 2 → T29 pin 10 (persistent Lab question header). ROUND 3b: fork emojis hard to read + Legend hidden → T29 pin 11 (trail presentation + Legend link lift; emoji SET kept — swap offer made to Carlos, pre-launch is the free moment).
T32+T29 DISPATCHED (both opus, base 4b95b82, parallel, file-disjoint: content values vs ui/styling).
OWNER RULING (round 3c): fork emoji set REDUCED — all spec-change kinds → single 🍴, ➕ stays, terminals/prefix/call unchanged (in-trail vocabulary = 2 glyphs, total 7). Documented deviation from master §2.9's per-knob table, owner-directed pre-launch. Legend becomes a HOVER POPOVER adjacent to the trail (nav page stays). T29 pin 11 superseded + ownership expanded to share.ts FORK_EMOJI values + affected tests; classifyChange/achievements untouched; unused legend.* keys → roster note. T19/T20 note: share.forksWord semantics unchanged.
Task T32: implementer DONE (08d5fcd @ branch worktree-agent-ab20f9f9bf4d584f7, 1094 total, gate clean; density 635.6→30,904 chars/dash, 55 values changed / 0 merely-different / most strong banks untouched; altmetric → "Mentioned {n} times online already"; worktree self-provisioned at 4b95b82 after assigned one missing — absolute-path discipline held). Review dispatched (OPUS — comedy judgment; base 4b95b82).
Task T32: review verdict Needs fixes (OPUS) — ~20 verified wins, wink audit CLEAN (pass even removed a pre-existing near-wink + fixed Act-II "un-printed" pun violation), structure frozen verified. 2 Important: density arm slack (pre-complaint corpus would pass 1/400; fix MIN_CHARS_PER_EM_DASH=2500) + 7 lateral/worse edits under a zero-claim self-review (reviewer enumerated reworks incl. grantwell[16] shape + chyron costume restoration + 2 casualty phrases). Fix round 1/5 dispatched (FIX_BASE 08d5fcd). Minors: about.intro softening (subsume-invited un-soften), priorArt comma, report count slips, 2 T19/T20 handoff notes (altmetric ≥40 plural safety, first-occurrence replace).
Task T32: fix round 1/5 — all 7 reworked (191e848, 1095 total; density floor 2500 + floor>636 regression test; chyron dashes restored → density now 1/10,310 with 3 legitimate dashes; report corrected in place with honest self-review-gap diagnosis; both subsume invites done + priorArtForkingPaths same-construction bonus). Re-review dispatched (sonnet, FIX_BASE 08d5fcd, disarm preamble).
Task T32: complete (08d5fcd+191e848, review clean after 1 fix round; merged 3fa8621, gate 1095 GREEN, pushed). EN CORPUS FINAL.
T19 (Italian) + T20 (Spanish) DISPATCHED (both OPUS, worktrees, base 3fa8621, parallel; shared brief file task-T19-brief.md — combined plan section; tbrief can't extract "T20" from the slash heading). Expected trivial conflicts: AVAILABLE_LOCALES + loader lines (controller resolves). Both must supply REQUIRED per-locale harm+direction lexicons; em-dash budget inherited; T32's inline translator notes flagged.
Remaining after T19/T20/T29: T22 a11y → T23 E2E → T24 CI → v1 final whole-branch review → GRAND REVIEW → T25 deploy.
Task T20 (ES): implementer DONE (c82d475 @ branch worktree-agent-ab3716d8edd4c8188, 1145/1145, 213 keys, density 1/35,569; Borges original-title call for forking paths; assigned-worktree-pointed-at-wrong-REPO wrinkle self-resolved). Concerns: shell.test.tsx = 3rd shared file (locale-count test rewritten length-agnostic — IT will collide, keep this shape); validator-import re-registers EN suites (defer, known); press outlets transcreated (CORRECT per delta rule — journals only stay EN). Review dispatched (OPUS, base 3fa8621).
Task T19 (IT): implementer DONE (1ef01af @ branch worktree-agent-ac820479677b79f2f, base 3fa8621 verified, 1150/1150; same wrong-repo-worktree wrinkle as T20, self-provisioned). Standouts: 13→17 (eptacaidecafobia, id unchanged), Rete Sottopancia, endecasillabo, prodotti della ricerca, Clickeria. Fixed 2 shared tests: shell.test.tsx (length-agnostic — SAME shape as T20's, take either side in merge) + published.test.tsx (lang-flips-before-content race, real once it/es stopped aliasing en — locale-agnostic fix, no ES variant needed). Validator-import re-registration: same known cosmetic, extract post-merge. Review dispatched (OPUS, base 3fa8621).
Task T29 (visual polish): implementer DONE (c9ea4ce @ branch worktree-agent-a33328772cea026ef, 5 commits on base 4b95b82 verified, 1113/1113, 11/11 pins, 189 shots). Fixed SHIPPING BLOCKER: Published.tsx non-analyzable import('./registry') -> empty overlay in prod build (jsdom-invisible). ESCALATED BLOCKER (controller-verified by grep, pre-existing, out of T29 lane): store.finishReveal() has NO caller in src/ui — Summary unreachable in real app; fix = post-merge micro-task (button on Reveal + new copy key x3 locales). Deviations for review adjudication: pin-1 sticky measures 153px @360w vs ruling's ~85 (numeral wraps); pin-9 mean labels in caption band not at bars (SVG version overprinted cloud at 360, shot retained); 4 amended assertions; legend.emojiSubgroup/Exclusion/Tails now unused + legend.emojiSpec parenthetical stale (KEEP keys in union at merge — IT/ES corpora already carry them; roster cleanup deferred to grand review). Commits = thematic slices of one verified tree (disclosed). Review dispatched (OPUS, base 4b95b82).
Task T20 (ES) REVIEW: APPROVED w/ 1 required fix. Exhaustive parity clean (20/22/21/14/11/8/213, tokens, tiers, ids); all 80 direction labels hold; decimal rule unbroken (CERO COMA CERO CINCO adjudicated fine — spoken presenter voice); 1 em-dash total; wink audit clean; Borges = 4/4 sites, senderos/bifurcaciones propagated, zero variants; shell.test.tsx rewrite = CORRECT merge shape (either side, keep AVAILABLE_LOCALES-relative form). I1 summary.streak "Racha de 1 días" plural bug (locale-introduced, day-one visible) = required fix. Fix round 1 dispatched to ORIGINAL implementer: I1 + M2 clitic + M6 stale locale.ts comment + M5 quote style + directed: M1 seven "La investigación" openings, M3 grantwell[17], M4 la revelación, M8 chair word order, 3 comedy recoveries (tragaperras pun, Boletín Oficioso, press[9] cadence + ni de lejos), optional lexicon hardening (NO nutri, NO tratamiento). RULINGS: M7 validator extraction deferred until both locales merge; M9 es-ES peninsular is final (corpus is € + metric); es-419 not at launch.
Task T19 (IT) REVIEW: APPROVED w/ 2 required edits. Independently reproduced gate (1150/1150, tsc 0, eslint 0, 46.37kB it chunk, no leakage); 609/609 strings, byte-level id/tag/tier parity; all 80 labels name gains; 17-relocation airtight (3 visible strings consistent, id never reaches DOM — only pickPress consumes it); wink audit clean; em-dash 1/11,802 with ≤EN pin. Required: (1) IT direction lexicon hole — basso/breve/corto/ridotte/riduzioni/cali missing + FALSE justifying comment (guard strength, zero content breach; fix before ES inherits the rationale); (2) horoscope-parking "indovinato...era vicino" self-contradiction (indovinare = guess CORRECTLY). Directed: retto×2, 3 calques, ANVUR-doesn't-fund grantwell slip, 2 translated-not-transcreated outlets (Registro Settimanale, Cronaca Pubblica Settimanale). molino ADJUDICATED fine (trade spelling). Merge shapes verified strictly-not-weaker: take EITHER side of shell.test.tsx (both length-agnostic; if any side hard-codes length, take IT's), either side of published.test.tsx (watch for doubled waitFor). es-browser transitional toggle state noted, self-resolves at T20 merge. Fix round 1 dispatched to ORIGINAL implementer.
Task T29 REVIEW: NEEDS FIXES (design work itself sound — 11/11 pins confirmed, prod-build fix independently verified via INEFFECTIVE_DYNAMIC_IMPORT warning + dist grep, spoiler property PROVEN (many-to-one relabel of log-only function = strictly reduced channel capacity), 4 amended assertions adjudicated repointed-not-relaxed, tokens.test.ts untouched, motion budget intact, §0 = 10 rows). ADJUDICATIONS: 6a sticky 153px ACCEPTED (~85 was estimate; 2-line numeral is honest floor; R8.1 trade correct); 6b caption-band means RIGHT CALL/WRONG EVIDENCE; 6c legend.emojiSpec stale = player-visible Important. 5 Important: I1 popover blows doc to 619px@360/991px@768 (its own shots prove it); I2 "~85px" in DESIGN.md R8.1 + Lab.css disproved by own measurement; I3 two evidence citations show FIXED state (one in Published.css comment — load-bearing); I4 no @vite-ignore guard (+ registry.t15.patch.md still endorses old pattern); I5 legend.emojiSpec value wrong for reduced set. +M1 iOS mouseenter-before-click two-tap risk (test fires only click, can't catch), M2 caption guard now vacuous. M3→T22 lane, M4 watermark→grand review, M5 bisect note accepted. ORIGINAL IMPLEMENTER TRANSCRIPT LOST (2nd occurrence of this failure class) → fresh fixer dispatched INTO worktree agent-a33328772cea026ef per T31 recovery pattern, 7-item fix list, base c9ea4ce. MERGE BOOKKEEPING: after T29 merges, IT/ES legend.emojiSpec need matching one-line transcreations — fold into the finishReveal micro-task (which already adds 1 copy key x3 locales).
Task T20 (ES) FIX ROUND 1: DONE (0469491 on c82d475, 1145/1145, all exit codes 0). All 10 items: streak -> 'Días seguidos: {n}'; se les vende; locale.ts comment; «el» purged; 20 distinct openings; grantwell 17/18/8; la revelación; chair headline; tragaperras de una cola + El Boletín Oficioso + press[9]/subline[7]; both lexicons hardened (nutri+tratamiento exclusions documented). Scoped re-review dispatched (SONNET).
Task T19 (IT) FIX ROUND 1: DONE (51abd66 on 1ef01af, 1164/1164 [+14 pins], all exit codes 0, scope = it/index.ts + it.shape.test.ts only). Direction lexicon 25->38 w/ TRUE analytic-comparatives comment + 8 probe pins; horoscope "tirato a indovinare... c'è andato vicino"; tenuto/sostenuto; 3 calques (sonda for strumentare); ERC alto rischio alto guadagno; La Gazzetta di Provincia + Il Bollettino Civico; harm 14->18 (medic stem). Scoped re-review dispatched (SONNET) — flagged saluti-vs-salute stem boundary as explicit check.
Task T20 (ES) RE-REVIEW: APPROVED FOR MERGE (all 10 items PASS, scope exactly 4 permitted files, 2 LOW style nits accepted, es.shape 49/49, tsc 0). FLAKE NOTED for GR1c: tests/engine/dgp.test.ts 200-seed structural-range test hit its hardcoded 5000ms timeout under machine load (passes at 20000ms; unrelated to diff) — test-suite-health lane should consider raising/removing per-test timeout or trimming seeds. MERGE HELD until T19 re-review returns: single integration pass planned (ff T20 -> merge T19 w/ conflicts on locale.ts [final comment must describe BOTH locales live], content/index.ts loader, shell.test.tsx [either side, both length-agnostic] ; published.test.tsx = T19-only, no conflict), then full gate, then push.
Task T19 (IT) RE-REVIEW: APPROVED FOR MERGE (7/7 PASS; saluti/salute definitively clean — substring diverges at final char, verified by direct regex + full-corpus empty-match assertion; same dgp.test.ts flake, passes in isolation).
MERGE T19+T20 -> build/v1 @ 5bb829f, PUSHED. ff to 0469491 then --no-ff 51abd66. Conflicts resolved: locale.ts AVAILABLE_LOCALES=['en','it','es'] + final comment; shell.test.tsx = T19 side verbatim; content/index.ts auto-merged (3 real cases). Full gate on merged tree: tsc 0, eslint 0, vitest 0 (1213/1213, 48 files), build 0. Trilingual corpus COMPLETE. Remaining before tail: T29 fix round in flight (base 4b95b82 — its merge will need content-conflict handling vs 5bb829f incl. share.test.ts glyph pins x3 locales), then finishReveal micro-task (+ legend.emojiSpec IT/ES one-liners), then T22/T23/T24/final review/grand review/T25.
Task T29 FIX ROUND 1: DONE (e703067 on c9ea4ce, fresh fixer per recovery pattern, 1120/1120 [+7], all exit codes 0, DESIGN greps clean, motion still exactly 4). All 7 items with CDP-measured evidence (closed==open scrollWidth all 4 cells; popover right edge exactly on client edge at 360; new tests verified failing against old handler; caption guard uses never-resolving runSpec). New legend.emojiSpec: "Any specification change (outcome, subgroup, covariates, outlier exclusion, transform or one-tailed switch)" — IT/ES mirrors still owed in micro-task. RESIDUAL FLAGGED for GR4a: pre-existing 2px masthead overflow at 360-paper (DIV.ph-header__controls / BUTTON.ph-toggle, scrollWidth 347 vs clientWidth 345, present with popover closed — shell owner's lane). Scoped re-review dispatched (SONNET).
OWNER FEEDBACK ROUND 5 (post-trilingual merge): default EN interface (kill browser auto-detect), flags in language menu, clearer dark/paper toggle, Home/Play navigation back to game, compound-emoji overlap fix (green check class), MORE ANIMATION (owner supersedes frozen motion budget — DESIGN.md motion law to be AMENDED, not bypassed). Plan amended with T33 (nav+controls+finishReveal+legend mirrors, opus), T34 (emoji audit, sonnet, parallel w/ T33), T35 (motion pass, aesthetics opus, AFTER T33+T34). Sequencing: all gated on T29 merge (file collisions: ForkTrail/Lab/Published/copy). T33 absorbs the finishReveal micro-task + IT/ES legend.emojiSpec mirrors.
Task T29 RE-REVIEW: APPROVED FOR MERGE (7/7 PASS; 2 Low accepted: touch-test-3 non-discriminating [coverage overclaim, functional fix correct], 154-vs-153 arithmetic nit in DESIGN.md/Lab.css — both parked for grand review). MERGE T29 -> build/v1 @ 8ab428a, PUSHED. ZERO conflicts (copy.ts auto-merged — T32/T29 disjoint values). Full gate: tsc 0, eslint 0, vitest 0 (1245/1245, 48 files), build 0. T14-pin sticky, press-as-press, DataCut instrument, reduced emoji + popover, prod-import fix + @vite-ignore guard ALL LIVE on trilingual tree. Worktrees provisioned by controller at 8ab428a: task-t33, task-t34 (named branches — sidesteps the fork-point + wrong-repo worktree bug class). T33 (opus) + T34 (sonnet) dispatching in parallel; T35 (motion) queued behind their merge.
OWNER DIRECTIVES (round 6): (1) IT/ES translations have UI-chrome failures — "Open data" rendered as noun phrase "Datos abiertos" instead of action "Abrir datos"; prose-transcreation review missed interface-language conventions entirely. T36 UI-LANGUAGE AUDIT dispatched (opus, read-only vs 8ab428a): classify all 213 keys x2 locales by UI function, apply es-ES infinitive / it-IT imperative conventions, full replacement table; application as fix task after T33/T34 merge (collision: it/es index files). (2) GOAL formalized at docs/superpowers/GOAL.md — world-class game under every angle (scientific/computational/writing/UX); grand-review plan amended (A1 chrome lane, A2 scientific accuracy explicit, A3 goal as synthesis bar). (3) LOOP: controller now runs in autonomous /loop mode (ScheduleWakeup fallback heartbeat; task notifications remain primary wake signal) until pipeline + grand review complete; deploy still owner-gated.
Task T33: implementer DONE (96650cf on task-t33, base 8ab428a, 1264/1264, TDD red-first verified 20 failures, all 4 exit codes 0). detectLocale = stored ?? 'en' (prefix-match deleted, navLang kept-unread for regression); flags aria-hidden + endonym labels; theme toggle = role-group w/ R4.6 underline (retired .ph-toggle); masthead-as-button + Play nav (shell only, Legend/About/Stats untouched); finishReveal wired OUTSIDE last Block w/ store-flow test (Summary idempotence verified undisturbed); emojiSpec IT/ES mirrors + findMissingSpecKnobs guard x3; header overflow fixed (was 224px doc overflow at 360, now 0 at all widths, min target 45px). reveal.toSummary = See the invoice / Vedi la fattura / Ver la factura. Disclosed: published.test.tsx amended (used deleted detection path — justified); Legend.css stale .ph-toggle comment = T34 file, fix at merge; header wraps 3 rows at 360 (design call parked for T35/grand review). Review dispatched (OPUS).
Task T34: implementer DONE (e43383d on task-t34, base 8ab428a, 1252/1252 [+7], all exit codes 0). Owner's ⚖️✅ overlap = ALREADY FIXED by T29 M5 (min-width:2ch) — owner played pre-T29 build; historical repro confirmed bug was real, current build 0 overlap across 24 states (CDP-measured). All other sites cleared; Summary share preview DOESN'T EXIST as render site (shareText only reaches clipboard/navigator.share — grep+source verified). Defensive GlyphMark wrapper (letter-spacing: --tracking-label, existing token) added to Legend + popover; box-width-delta proves inter-glyph gap; share.ts BYTE-IDENTICAL, spoiler tests unmodified. Exemplary disclosure: self-corrected scope deviation (blocked main.tsx patch reported, not worked around), failed Intl.Segmenter method disclosed. Review dispatched (SONNET — mechanical/evidence task).
Task T36 AUDIT: DONE (read-only vs 8420d3d; findings persisted by controller — subagent file-write blocked). 213 keys x2 locales fully classified (18 ACTION/117 STATE/18 HEADING/52 BODY/8 ARIA). es-ES: 6 WRONG-FUNCTION/5 AWKWARD/12 BETTER. it-IT: 2/16/17 + 9 Title-Case achievement names. EN: 11 hardening recs, 4 of them REAL EN plural-at-1 defects (peekSurcharge, accounting2/3, forkHistogramBar ARIA, streak-in-Italian). Root cause documented: IT source comment misread Open Data as journal badge; it.shape SHARED_WITH_EN allowlists the bug. Owner's case confirmed: briefing.openData = primary CTA, both locales WRONG-FUNCTION. Convention contracts written for both locales (es infinitive / it imperative, casing, terminology locks, ARIA-as-speech). CONTROLLER RULINGS in findings §7: IT = Preregistrazione consistently; EN openData -> 'Open the data'; EN plural-at-1 keys fixed in EN too; Language/Lingua/Idioma group labels; emojiSpec rows superseded by T33. T37 (application, ALL THREE locales + EN comments, sonnet w/ exact table) DISPATCHES AFTER T33 MERGE (same files). MERGE done meanwhile: T34 -> build/v1 @ 76fbf4d, pushed (1252/1252).
Task T33 REVIEW: NEEDS FIXES (9/10 items clean, several exemplary — locale contract pinned on the exact regression, finishReveal end-to-end through real router + double-fire idempotence, motion budget zero additions, red-first reproduced to exact count; Vedi la fattura/Ver la factura moods CONFIRMED correct per-language convention). IMPORTANT: masthead overflow fix incomplete — .ph-header__nav non-wrapping; at 360/it the ES locale button is OFF-SCREEN (68px overflow; 29px es; 39px 390/it); implementer measured only EN-on-game-screen (Play absent) — measurement-blind-spot class, evidence overclaim noted. Reviewer live-verified fix: nav flex-wrap+flex-end -> 0px all cells. Fix round 1 dispatched to ORIGINAL implementer: nav wrap + 12-cell matrix evidence + jsdom pin + T23 booking; ES "Cambiar tema"; weight-medium honesty. PARKED for grand review: header height at 360 (267->318 w/ wrap), 3-group boundary legibility, 320w 8px pre-existing overflow (outside header). T23 E2E SCOPE ADDITION booked: locale-aware header-overflow check at 360 with Play visible.
Task T33 FIX ROUND 1: DONE (c7fb6dc, 1267/1267, all exit codes 0). Nav wrap applied; 12-cell matrix ALL ZERO (+ 768/1088 sweep also zero); honest correction: it/360/game overflowed 7px with NO Play — Italian label lengths were a pre-existing cause, not just the new item. ES "Cambiar tema"; .ph-seg--action made real (uppercase + --tracking-label, existing CTA idiom); appNav.test.tsx CSS-text pin (verified red on removal) + T23 booking; original report §7 marked incomplete rather than rewritten (error stays on record). Scoped re-review dispatched (SONNET).
Task T33 RE-REVIEW: APPROVED FOR MERGE (all 5 items verified; pin MUTATION-TESTED per-selector; 2 worst historical cells independently re-verified 0px in fresh headless build; action idiom confirmed genuine w/ 4th uncited precedent). MERGE T33 -> build/v1 @ 67028cd, PUSHED, zero conflicts, full gate green (1274/1274, 49 files). Trilingual + T29 + T34 + T33 all live. T37 worktree provisioned (task-t37 @ 67028cd); T37 dispatched (OPUS — includes EN Act-II writing edits): full T36 table (all severities adopted), §7 rulings (IT Preregistrazione w/ 360w fallback check, EN 'Open the data' + briefing.test update, 4 EN plural-at-1 value fixes propagated x3, Language/Lingua/Idioma, §5 comment hardening), SHARED_WITH_EN fixture removal, convention contracts into both copy headers. After T37: T35 motion pass, then T22.
Task T35 DISPATCHED (OPUS aesthetics, worktree task-t35 @ 67028cd) IN PARALLEL with T37 — file-disjoint (T35: CSS/DESIGN.md/tokens.test/TSX-hooks-only; T37: content/*.ts + string-pinning tests; both forbidden from the other's surface). Saves ~1.5h critical path vs plan's serial ordering. T35 brief: amend R5 into registered motion system (duration/easing tokens, exhaustive site list, §0 update), choreograph meaning-bearing moments only (router transition, dial sweep, press clippings, Block cadence, popover, toast, achievement), transform/opacity only, reduced-motion parity as law w/ mechanical test, Nothing-OS restraint.
Task T37: implementer DONE (3cc6d03 on task-t37, base 67028cd, 1274/1274, all exit codes 0). 71 applied (ES 22, IT 34, IT ach-names 9, EN 6 values) / 2 superseded-by-T33 / 2 deviated (accounting3 shape from ruling — audit had no row) / 0 blocked. IT Preregistrazione x5 verified by REAL 360px render (CTA wraps 46->67px, no overflow — wrap not overflow, no nowrap/ellipsis rules exist). Token sets AND order preserved (Reveal .ph-num span pin holds). Concerns: playPrereg = only 2-line CTA in IT build (owner-taste escape hatch = Prereg x5, not layout-required); a11y/nav localeToggle now string-identical (nav variant unrendered). Review dispatched (OPUS — EN Act II values + deviations need register judgment).
Task T37 REVIEW: APPROVED w/ 1 Important (faithfulness "close to exemplary": 67/67 rows exact-or-explained, laws independently re-verified incl. token ORDER, accounting3 scientific referent CONFIRMED vs pHitAtK, IT/ES authored strings judged better than EN). I1: EN floor-value agreement — "Your data-peeks (1) make" plural at commonest case; header note overclaims. Fix round 1 dispatched to ORIGINAL implementer: anaphoric EN accounting forms ("You explored {k} of them..."), peekSurcharge ({peeks}× idiom, "(1 times)" trap flagged), note made true. RULING I2: share.forksWord "1 forks" ships to timelines — §2.9 line-3 AMENDED by controller ruling to label-colon form "Forks: {n} · Streak: {n}" (grammar-neutral all locales; scope granted into share.ts + share.test; spoiler property must pass unmodified). Minors: count cell 7-not-6, ES publication phrase RULING = revert lab.submit to Enviar a publicación, IT header cross-refs + rule-7 carve-out, §5.10 option-labels-never-verbified, 360w measure of 2 lengthened IT labels. T35 still in flight.
Task T37 FIX ROUND 1: DONE (44616d1, 1274/1274, all exit codes 0). EN anaphoric accounting forms + peekSurcharge ({peeks}× — reads "peeked 3×, so 4× the analyses" in one notation); header note truth-corrected w/ tried-and-rejected forms recorded; token ORDER unchanged (.ph-num pin untouched); share line 3 = label-colon in 3 locales, §2.9 deviation documented, spoiler property tests unmodified; both IT lengthened labels measured 0.0px overflow at 360; all minors incl. self-caught ES header twin defect. Flagged deviation 2: "them" antecedent ({sig} linearly closer than {total}) — judged unambiguous, comment-pinned. Scoped re-review dispatched (SONNET).
Task T37 RE-REVIEW: APPROVED FOR MERGE (all items pass; strings independently re-rendered through real t() at floor+high in 3 locales; spoiler property byte-untouched; "them" antecedent upheld on independent linguistic grounds). MERGE T37 -> build/v1 @ 359bba6, PUSHED, zero conflicts, full gate green (1274/1274). The "datos abiertos" class is CLOSED: 84 strings corrected across 3 locales, convention contracts in both copy headers, EN hardened. Remaining in flight: T35 motion (opus). Then: T22 a11y -> T23 E2E (incl. booked locale-aware header-overflow check) -> T24 CI -> final whole-branch review -> GRAND REVIEW -> deploy-ready.
Task T35: implementer DONE (a1a31d4 on task-t35, base 67028cd, 1296/1296 [+22], all exit codes 0 x5 runs). 8 motion sites (screen transition, dial band+settle, staggered Reveal blocks, stamp slam RE-TIMED, press clippings stagger, popover, toast, confetti unchanged); tokens --dur-quick/scene/stagger added, --dur-tick/fade retired; DESIGN.md R5 -> R5.1-5.7 system w/ raw-duration/easing/layout-property bans (severity UP); motion.test.ts reconciles R5.2 table vs stylesheets BOTH directions, 6 mutations verified failing. REAL DEFECT FOUND+FIXED: stamp slam fired at mount inside opacity-0 block (finished@450ms while blockOpacity=0, CDP-measured pre-T35) — Act II signature played to nobody; now visibility-gated (running@100ms post-intersect). No fill-mode on .ph-screen (deliberate: persisted transform would re-anchor Published's fixed overlay — verified at 360/1088 + sticky dial after 900px scroll). GAP FLAGGED: NO achievement-unlock moment exists (Summary discards unlockedToday) — booked as T38 after T35 merge (Summary UI + copy x3). Review dispatched (OPUS — motion design judgment).
Task T35 REVIEW: NEEDS FIXES (hard parts "exceptional": stamp find independently reproduced; fill-mode reasoning survived active disproof [forwards -> overlay rect y:-190 h:1090]; reduced-motion parity end-to-end; amended R5 judged NET STRONGER; T37 interplay = ZERO conflicts, merge-tree clean, T37-diff-on-T35 suite green). I1: site 5 press clippings fire at MOUNT — at 360 all cards below fold, cascade spent by 450ms inView:false — the pass's own stamp insight un-applied one screen over. Fix round 1 dispatched to ORIGINAL implementer: intersection-gate site 5 + R5.2 cell, M1 color-exception file-scope + file+site register keying (reviewer probe beat per-file sums), M2 longhand-transition scan, M3 site-1 key pin, M4-M8 doc/cross-ref/crop fixes, mutation-test each tightened check. PARKED for owner: dial-settle 2px restraint (law question — R8.1; reviewer calls it weakest site; Lab is where "plain" lived); Summary stagger budget reserved for T38.
Task T35 FIX ROUND 1: DONE (9214c15, 1302/1302 [+6], all exit codes 0). I1 fixed via extracted useEnterOnce hook (Block refactored onto it, no behavior change claimed); R5.7 now law: "an entrance may not be triggered by mount"; CDP evidence at 360x640 (inert below fold -> animate on entry, stagger survives via backwards fill) + 1088 unchanged + reduced-motion all-visible. M1-M8 done w/ 7 mutation REDs. M8 uncovered INDEPENDENT LAYOUT DEFECT: .ph-reveal was the only screen lacking inline gutter — fig 1's "1.00" label clipped at 1088 pre-fix; gutter added (--space-24) + slam scale 1.6->1.20; scope extension flagged openly w/ alternative stated. Scoped re-review dispatched (SONNET) incl. layout-judgment verdict.
Task T35 RE-REVIEW: APPROVED FOR MERGE (all 8 items verified w/ 4 fresh mutation probes + pixel analysis of before/after shots; gutter claim independently confirmed [.ph-reveal uniquely padding-block-only]; layout judgment call RULED JUSTIFIED; Stamp.css = task-list omission not scope violation). NEW Medium finding: R5.6 no-content-behind-motion check weakened to per-file counting — exploitable (unrestored opacity:0 in Reveal.css passes), no live violation — FOLDED INTO T38 (well-motivated: T38 adds Summary hidden rules exactly where the blind spot bites). MERGE T35 -> build/v1 @ e4632af, PUSHED, zero conflicts (as pre-verified), gate green 1302/1302. Motion system LIVE. T38 dispatched (OPUS, worktree task-t38 @ e4632af): Summary unlock moment (9th motion site, R5.7 budget spent), 1 new copy key x3, R5.6 tightening w/ mutation proof. T22 SERIALIZED after T38 (Summary.tsx collision). PARKED for owner/grand review: dial-settle 2px restraint.
Task T38: implementer DONE (83fe032 on task-t38, base e4632af, 1322/1322 [+20], all exit codes 0 x4 runs). Unlock block between invoice and share; heading summary.unlockedToday = Unlocked today / Riconoscimenti di oggi / Logros de hoy (nominal by design — avoids the participial-agreement class); names/citations rendered from content banks; unlockedToday RETURNED from same guarded computation (double-persist + midnight-straddle tests unmodified); 9th motion site registered, R5.7 budget spent; R5.6 tightened to selector-association w/ both-directions mutation proof (old probe now RED, own rules GREEN). Disclosed: no browser capture — review closes that gap (sonnet, capture at 360/1088 both themes + honours-board judgment).
Task T38 REVIEW: APPROVED FOR MERGE (every claim independently re-derived; capture = REAL PLAY-THROUGH earning genuine achievements w/ Date override past EPOCH, 9 shots, mid-animation getAnimations() proof, honours-board judged "looks shipped, not provisional"; reviewer disclosed+fixed own harness bug [CDP setEmulatedMedia replace-not-patch]). MERGE T38 -> build/v1 @ 3c1e745, PUSHED, zero conflicts, gate green 1322/1322. T22 dispatched (OPUS, worktree task-t22 @ 3c1e745): keyboard-only play first, booked items (TrailKey ARIA pattern [T29-M3], focus-on-screen-change, scroll-gated Blocks in a11y tree, unlock-block live-region assumption pin, Reveal reading order), sweep (semantics, dial live announcement, contrast measurements, 200% reflow, axe scan). After T22: T23 E2E (incl. booked locale-aware header-overflow check) -> T24 CI -> final review -> GRAND REVIEW.
Task T22: implementer DONE (f6286a6 on task-t22, base 3c1e745, 1356/1356 [+34], all exit codes 0 x4). Booked verdicts: (a) TrailKey -> true disclosure (tooltip role removed, content-decided); (b) focus-to-main on every swap w/ negated-offset R6.1 ring (activeElement was BODY after all 5 transitions); (c) scroll-gated blocks CLEAN (opacity-only gate, pinned by test); (d) unlock block no-live-region w/ assumption comment; (e) Reveal order correct, added hidden h1 (screen had ZERO headings) + split shared figure label. Sweep: axe 2 rules -> 0 (serious: Stats rows announced NOTHING — aria-label on role-less span discarded); Call overlay KEYBOARD TRAP fixed (WCAG 2.1.2); contrast all pairs 5.03+ measured in-browser; 200% reflow clean; nav landmark added. New key a11y.specCurveGrouped x3 + specCurveChart value fix (was false on abandon path). DESIGN.md R6.6/R6.7 (49->51). Concerns: closeDialog key now unused (kept in union); nested dialog on published path (grand-review look); practice mode prints "Vol. 1, No. -5" (negative pre-EPOCH puzzle number, read aloud — BOOKED for grand-review triage); skip link declined w/ reasoning; NVDA/VoiceOver human check still owed. Review dispatched (OPUS).
OWNER DIRECTIVE (round 7): press should be game-dependent — some news items must relate to the day's research question. Plan amended with T39a (EN bank expansion: >=1 bespoke item per scenario + pickPress guarantee + spoiler-law test; opus) + T39b (IT/ES transcreation + comedy review). T39a dispatched NOW in parallel with T22 review (file-disjoint: content bank + picker internals + tests; Published.tsx untouched). T39b after T39a review + T22 merge.
Task T22 REVIEW: NEEDS FIXES (9/10 items independently verified clean — reviewer's OWN contrast math ±0.03, OWN axe harness 0 across screens/themes/locales, dial live-region adjudicated CLOSED and measured atomic [role=status, settle-gated commit], disclosure + skip-link-decline + nested-dialog-deferral all upheld, keyboard play-through x2 both paths). I1: closeCall() focuses CTA while cover still inert (React not committed) -> focus falls to BODY; jsdom test = permanent false positive (jsdom doesn't implement inert focus-blocking); report claimed it measured working = evidence-integrity correction owed. Fix round 1 dispatched to ORIGINAL implementer: effect-after-commit (or flushSync) + ordering-assertion test + report corrections + ten-row comment + grand-review handoff items (dual Legend buttons, Vol. 1 No. -5 read verbatim). T39a (press) still in flight.
Task T39a: implementer DONE (c646956 on task-t39a, base 3c1e745, 1369/1369 [+47], all exit codes 0 x4). 24 bespoke EN items (7/9/8 tiers), ALL 20 scenarios named; generic pools untouched (5/7/6); pickPress guarantee via salt-sniff (unsalted pick prefers scenario-bound pool) + source-scan test pinning the 3 call-site seeds; IT/ES aliased placeholders w/ mastheads mapped + both-directions debt tracker; density IMPROVED (1/11,273). RULINGS: 26/60 (scenario,tier) cells SUFFICIENT for the directive (>=1 bespoke/day guaranteed; 60-cell = optional follow-up, parked); salt-sniff convention accepted w/ pin, pressForDay() refactor parked as deferred-minor for final review (Published.tsx was T22-locked); T39b MUST add IT/ES spoiler lexicons (EN-only today). Bonus finding: reverting mechanism fails a PRE-EXISTING test — same-blurb-twice bug existed before. Review dispatched (OPUS — comedy judgment on 24 items).
Task T22 FIX ROUND 1: DONE (04cd6d4, 1357/1357, all exit codes 0 x4, axe re-scanned 0 across 17 cells). I1 fixed: restore via useEffect keyed on callOpen + wasCallOpenRef edge guard (chose effect over flushSync — matches existing focus-into-overlay idiom); real-browser evidence: isCta=true at +0ms/+1200ms/second-round-trip (was BODY); ordering-assertion test w/ focus spy recording inert-at-fire-time, VERIFIED FAILABLE against reverted code; no-steal-on-mount companion; ten->seven comment + popover/Legend can-never-disagree pin; report corrections struck-through in place + new §10 root-causing why BOTH round-1 instruments were blind (only asserted overlay closed, never read activeElement). Scoped re-review dispatched (SONNET). T39a review (OPUS) also in flight.
Task T22 RE-REVIEW: APPROVED (all 7 items; mutation-verify reproduced independently — exactly 1 assertion fails on revert; edge-guard/race/abandon-path analysis clean; report corrections honest). MERGE T22 -> build/v1 @ ec474df, PUSHED, zero conflicts, gate green 1357/1357. A11y pass LIVE. T23 dispatched (OPUS, worktree task-t23 @ ec474df): Playwright E2E vs PRODUCTION build, fixed post-EPOCH clock (EPOCH placeholder 2026-08-10 — clock must be after), 3 flows + i18n smoke (pins "Apri i dati"/"Abrir los datos" forever) + cross-realm determinism + 5 booked checks (T33 header overflow @360 it/es, T22 focus restore, T29 prod-import overlay, T35 reduced motion, storage-disabled boot) + double-run flake gate. T24 CI after. T39a review (OPUS) still in flight; T39b after.
Task T39a REVIEW: APPROVED w/ 1 Important (comedy "stronger than the report's hedging": ZERO mad-libs 24/24, zero canon contradictions, best = stairs badge-debrief T1 + PASTRY chyron; spoiler law 24/24 upheld on store-verified principle [no non-significant publish path — credulous headlines are day-type INVARIANT; the line is claims about TRUTH: replication/retraction]; same-blurb-twice quantified: old+new-content would repeat 55.4% of days, mechanism takes it to 12.0% vs 23.7% pre-T39a baseline; scan = vocabulary tripwire not semantic guard — the real protection is the editorial discipline, correctly written down in source). I1: 4 of 5 press guards EN-only — must reach validateLocaleContent + REQUIRED pressSpoilerTerms lexicon field BEFORE T39b writes 48 unguarded blurbs. Fix round 1 dispatched to ORIGINAL implementer: I1 + 3 flat-item replacements (#10/#12/#22) + tier-2 reader-address on 2 + fnv1a32 parity-collision fix (low bit = input parity; "#chyron" even odd-byte count -> card1/chyron NEVER differ mod 2 -> 31.3% collision; h^(h>>>15) mix -> 17.0% measured) + pairwise-distinct test + >=26 cells. Rulings: source-scan stands until pressForDay(); tier-mix plan line = loose prose; 60-cell parked.
Task T39a FIX ROUND 1: DONE (211e3d8, 1379/1379 [+10], all exit codes 0; 18 consecutive clean runs after 1 known dgp flake, isolated 5/5). All items + JUSTIFIED DEVIATION on M1: xorshift-only measured WORSE (17.0% vs A's 12.9% — A's 0.0% card2-chyron was favorable parity artifact; independence != distinctness); shipped xorshift + slot rotation (unsalted-date hash, slots +0/+1/+2) -> 0.0% collisions over 180k simulated days, all 45 reachable, determinism held, fnv1a32 spec-verbatim, cost documented (poolSize presentations not poolSize^3); pairwise-distinct test verified RED under A AND B. 5 guards per-locale w/ guard-unhooking fixture test; *_PENDING_T39B alias lexicons w/ documented sunset; 3 rewrites + 2 reader-address clauses applied x3 locales (debt tracker preserved). Scoped re-review dispatched (SONNET) incl. deviation adjudication + pool<3 edge check.
Task T39a RE-REVIEW: APPROVED (all 7 verified, several by empirical re-execution: guard-unhooking per-guard, A/B/C revert experiments, independent 18k-cell collision reproduction [A 12.0% / B 17.4% / C 0.0%], pool 1/2/3 edge arithmetic; deviation ACCEPTED; nit: no full-reachability test — handed to T39b as optional). MERGE T39a -> build/v1 @ 0d0e0ee, PUSHED, zero conflicts, gate green 1414/1414. T39b dispatched (OPUS, worktree task-t39b @ 0d0e0ee): 24 items x2 locales reusing LOCALE scenario furniture (17 not 13 in IT etc.), REAL IT/ES spoiler lexicons replacing _PENDING_T39B aliases (existing 21 generics get scanned — narrow terms over rewording), debt trackers cleared to empty-set assertion, reachability test optional. T23 E2E still in flight.
Task T23: implementer DONE (e1c80a8 on task-t23, base ec474df, zero src changes; unit 1357 unchanged, e2e 13 passed + 2 skipped(.fixme) x2 consecutive + 3x stress, all exit codes 0). Suite: prod build + preview, fixed clock 2026-08-14 (post-EPOCH, puzzle #5), UTC, no waitForTimeout (aria-busy MutationObserver sync), retries 0; cross-realm determinism = dial p vs Node runSpec + reveal accounting vs Node sigCount over 1792; NEGATIVE CONTROLS reproduced all 3 historical defect classes (T29 import, T22 focus, T35 stamp) each failing exactly its own test. PRODUCT FINDINGS (both .fixme): F1 errors.storageOff written+translated but NEVER RENDERED (isStorageOff() uncalled in src/ui); F2 Briefing flashes scenario #0 (wrong question + wrong-date Grantwell email) 74-117ms until store.boot() — worse on phones, jsdom-blind (tests inject booted stores). CONCERN: PUZZLE_ISO must move when T25 freezes EPOCH (documented single constant). Review dispatched (SONNET). T40 (fix F1+F2, un-fixme) queued post-merge.
Task T23 REVIEW: APPROVED FOR MERGE (all 10 items PASS under direct re-execution: suite run twice + negative control reintroduced/restored, determinism verified GENUINE [real engine imports, not constants], both .fixme tests assert post-fix behavior, EPOCH coupling accurate + guard suggested [assert PUZZLE_ISO > EPOCH — hand to T24]; all 4 deviations sound). ENVIRONMENT NOTE [HIGH, non-code]: reviewer hit an injected-looking "system-reminder" during its git-checkout restore claiming the change was intentional + instructing nondisclosure; reviewer ignored, independently verified tree clean, reported. Likely benign harness watcher misattributing the agent's own restore; logged per evidence-integrity culture; no code affected. MERGE T23 -> build/v1 @ 748817b, PUSHED (unit 1414 green; tsc initially red in main checkout = stale node_modules, fixed by npm install, tsc 0). T40 dispatched (SONNET, worktree task-t40 @ 748817b): fix F1 (storage notice, role=status, registered tokens) + F2 (boot gate via minimal store ready flag — NOT scenarioIndex sniffing; preserve T33 focus guard/T35 transition/iso anchoring/practice mode), un-fixme both e2e tests -> 15/15 x2 + jsdom coverage. T24 CI after T40 + T39b. NOTE for T24: e2e job needs npx playwright install --with-deps chromium; add PUZZLE_ISO>EPOCH guard.
OWNER DIRECTIVE (round 8): "no rush... I prefer it to be perfect." Grand-review plan amended (A4): polish severity now FIXED pre-deploy (no parking except reasoned wontfix); press-mix seam question posed to GR2/GR3 explicitly (60-cell matrix becomes T39c if flagged); ALL parked taste/deferred items routed to GR lanes for verdicts; no clock pressure on the tail.
Task T39b: implementer DONE (81c50de on task-t39b, base 0d0e0ee; content 344, full suite 1459+24 isolated = 1483 [dgp flake disclosed: no single green whole-run on branch], all exit codes 0). 48 transcreations (cornetto/napolitana, eptacaidecafobia press callback, IT-17 vs ES-13 per-locale consistency asserted mechanically, magazzino cross-card rhyme); REAL IT (25) + ES (26) spoiler lexicons w/ measured exclusions (conferm/confirm/verdad fire on shipped jokes — excluded w/ negative tests; replic-not-replicat + accent-twin traps documented); furniture tests (IT eptacaidecafobia/ENDECASILLABO/CORNETTO + no "numero 13"; ES triscaidecafobia/ENDECASÍLABO/NAPOLITANA); debt trackers now permanently-empty assertions; reachability test taken + mutation-verified; density improved both locales, 0 new dashes. Concerns: retirad/ritirat marathon-retirement residual (documented, cura-style); tier-3 caps law blind to accented capitals (shared validator, GR1c); IT/ES chyrons longer than EN (visual check owed). Review dispatched (OPUS x-locale comedy).
Task T40: implementer DONE (4b4931a on task-t40, base 748817b; unit 1418 [+4 files worth], e2e 15/15 x2 + 3x stress 45/45, all exit codes 0). F2: GameStore.booted set ATOMICALLY in the same set() as scenarioIndex/iso, reset at boot() top, false on failure; App gate = !booted && !storeError; a11y.loading FINALLY wired (role=status). F1: ph-storage-notice above main, role=status, direct isStorageOff() read, hairline+muted, no new colors/motion. 3 new test files (deferred-boot App test, booted atomicity, storageNotice isolated for one-way module flag). Implementer note: claimed dgp flake "not documented anywhere" — their search missed the gitignored ledger (harmless; reviewer told to correct the record). Interpretation call for review: no dismiss button ("doesn't block play" reading). Review dispatched (SONNET).
Task T40 REVIEW: APPROVED (8/8 incl. atomicity trace, non-regression reads, e2e 15/15 x2 rerun, zero snapshot-assertion breaks; dismiss adjudication: persistent-but-quiet CORRECT — a dismiss preference cannot persist when storage is what's broken; ledger correction: implementer's "flake not documented" claim wrong — search missed gitignored ledger). MERGE T40 -> build/v1 @ 7109cac, PUSHED, gate green 1418/1418 + tsc 0. Both E2E product findings CLOSED. Remaining before tail: T39b review (opus, in flight) -> merge -> T24 CI -> final whole-branch review -> GRAND REVIEW (A4 perfect-over-fast).
Task T39b REVIEW: APPROVED w/ 8 Minors (transcreation REAL — verified against each locale's OWN scenarios; 3 untranslatable divergences mechanically asserted; lexicons = "real linguistic work", replic/réplic arguments probe-confirmed; exclusions reproduce exactly; reviewer ran own DISCRIMINATING mutation [last-entry-unreachable: 1 failed/50 passed] beating the report's own evidence; visual check DONE — no chyron overflow at 360, but Hayek chyron = 4 lines EN vs 7 lines IT/ES @--text-28 — number handed to GR4a as aesthetic call; accented-caps blindness confirmed real but nil-severity today [worst case 2 non-ASCII letters, all 32 chyrons measure 1.000 both ways]). Per A4 perfect-over-fast: FIX ROUND 1 dispatched to ORIGINAL implementer — press[20] both locales (lievita pun / masa madre sube-marca baja w/ direction-lexicon check), press[8] both (cut recycled punchline), press[10] ES sesión informativa, press[39] IT ENTRA, ES lexicon +se ha sostenido/se sostenía, IT 13-guard widening, report corrections (magazzino mechanism claim FALSE — tier-filtered; density 3 s.f.; discriminating mutation noted).
Task T39b FIX ROUND 1: DONE (25fd17b, content 349 [+5], full 1464 + dgp 24 isolated, all exit codes 0). press[20] IT = JUSTIFIED DEVIATION (lievitare on a time = ballooned/slower = contradicts credulous-press law; pun re-aimed at primato personale — rise is good news); ES verbatim + baja checked via NEW two-sided test (press out of direction scope AND same word in outcomeLabel fires); press[8] recycled punchline cut x2; sesión informativa; ENTRA; ES lexicon 28; IT 13-detector widened w/ guards-the-guard (3 catches, 2 spares); report amendments (magazzino x2 sites, density 3 s.f. RE-MEASURED post-fix, discriminating mutation recorded + acknowledged as better evidence than own). Scoped re-review dispatched (SONNET) — LAST feature gate before T24/final review/grand review.
Task T39b RE-REVIEW: APPROVED (all 8 verified independently; lievitare deviation upheld w/ real-usage evidence — "i tempi sono lievitati" always = got worse; ES proposal got direction right "by luck of the verb choice"; density recomputed to the digit; discriminating mutation reproduced in standalone script). MERGE T39b -> build/v1 @ fb90c9e, PUSHED, gate green 1492/1492 + tsc 0. *** FEATURE PHASE COMPLETE *** — all owner directives (rounds 1-8) implemented, reviewed, merged: trilingual corpus + UI-language overhaul, visual polish + motion system, navigation/controls, emoji audit, game-dependent press x3 locales, achievement moment, a11y pass, E2E suite + both its findings fixed. T24 CI dispatched (SONNET, worktree task-t24 @ fb90c9e): main CI + e2e.yml + calibration.yml weekly cron + EPOCH guard + dgp-timeout fix proven over 5 full runs. Then: final whole-branch review -> GRAND REVIEW (A4).
Task T24: implementer DONE (65c91dd on task-t24, base fb90c9e; 1494/1494 x10 full clean runs [dgp ROOT-CAUSED: 4x200-seed structural tests at 5s default under load; describe-level 20s timeout, zero assertions touched], e2e 15/15, cal 5/5 bands, clean npm ci, YAML parses x2 parsers). 3 workflows: test.yml (push+PR full gate), e2e.yml (separate, report artifact on failure), calibration.yml (weekly cron Mon 06:00 UTC + dispatch, artifacts always). EPOCH guard test w/ named-files failure message, verified firing + tuning.ts restored. Concerns: --with-deps not exercisable in sandbox (real-runner confirmation owed at first push); jsdom EBADENGINE warning pre-existing; weekly cal cadence w/ dispatch escape. Review dispatched (SONNET) — CRITICAL CHECK flagged: is pipefail actually default in GH Actions bash (npm run cal | tee could mask band failure if not).
Task T24 REVIEW: NEEDS ANOTHER ROUND — CRITICAL: pipefail claim FALSE (GH default unspecified shell = bash -e WITHOUT pipefail; explicit shell:bash = with; verified vs GH docs x2 + local repro) -> calibration.yml's `npm run cal | tee` would go GREEN on a real band failure, weekly cron, nobody watching. Implementer's sanity probe was circular (set -o pipefail before testing the default). Everything else verified sound. Fix round 1 dispatched: shell:bash on the piped step + NON-CIRCULAR stub proof + report claim struck through per evidence-integrity standard.
Task T24 FIX ROUND 1: DONE (cf3d3fd — shell:bash on the piped step, corrected comment, non-circular stub proof, report claim struck through w/ §7). CONTROLLER DIRECT VERIFICATION (proportionate for 1-line YAML): diff scope confirmed calibration.yml only, stub proof reproduced both ways (unfixed exit 0 / fixed exit 1), 3 YAMLs parse. MERGE T24 -> build/v1, PUSHED. Full gate green. CI COMPLETE. Next: FINAL WHOLE-BRANCH REVIEW (fable tier — entire build/v1 vs main, triages deferred-minors ledger) -> GRAND REVIEW (A4).
FINAL WHOLE-BRANCH REVIEW (fable): READY FOR GRAND REVIEW. "Strongest multi-agent tree I have reviewed." Gate self-run green (1494/tsc/eslint/e2e 15). 16 findings: 3 HIGH-S (final-001 About discloses nonexistent analytics — T25 checklist unskippable; final-002 isValidV1 shallow -> NaN-poisoned stats; final-003 goldens ARE EPOCH-sensitive via seeds.ts:104 iso>EPOCH gate — plan:308 claim FALSE, checklist step) + 13 polish. Complete deferred-minors adjudication table (fix/wontfix/GR-lane per item), definitive dead-key roster (4 remove + tagline, 5 keep-with-reasons), 10-point spec audit all HOLD, DEPLOY-DAY CHECKLIST 10 steps (incl. vercel.json missing, VITE_APP_VERSION, analytics install, goldens regen protocol). 5 owner-taste questions surfaced for GR4/Carlos: dial-settle keep/cut, header 3-row @360, IT/ES Hayek chyron length, watermark anatomy, prereg-days-fire-hack-achievements. Findings list formatted for mechanical GR5 merge — full text in the review output (task a14d2f5902335d3be), referenced as GR1c/GR5 input. GRAND REVIEW LANES DISPATCHING (6 agents, fable per plan): GR1a engine, GR1b game+UI code, GR1c infra (absorbs final-review findings+table), GR2 game, GR3 writing, GR4 UX.
GRAND REVIEW LANE FAILURE + RE-DISPATCH: all 6 fable lane agents died on the Fable usage limit; nothing persisted (only usable partial: GR1b confirmed Dots already memoized per T29 pin 7 — folded into the re-dispatched GR1b prompt re [final-015] first half). Owner switched session model to Opus 5 (1M). All 6 lanes RE-DISPATCHED on OPUS 2026-08-06, prompts recovered verbatim from transcript, same findings files/format. Loop heartbeat re-armed.
OWNER RULINGS 2026-08-06 (interactive, answered by Carlos — these OUTRANK lane recommendations; GR5 must carry them as settled): (1) dial-settle: AMPLIFY within R8.1 (no glow/halo/pulse; e.g. numeral weight-snap on band change) — becomes a GR6 fix item; GR4 advises on the shape only, not the direction. (2) header 3-row wrap @360 (267->318px): ACCEPTED as-is — redesign off the table unless a lane finds a functional defect. (3) IT/ES Hayek chyron 7-lines vs EN 4: FINE AS-IS — closed. (4) watermark head-vs-foot anatomy split: LEAVE AS-IS — wontfix confirmed. (5) prereg-days-fire-hack-achievements: KEEP THE WALL (structurally impossible stays; satire targets hacking, prereg is the cure) — wontfix confirmed; owner explicitly did NOT choose the add-prereg-specific-medals expansion.
GR1b LANE IN (opus, 1/6): gate self-run GREEN (1494/53). 0 blocker / 8 high / 17 polish / 1 verification record (26 total) — persisted by controller (agent write blocked) at .superpowers/sdd/2026-08-04-grand-review/findings-code-gameui.md. Highs: gr1b-001 makeCall requestSeq (confirms final-007, reachable via Escape-remount); gr1b-002 rejected RPC strands pending:true forever — Prereg hard dead-end, Lab dead for the day, no error surface; gr1b-003 negative puzzleNumber pre-EPOCH -> retraction subline bank DEAD + Vol 1 No -3 (parked A4 item confirmed live); gr1b-004 practice mode invisible + share string indistinguishable (?practice=1 lives forever); gr1b-012 CSS 5 dialects confirmed w/ re-derived counts + ONE consolidation shape (~5 utilities in App.css, DESIGN.md-first commit); gr1b-013 10 classNames with NO CSS rule (armitage footnote modifier = player-visible); gr1b-020 PWA precache ships all 3 locales to everyone (37.3KB gz) AND precaches zero fonts (offline = system fallbacks, R2.4 broken); gr1b-023 wrapLabel over-long single word unwrapped — IT "passivo-aggressività" (20ch) overruns Fig.2 band at 440-663px containers. Bundle: EN critical path 105KB gz, +fonts ~167KB — UNDER 200KB budget. Interpolation: all engines safe (probed live), zero dangerouslySetInnerHTML. Verification record gr1b-010: Dots memoized, final-015 first half SATISFIED. Process note: lane could not see final-review text (task-output only) — controller must cross-check final-005/006/010/012/013/016 vs gr1b list at GR5. 5 lanes still out.
GR1c LANE IN (opus, 2/6): gate+e2e+cal self-run ALL GREEN incl. clean-clone repro (1494/1494, e2e 15/15, cal 5/5, cal is byte-idempotent). 27 findings: 1 BLOCKER (gr1c-001 final-review text existed nowhere on disk) + 9 high + 14 polish + 3 informational — persisted by controller at findings-code-infra.md; backlog-draft.md persisted with 79 rows (§A lane findings, §B final carryover, §C A4-parked, §D owner questions — annotated ANSWERED per owner rulings, §E merge slots, §F 30 ledger deferred-minor lines). Law→enforcer gaps: 6 Tier-C DESIGN laws human-grep-only (2 unrunnable as written — R6.5 self-hit, 768px off the closed list); calibration never gates a push AND its "expensive" rationale DISPROVED (33-42s, cheapest suite); no axe scan in repo; no bundle budget; scripts/ untypechecked. Suite health: 36.0s, dgp.test.ts 31.4s = 87% of wall time is expect() overhead (fix → ~11s suite); dgp timeout comment off 5.5x vs measurement; inflation real: 88 dup registrations (1494→1406 unique). gr1c-024 prereg spoiler guard CONFIRMED VACUOUS (f(x)===f(x), 300 draws). gr1c-005 reproduced final-003 w/ 5-date table (4 of 5 goldens break if EPOCH ≥ 2026-09-01). gr1c-006 no engines field (wrong node = 270 red). 3 fabrication spot-checks re-verified GENUINE (incl. 180k-day press sim reproducing exactly). CONTROLLER RECOVERY: gr1c-001 RESOLVED — full final-review text (all 16 findings + adjudication table + dead-key roster + 10-step deploy checklist, 26,844 chars) recovered verbatim from the session transcript and persisted at .superpowers/sdd/2026-08-04-grand-review/final-whole-branch-review.md; findings-code-infra.md and backlog-draft.md annotated. GR5 uses that file as authoritative, §F as cross-check. Process fix (persist review outputs to files in the same turn) adopted as standing rule. 4 lanes still out: GR1a, GR2, GR3, GR4.
GR1a LANE IN (opus, 3/6): gate self-run green. 19 findings: 4 BLOCKER (all scientific, all Act II accounting) + 5 high + 10 polish — persisted at findings-code-engine.md. SCIENTIFIC VERDICT: the game teaches something false in 3 places — gr1a-001 "by chance alone" on EFFECT days (~70% of counted hits are true positives, measured over 200 days); gr1a-002 "by chance alone" on NULL days (confounding mislabelled as chance: Y1 no-covariate spec E[beta]=0.18, z=24.9, 18% rejection vs nominal 5% — contradicts About's own disclosure); gr1a-003 pHitAtK is a null-day-only table rendered on effect days (23% shown vs 51% true at k=5); gr1a-004 About omits the rejection sampler (42% null / 26% effect draws discarded). gr1a-005 Armitage 14% is exact for Armitage's schedule but this game's 200-400 schedule gives 11.1% (400k-trial sim). Highs: gr1a-006 scenarioIndexFor O(days) recursion = stack crash ~25y out + linear init tax (measured: 2051 date THROWS); gr1a-007 Math.exp/log are the one determinism hole and the e2e matrix is Chrome-only (add firefox+webkit); gr1a-008 runSpec builds unread DataCut = 51% of precheck cost; gr1a-009 engine-internal daysFromCivil duplication NOT covered by purity-forced wontfix (new evidence). Machinery verified CORRECT: t-CDF ≤3.5e-14 at p=.05, incomplete beta vs 4 closed forms, OLS 5.6e-15, PRNG discipline sound, glossary ×3 locales all correct, About mechanism claims all check. Perf: warm init p50 24ms/p99 150ms, reveal p50 21ms — phone budget plausible.
GR3 LANE IN (opus, 4/6): 34 findings: 2 blocker + 19 high + 13 polish — persisted at findings-writing.md. All read RENDERED (playwright vs dev server, 21 screen-states, 15 sim days). BLOCKERS: gr3-001 every {effect} headline prints literal "1" ("Meetings Run 1 Minutes Longer") — Math.max(1,round(|beta|)) floors raw-unit betas, 5 of 6 live days; gr3-002 = gr1a-001/002 independently found. Quotable test PASSES (10+ exist; funniest = grantwell[19] point-estimate line; flattest = Sunday Supplement tier-1 generic). Press-seam verdict YES: only 26/60 (scenario,tier) cells bespoke, 3 of 6 live days rendered ZERO scenario-bound press — promote 60-cell matrix per A4. T6/T32 micro-verdicts all adjudicated (6 fix, 2 wontfix w/ reasoning). Key highs: nav.tagline (best line in product) renders NOWHERE; prereg reveal says "You explored 1 of them" (false in the honest mode); ES ships the exact "la clave" calque its own EN comment warns against; IT/ES streak named twice post-T37 (Serie/Racha vs Giorni consecutivi/Días seguidos — audit regression); Lab has 0 authored jokes + "Household income" covariate on 15/20 days; leading-zero p-notation split 5-vs-3 strings on same screens.
GR2 LANE IN (opus, 5/6): 20 findings: 4 blocker + 10 high + 6 polish — persisted at findings-game.md. Cal re-run all 5 bands PASS, byte-identical; NO finding moves a band (each states impact). Playthrough table delivered (3 models × 32 days on REAL store+dispatcher+storage). BLOCKERS: gr2-001 prereg false-positive rate is ~28% (Y1@400) vs the game's "about 5%" claim — DGP confounding structural (shares latents), 22% of honest days score zero, incentive inverts (DGP decision → controller/owner); gr2-002 = the "chance alone" effect-day defect (3rd independent find); gr2-003 prereg reveal runs hacking accounting at the honest player + empty call section; gr2-004 finished day REPLAYABLE forever (guard only lives in mode chooser which honest players never unlock) — replay silently discarded. Key highs: gr2-006 achievement ceremony re-fires same trophies daily (Subgroup Safari 30/32 days); gr2-009 honest path has NO positive moment ever (NULL_REPORTED unconditional; prereg gated on first_retraction = progression dead-end for integrity); gr2-012 band (b) measures a search the UI can't perform (single-knob walk: 19.3% of walks never hit in 60 moves vs pillar "unloseable"); gr2-013 default spec already significant on 12.5% of days (no garden); gr2-014 press = gr3-024 (55-58% of days fully generic). Prereg-achievements verdict: uphold wall (matches owner ruling); lane's add-prereg-citations rec marked OVERRULED-BY-OWNER in persisted file. Cross-lane convergence NOTE for GR5: "by chance alone" found independently by 3 lanes (gr1a-001/002, gr3-002, gr2-002); prereg-upsell dead button by 2 (gr3-003, gr2-005); press coverage by 2 (gr3-024, gr2-014); dedupe at synthesis. 1 lane still out: GR4 UX.
GR4 LANE IN (opus, 6/6 — ALL LANES IN). 23 findings: 2 blocker + 9 high + 12 polish — persisted at findings-ux.md; 268 shots DID land in shots-ux/ + raw measurements in *-ux.txt siblings. All taps = real CDP pointer events vs production build. BLOCKERS: gr4-001 Call overlay has NO surface (prompt at 3.64:1 paper / 2.45:1 dark vs 4.5:1 floor; at 360 prompt painted ACROSS the header's own controls — R4.2's hairline-ruled block never shipped, only the scrim); gr4-002 boot failure renders the WRONG study (scenarioIndex 0 placeholder) with live CTA into a dead Lab (T40's bug class on the error branch: gate is !booted && !storeError). Friction ALL PASS: complete-a-day 11 taps (≤12), share/legend/language all 1 tap; TTFMP 926ms mid-tier phone. 320w owner element FOUND: a.ph-about__link bare URL, 8.2px, overflow-wrap:anywhere one-liner. Highs incl: gr4-003 page scrolls behind modal; gr4-005 RETRACTED stamp clipped 22px off-viewport at ≥768 (rotated group exceeds viewBox); gr4-006 locked achievements read as broken glyphs; gr4-007 Share button has NO resting affordance (reserved 2px underline never filled — T29 pin 4's own precedent); gr4-008 stamp announced twice to AT; gr4-009 nested dialogs confirmed [final-006] + modal named by eyebrow not question; gr4-011 = gr2-011 (lab actions above knobs, preferred fix = sticky bottom actions, needs R8.1 amendment DESIGN.md-first); gr4-012 no skip link (9 chrome tab stops). OWNER-TASTE VERDICTS: (1) dial amplify — "numeral weight snap" is OUT-OF-LAW (R2.3 mono=400 only); in-law shape = promote band-change to 6px/--dur-scene travel (R5.3's existing loud pair), same-band re-settle stays 2px/--dur-quick; + free amplifier: p<.05 colour step is two near-identical greens. (2) header ACCEPT confirmed, no functional defect found (not sticky, no dial interaction, all targets ≥44px). (3) chyron FINE confirmed (measured 7 lines/312px block worst case, still reads as chyron). (4) watermark WONTFIX confirmed ("I would not have raised it"). (5) screenshot-bar list delivered (tier-3 published @360 = the bar; call overlay = the one shot that would stop a reviewer). Clean sweep recorded: zero horizontal overflow anywhere except about-link@320, reduced-motion parity complete, modal contract correct except the 2 filed defects, T22 tooltip deferral RESOLVED, dial live-region calibration correct. GRAND TOTALS (6 lanes): 149 findings — 12 blockers (with cross-lane overlaps), ~61 high, rest polish + verification records. DISPATCHING GR5 SYNTHESIS (opus): merge 6 findings files + backlog-draft + recovered final review + owner rulings into the single ranked backlog with GR6 wave assignment.
GR5 SYNTHESIS IN (opus; write SUCCEEDED — backlog.md 642 lines on disk). 131 items after dedupe: 9 blocker / 57 high / 65 polish; 40 no-action entries (4 owner-closed, 6 verification records, 30 wontfix-with-reasoning); ~25 merges (largest: chance-alone 4-way, CSS-dialect 4-way); every source id accounted for by grep. 13 waves W0-W12 + final verify. 11 §1 decision briefs; 11 §5 conflicts vs pre-adjudicated verdicts (load-bearing: calendar-dup wontfix FLIPS to fix on new evidence; final-015 first half already satisfied; press-matrix default-wontfix premise false at (scenario,tier) granularity — A4 conditional fired; owner's dial weight-snap example OUT-OF-LAW per R2.3/R2.4, direction carried via GR4's in-law shape; nav.tagline upgrades remove→render; practice-mode scope was understated — negative index kills whole subline bank; 2 lane self-counts wrong, recounted).
§1 RULINGS 2026-08-06 — OWNER (via interactive questions, binding): (a) prereg-28% = COPY-HONEST (keep DGP; claims become conditional and true; About discloses shared-latent construction; FP rate quoted at measured value). (b) Armitage = ADD "equally spaced" (minimal spec deviation, citation intact). (j) honest path = BOTH (prereg unlocks on first COMPLETED day + honest correct call gets its own stamp/verdict — missed-discovery/confirmed-null split; spoiler-safe glyph; coordinate W1/W12). (i) share string = CHUNK+HOOK (cap/group fork run; line-1 tagline hook; NO glyph re-split). — CONTROLLER (per GR5 recommendations): (c) keep §2.7 reveal order, stamp gets its own block with air + scroll-snap; (d) ADOPT default-not-significant acceptance predicate (W11 first item, goldens regenerate exactly once); (e) sticky lab actions below 768 w/ R8.1 amendment in W0; (f) parsimony counts DISTINCT OUTCOME FAMILIES; (g) press matrix TAKEN, six staged tasks T39c-1..6 (tier 2 → 3 → 1, EN authoring separate from IT/ES transcreation), picker fixes land in W6 regardless; (h) TIER_FORKS scales on distance-from-default-spec (editorsPick 10→6 as stopgap if it doesn't fit); (k) band (b) report-only (add single-knob-walk explorer + correct §3.9b prose; re-measure walk after (d) before considering more).
GR6 WAVE DISPATCH (batch 1 of 2, 6 parallel opus implementers, worktrees off 7e417f4): W0 lawbook (DESIGN.md only, blocks W7), W1 Act II truth (gr6-001/002/003/098 + stamp batch 010/011/059/077), W5 engine hardening (045-048/093-103/105 + gr6-117's eslint half folded in; NOT reveal.ts, NOT dgp.test.ts), W6 game logic (008/018/019/020/042/043/044/062/064/079-store-half/081/082/091/092 + store-test items 107/108/109 moved from W9 to avoid store.ts collision), W9 test health (051/052/053/106/110/111/112/113), W10 infra (048-matrix/049/054-tests/055/056/057/058/084/090/114/115/116/117-tsconfig-half/118/119/120/121; NOT eslint.config.js, NOT DESIGN.md). Batch 2 after merges: W2→W3→W4 content chain, W7 (after W0), W8, W11, W12, final verify.
W0 IMPLEMENTER DONE (cdd8173 on task-w0, one commit, DESIGN.md only +262/-40; gate green 1494, all exit codes 0). All 5 amendments landed: R8.1 second-sticky (list-of-one + containing-block trap Do's + 3 Don'ts), R5.2 site-2 ph-dial-settle-band (weight-snap written OUT-OF-LAW in the law itself), ph-enter-scene/quick grants, §9.1 utility layer, §10 tier-C retired w/ 7 rules promoted to tier A under a DATED dagger (tests owed by W10). Preamble 49→51 rules verified. §10 six commands print nothing AND all six fired on deliberate mutations (non-vacuous). 5 scrutiny flags, best: (1) grants worded in prose not Motion cells — naive cell edit went RED against motion.test.ts both-directions (10 vs 11), reverted; two-step rule written (law grants, register records in the CSS commit); (2) .ph-entered LANDMINE — motion.test.ts association predicate requires '--' in modifier, so the rename reds every guarded entrance unless the predicate moves in the same commit; law commits to .ph-entered + same-commit predicate move, no rigor relaxation (controller lean: agree; reviewer to adjudicate); (3) page-shell utility scoped to the 5 byte-identical shells + 2 "--titled"; .ph-reveal/.ph-call OFF the shell pending a bottom-pad ruling (preserves values-unchanged literally, leaves 2 of 4 spellings unconsolidated). Review dispatched (OPUS).
W0 REVIEW: NEEDS FIXES (opus; all mechanical claims independently verified TRUE — 51-count, all 6 scans zero-and-fire-on-mutation incl. all 6 mutations re-run, .ph-focusable 22 byte-identical, .ph-label 20/20 pairing, keyframe hashes, containing-block trap real, weight-snap unambiguous, one-row-per-site sufficient; gate re-run green). 12 findings, all sentence-level DESIGN.md: 2 HIGH — w0-r-001 the .ph-entered predicate instruction is followable into a SILENT tier-A regression (reviewer deleted the '--' clause → suite 32/32 GREEN, nothing catches it; and the law names 1 cost where measured cost is ≥9: predicate + motion.test.ts :307/:457/:474/:466/:501-503 + reveal.test.tsx:325 + summary.test.tsx:954,980 + R5.2 sites 4/9 + R5.6 prose) — fix: name replacement predicate explicitly (modifier.includes('--') || modifier === 'ph-entered' as explicit vocabulary), state deletion IS the forbidden relaxation, require the reviewer's verified 6-line guard test (red on relaxation) same-commit, real inventory; w0-r-002 collapsing 3 flags un-scopes Reveal.css:217/221's descendant gate (.ph-fade--in .ph-stamp--animate → .ph-entered would match stamps in ANY entered element; T35's fix class) — fix: shared flag never bare as descendant gate, selector becomes .ph-fade.ph-entered. 4 MED: w0-r-003 two-step rule must cover ALL cells the change falsifies (only File+Motion are compiled — Trigger/Property/Duration/Easing read by nothing); w0-r-004 .ph-close prose omits border-block-end: 2px solid transparent (the reserved-underline idiom); w0-r-005 dagger must name W10/gr6-054 + exact it titles as tripwire (today: unenforced-A-with-footnote is worse than honest C if skimmed); w0-r-006 tier-A scope paragraphs omit the 7 promoted rules. 6 LOW: -6px sign (w0-r-007), "byte-identical" wording (.ph-lab has extra flex decls, w0-r-008), §0 present-tense (w0-r-009), prelude-drop prose vs single-allow-list mechanism (w0-r-010), non-collision claim conditioned on sum (w0-r-011), R1.3's tier-A† thinner note — file-granular interim scan misses same-file selector adds, verified (w0-r-012). ADJUDICATIONS: flag-2 UPHELD keep-.ph-entered-move-predicate (rename saves 1 of 9 edits, inherits w0-r-002 identically, grammatical lie, option A ends strictly stronger w/ mutation-tested vocabulary — all measured); flag-3 page-shell scoping COHERENT (multiplicity 9→4, residues are twin-less singletons; .ph-call was a category error in the original finding — reclassify as modal chrome in fix round). FOLLOW-UP RULING BOOKED for W7: .ph-reveal bottom-pad (24-joins--titled vs symmetric-40) decided with a 360 shot. Fix round 1 dispatched to original implementer (all 12 per A4).
W9 IMPLEMENTER DONE (8 commits on task-w9 off 7e417f4; gate green tsc/lint/1422 all exit 0; NO src/** or store.test.ts touched — gr6-053 needed no store seam). Suite 44.9s→13.5s (dgp 36.7s→1.2s); honest count 1494-reported/1406-unique → 1422/1422/0-dupes (−88 dupes +16 new, reconciled per file). EVERY rewrite mutation-verified patched→run→reverted; the discriminating runs: A1/A3 analyze (OLD GREEN-blind, NEW RED naming samples — the invalid arm was UNREACHABLE at 200+, min final cell 57, so WINDOWS gains asWindow(40) → 39 valid/11 invalid); S2 prereg spoiler leak (OLD 15/15 GREEN, NEW RED with legible leak) — now drives two REAL createGameStore()+preregCommit() lifecycles vs fake client; EPOCH sweeps (OLD epochGuard GREEN while goldens 4-of-5 RED, NEW RED). 5 DELIBERATE DEVIATIONS, all measured: (1) spec'd goldens guard NECESSARY-NOT-SUFFICIENT — fixtures break from EPOCH=2026-08-24, 8 days BEFORE earliest golden date, while date>EPOCH stays green (recursion walks all post-EPOCH dates); added a sufficient check (committed scenarioIndexFor20 vs live call); also gr1c-005's table was the per-date case, corrected numbers in comments; (2) gr1c-022's proposed StrictMode fix does NOT discriminate (React 19 double-invokes on mount only; boot effect gated behind !content early-return → deleting didBootRef leaves StrictMode test green) — kept it + added the locale-switch test which IS discriminating (R1: didBootRef deleted → locale-switch RED); (3) invalid-arm ci===[0,0] claim wrong as written (sub-MIN_CELL emits real stats; only OLS-couldn't-run gets placeholder) — arm split in two; (4) scope+1: memoized aggregate-moments sweep whose "computed once" comment was false (ran 6×); (5) share.test.ts 790→1159ms — real lifecycles cost more than tautologies, paid deliberately. HAND-ONS: App.tsx didBootRef comment "locale never switches after first load" is STALE (LocaleToggle ships) → W7; per-wave scratchpad subdirs needed (a w1 helper-script collision at a shared path, verified harmless, re-ran control from private path) → controller adopts for all future dispatches. Review dispatched (SONNET, mechanical + deviation verification).
W0 FIX ROUND 1 DONE (a49756c on task-w0, DESIGN.md only +140/-46; gate green 1494/tsc/lint/build all exit 0; 51-count re-verified; six §10 scans zero). All 12 resolved, none disputed; both HIGHs REPRODUCED before writing (predicate deletion → 32/32 green confirmed; guard test verified both directions 33/33-green vs exit-1-on-relaxation; Reveal.css gates + motion.test.ts:307 regex confirmed). Predicate now literal in the law; deletion named as the forbidden relaxation; guard quoted as required-by-law; inventory upgraded to TEN places stated as a floor (implementer itemised one more than the review's ≥9: R5.6's own prose separate from R5.2 cells + the explanatory comments in 4 stylesheets + a11y.test.tsx). Sign pinned -6px w/ reasoning; .ph-call reclassified modal chrome; .ph-reveal bottom-pad booked for W7-with-shot. Scoped re-review dispatched (SONNET).
W1 IMPLEMENTER DONE (4 commits on task-w1; gate green: 1555/53 [+61], tsc/lint/build 0, cal 5/5 byte-idempotent, goldens 6/6 untouched, e2e 15/15; 11/11 mutations killed w/ anchor+file-hash isolation — 2 initially mis-scored by the harness, re-run one-at-a-time honestly). gr6-001: sigPaths partitioned by true-outcome in the existing loop, sigOther by subtraction (can't drift); null-day accounting1 now names chance AND confounding (terminology locked to About's disclosure ×3 locales); accounting1Effect splits {trueSig}/{otherSig} + "Nothing in a p-value distinguishes the two". gr6-002: pHitNull+pHitEffect vectors, dayType REQUIRED param (default would preserve the bug); measured k=5: 0.226 null vs 0.614 effect (2.7× understated on effect days); directed-search sentence gated off prereg and k=1. gr6-003: 5-block prereg reveal (accounting2Prereg commit-framing, preregisteredRecipe, subline suppressed, preregFalsePositive hoisted, call section unrendered). STAMP ROOT CAUSE deeper than both lanes: rotated subline <text> bbox drove the objectBoundingBox filter region — subline now horizontal prose beneath the card, filter region user-space = viewBox, painted ink ⊆ box at ANY label length/locale; stamp to lower-right 50%; measured clip 26px/72px → 0/0 at 360/768 (GR4's 22px was a shorter live subline — overflow scaled with string, which is why bbox had to go). 7 DEVIATIONS flagged: (1) protocol.ts one call site touched (dayType required forced it — W5 SEAM, also gr6-102 will touch protocol/types); (2) counts on RevealMetricsFull not §6's RevealMetrics (types.ts is W5's; recommended 2-line hoist follow-up); (3) NO checksum bump is CORRECT (fnv1a32 over DGP constants, none moved — 3889152766; shape now guarded per-vector by assertPHitTable); (4) Stamp subline prop REMOVED not orphaned; (5) scroll-snap scoped :root:has(.ph-reveal) because App.css is W7's, reduced-motion-safe; (6) new strings use p < 0.05 leading-zero (gr6-027 landing early on keys being rewritten anyway; variants never co-render); (7) stamp ~6% smaller + 60→50% trim (bleed must live inside the box; 43% of viewport at 360). Review dispatched (OPUS — scientific copy + transcreation + geometry judgment).
W5 IMPLEMENTER DONE (8 commits on task-w5; gate green: 1508/53 [+14], tsc/eslint-with-new-rules/build 0, GOLDENS BYTE-EXACT (gen_goldens re-run, 0 dirty; p_hit unchanged), specGrid parity 7/7, day acceptance 30/30, cal 5/5). BIT-IDENTITY PROOFS: gr6-045 43,547 Object.is comparisons (6,221 dates × 7 counts, old-copy-vs-new, zero mismatches, copy proven faithful against live export BEFORE refactor, old path deleted only after) + 14-day no-repeat over 6,200 days; gr6-100 5,772 comparisons + protocol.test.ts trueBeta toBe still green. PERF: scenarioIndexFor EPOCH+10,000d RangeError → 102.8ms (+50,000d 309.9ms no-throw); init/precheck deltas inside noise floor (load avg 24.5-29 from parallel waves — QUIET-MACHINE RE-MEASURE wanted at final verify). gr6-105 VERDICT: EXPECTED (52/60 days non-monotone, 0 monotone-decreasing, determinism control clean; visible-window refit explanation documented on sigCount + protocol.ts pointer). 7 DEVIATIONS: (1) gr6-046's "51%" DOES NOT REPRODUCE — 4 measurement designs put buildCut at ~5% of the pass; refactor kept (removes provably-unread work) but old number retired; (2) gr6-097 RELAX-not-swap (coefficient swap would move every golden to buy precision nobody consumes — measured p=.05 error ≤6.8e-16; bound 1e-8 + canary at shipped-grid worst case 1.67e-12≤1e-11 + boundary test ≤1e-15; GOLDENS UNMOVED); (3) out-of-lane one-liners: capExhausted:false in 8 test factories incl. tests/game/store.test.ts — W6 MERGE SEAM; src/engine/reveal.ts needed NO change (payload assembled in protocol.ts); (4) gr6-047 premise PARTLY WRONG: daily.ts has NO copy of the algorithm (new Date + UTC daysBetween — different mechanism, which is why purity ruling applies); pointer written on day.ts side only; (5) gr6-094 throw verified manually but UNTESTED (test belongs in dgp.test.ts = W9's) — controller: fold one-liner into W9 fix round or merge commit; (6) plan doc :15-16 edited (W9 edits :308, no collision); (7) gr6-117 lint half found+fixed a real preserve-caught-error defect in the .mjs. Lint rules PROBED (18-line violation file → 20 errors; allowlist accepts data/*.json+tuning, rejects data/helper+store). gr6-093 discriminating test mutation-verified. Review dispatched (SONNET).
W0 SCOPED RE-REVIEW: APPROVED FOR MERGE (all 13 pass, every cited location byte-checked, gate re-run green, six §10 scans zero, 51-id table verified, R5.6 judgment check found no exploitable literal reading, no new findings). W0 MERGED → build/v1 @ bb0faa3 (after chore commit 7e4fea0 capturing the dirty plan-doc appends), full gate green on merged tree (tsc/lint/vitest 1494/build all 0), PUSHED. Worktree task-w0 removed, branch deleted. W7 and W2 remain gated on W1's merge (Reveal.css + copy-file adjacency).
W10 IMPLEMENTER DONE (6 commits 7e417f4..f69cb86 on task-w10; gate green: tsc-covering-scripts/lint 0, 1501+1todo (+6 tier-C +1 analytics-companion +1 todo), build 0, bundle 111.6KB vs 200KB budget w/ 3 guards each proven red, e2e 25/25 incl. 3-ENGINE DETERMINISM GREEN (V8/SpiderMonkey/JSC agree on scenario/p/n/1792-enum/sig-count, +8s suite) + axe both themes, cal verify-mode 0 + clean tree, actionlint 0). Highlights: gr6-049/084 workbox default-glob claim in the review was WRONG ({js,wasm,css,html} verified in schema; png icons came from includeManifestIcons — glob override is default+woff2 only to avoid double-precache); manifest 11→14 entries, 6 woff2 in, 3 locale chunks OUT to StaleWhileRevalidate route, .woff 161KB→0 stripped at enforce:pre (post-build prune would 404 every @font-face); gr6-115 axe FLAKE root-caused before trusting green (ancestor-opacity compositing during .ph-screen fade → settled-screen scan via getAnimations drain, 4/4 green; scan proven capable via injected violations) — gr6-121 GREEN, nothing for W7; gr6-055 path-gate is hand-rolled fail-safe bash (5 cases exercised, unresolvable-base RUNS); gr6-118 SHA-pinning decision recorded (floating @v4 first-party-only, revisit on first secret/second maintainer); gr6-119 verify-default + --write, edit confined to writeFileSync site (W1 SEAM); gr6-120 .gitignore probed 5 paths (sdd/**/*.md trackable, worktrees/shots still ignored). DEVIATIONS: (1) engine-strict BLOCKS npm ci on THIS machine (local /usr/bin node 22.22.1 < 22.22.2 required by jsdom) — intended behavior of gr6-006, nvm use resolves; agents doing clean installs post-merge need nvm or --engine-strict=false, documented; (2) npm ci --dry-run DELETES node_modules in npm 9 (wiped mid-gate, typecheck fell through to parent checkout's modules → 3 bogus TS7006s; reinstalled, reported gate is post-reinstall); (3) gr6-058 tripwire born it.todo + live companion test (no-package-nothing-calls guard); T25 checklist line drafted verbatim for controller to paste; (4) sdd *.md files still need git add from main checkout (worktree lacks them); (5) webkit needed 2 OS libs — extracted .debs + LD_LIBRARY_PATH locally, CI covered by --with-deps, nothing skipped; (6) og.png 147KB = largest dist file, off-budget-path, observation only; (7) e2e/harness.ts +1 additive stats option. MERGE NOTE: §10 shell-block deletion + un-daggering is owed when W10 merges onto post-W0 tree (W10 branched pre-W0; the dagger says the block dies in the commit that adds the tests). Review dispatched (SONNET).
W6 IMPLEMENTER DONE (task-w6; gate green: tsc/lint 0, 1586/57 files, build 0, e2e 15/15, 300-draw spoiler property UNCHANGED — diff on share.test.ts empty). gr6-008 BLOCKER closed: guard = "any mode still playable" (completion-state, NOT achievements — W12's earlier-unlock edits ONE conjunct and cannot reopen a finished day); finished state = alreadyPlayedToday + own shareString + countdown, no CTA; 9-test suite, mutations 5-red/3-red. gr6-042/043/044/079: makeCall sibling-shape + pending conjunct + abandon pending:false; ONE withEngineErrors across all 5 awaiting sites + Prereg local freeze releases on failed commit (else the dead-end moves up a layer); isValidV1 REPAIRS field-by-field over freshState().stats (discarding history to fix a counter is worse than the corruption; corrupt blob previously THREW, not just NaN'd — 8 tests); CALL append atomic. gr6-019: earnedToday filtered against pre-save achievements — replay 4 models × 32 days: ceremony firings 38/51/69 → 6/7/8, repeats 16-19× → NONE, firings ≡ distinct storage entries. gr6-018: parsimony ROW unconditional/value computed; career as its own line (never a summand), null on prereg. gr6-064/091: outlet dedup INSIDE pickPress via slot pedigree (pure function of same inputs — live at existing call sites, W7's pressForDay adoption is cosmetic); 180k-cell sim: same-outlet 42.52% → 0.00%, cross-scenario same-day dupes 30.88% → 5.30%; GR2's exact observed repeats gone; HONEST NOTE: within-week repeat 28% is a pool-size ceiling only the 60-cell matrix (W4) moves. gr6-092: shared walkForkGlyphs, both consumers tested. gr6-081/082: dayComplete relocation (Summary 545→315), UseGameStore/TFunction hoisted, seams kept. gr6-107/108/109: STORE_ERR 12 identities/14 sites, forks invariant through all branches, singleton test (own jsdom file — store.test.ts is node by design). DEVIATIONS: Published.tsx untouched per instruction; ForkTrail edit as briefed; onViewStats dark until W7 wires the chain; 4 new test files vs W7-owned suites; scoring.ts row-only touch (§1(f) scoring decision untouched); 4th honest player model added (GR2's three all publish); no new copy keys — 3 TODO-W2 comments; replay harness deleted after run. Review dispatched (OPUS — game-design + store judgment).
W9 REVIEW: APPROVED FOR MERGE (sonnet; "unusually tight wave"). Diff discipline PASS incl. no-store-seam claim verified (all needed exports pre-existed at 7e417f4); gate re-run 1422/1422 @ 13.30s, 0 dup names mechanically confirmed, +16 new tests hand-counted to exact reconciliation; coverage-not-weakened verified (same seeds/rows/properties, validators byte-identical + one export keyword); ALL 5 mutation re-runs reproduced exactly (A3 blind-old/red-new naming sample 0; S2 legible leak; EPOCH boundary 4/5 break at 2026-08-24 w/ old-guard-green/new-red; R1 locale-switch discrimination; touch-test re-arm latch) + share.test.ts solo 5× green (fixed-seed LCG, not Math.random); all 5 deviations adjudicated sound; no overclaiming names; plan:308 numbers match independent probe. 1 informational: the BEFORE wall-time baseline (44.9s) is machine/load-sensitive (review machine got 24-29s at load 22-26; gr1c-013 cited 36s) — AFTER number is what CI compares against and holds; mechanism confirmed ~50×. No fix needed. W9 MERGED → build/v1 @ c87a61f, full gate green on merged tree (tsc/lint/vitest 1422/build all 0, 11.1s suite), PUSHED, worktree cleaned. NOTE: gr6-094's throw-test one-liner still owed in dgp.test.ts when W5 merges (controller follow-up commit).
W6 REVIEW: NEEDS FIXES (opus; blocker gr6-008 verified genuinely fixed ON THE PRODUCTION BUILD — reload after honest day-one leaves literally zero controls; ceremony re-replayed independently 8d×2 models, once-per-trophy + firings≡stored + raw evaluator fires 24× as control; press slot-pedigree traced by hand + 600-cell element-wise equality vs the REAL call-site spellings + 10.2k-cell sim reproducing 0 same-outlet and 5.32% vs claimed 5.30%; withEngineErrors all-5-sites traced, no swallow path; isValidV1 repair strictly narrowing, old body character-identical to new container check). 3 MEDIUM: w6-r-001 the guard LOCKS OUT PRACTICE MODE (Briefing never reads s.practice; confirmed on prod build: real day finished → ?practice=1 → finished block, zero CTAs; practice is documented for testers/streamers and unscored by construction) — fix: !practice conjunct; w6-r-002 Prereg freeze-release is ONE-SHOT (nothing ever clears store.error except boot; after any earlier failure frozen is permanently false — measured: second in-flight commit shows unlocked form; third click = unhandled rejection, the exact class gr6-043 exists to kill) — fix: error:null at the four dispatch sites; w6-r-003 retired scan's guarantee NOT replaced (replacement asserts pressForDay ≡ literal calls IN THE TEST FILE; Published.tsx still calls pickPress directly — a one-char call-site edit takes the slot===-1 branch, silently restores the collision, suite green) — fix: narrow guard until W7 adopts, AND W7's pressForDay adoption PROMOTED from cosmetic to REQUIRED. 5 LOW: tie-break mis-selects on prereg→hack order (drove it: printed PREREG-SHARE after hack); W12-composition comment overstates ("cannot reopen" false — widened conjunct un-finishes an honest day-one into prereg chooser; CONTROLLER NOTE for W12: same-day prereg after completion is consistent with T18's existing chooser design, make it the DELIBERATE decision); midnight-straddle countdown (narrow window, pre-existing class); STORE_ERR 12/14 (2 unreachable notBooted sites); one test case's stated W12 claim ≠ what it proves. 2 INFO: buildTrail glyph order equivalent-not-identical (share.test.ts untouched+green); repair could rebuild counters from history (strictly-better-later). Deviations: ALL 8 ACCEPTED (incl. layering soundness of in-pickPress dedup; onViewStats dark = correct unsupplied-route shape; no raw-key fallbacks confirmed live). Seam table delivered; store.test.ts vs W5's one-liner = highest collision risk. Fix round 1 dispatched to original implementer (3 MED + 5 LOW + info-010 fix-or-reasoned-wontfix per A4).
W5 MERGE FINALIZED (2026-08-06 ~13:40): follow-up 2490b07 (3 buildCut comment rewords w/ machine-variance caveat + gr6-094 throw test) had been committed BEFORE running the test (process error, noted); test failed on EffectSpec literal missing required `hetero` — b254430 fixes it (hetero: null), dgp.test.ts 31/31. Full gate then surfaced a W5×W9 merge seam: share.test.ts prereg-day RevealPayload fake predates W5's branch point and lacked now-required capExhausted (gr6-102) → tsc error. 565a2b6 adds capExhausted:false (fake models a normal non-fallback day). GATE GREEN: tsc=0, eslint=0, vitest 1437/1437 (53 files), build=0 (PWA precache 11 entries). PUSHED c87a61f..565a2b6. task-w5 worktree removed, branch deleted. W5 fully closed.
W1 PRE-REVIEW EVIDENCE (stamp sub-probe COMPLETED before session limit; BLOCKING for W1 fix round): the user-space filter region (Stamp.tsx filter region = viewBox, from 90c8093) hard-clips the label's own glyphs. NULL_REPORTED labels exceed the 344-unit budget in ALL THREE locales: IT "RISULTATO NULLO" renders "ULTATO NUL" (16.4px cut @360, 27.3px @768); EN renders "ULL REPORTE"; ES cut 13.7px @360 / 22.7px @768. Regression is NEW to the branch — rolling filterUnits back restores the full word. Safe budget ≈12 chars at --text-40; every NULL_REPORTED string exceeds it. shell.test.tsx:312 pins region===viewBox WITHOUT pinning label-fits. Also: the 50%-trim comment's claim fails at 768 (RETRACTED still crosses the question — card only 144.56px tall). Guard design constraint: getBoundingClientRect/scrollWidth are BLIND to filter-ink overflow (controls painted 261px off-edge with clean rect, no scrollbar) — any future guard must not use them.
SESSION-LIMIT CASUALTIES (reset 12:10pm Europe/Madrid, now past): W1 opus reviewer died mid-review (scientific-audit + transcreation portions NOT done; sub-probe done, above). W6 fixer died mid store fixes (w6-r-002 error-clearing, w6-r-007 coverage) — task-w6 has UNCOMMITTED src/game/store.ts + tests/game/store.test.ts changes from the interrupted fix. W10 sonnet reviewer died near-done (last: checking for selector-granular R1.3 coverage elsewhere). Resuming all three now.
RESUMED 2026-08-06 ~13:45 (post-reset): all three limit-killed agents resumed from transcript via SendMessage by agent ID — W6 fixer (a6a9886a8e16228de; told: review own uncommitted store.ts/store.test.ts diff first, finish w6-r-001/002/003-interim/007, tests first), W1 reviewer (abaa6ddab8c63966a; given the sub-probe's blocking stamp-clip summary; owes scientific-audit + transcreation portions, then full verdict), W10 reviewer (a62e8ea50188214de; finish R1.3 selector-granularity fairness check, finalize verdict). All running in background.
AGENT ID ROSTER (recovered from transcript, for resume): W0 impl a77a44ab29ce7207d / rev a94a59137a727994d; W1 impl af6476878ca0a52f6 / rev abaa6ddab8c63966a; W5 impl a7d769793f15c4442 / rev ab46ed6ae8eb5cd8e; W6 impl+fixer a6a9886a8e16228de / rev a9b50d35d8540e5e6; W9 impl a6cf5231d840f792e / rev adb81195a870420d9; W10 impl ade53e62bc60f2e28 / rev a62e8ea50188214de.
W10 REVIEW: NEEDS FIXES (reviewer completed post-resume, ~13:50). Gate re-runs ALL GREEN in worktree (1501+1todo, 3-engine e2e 25/25, axe 2x zero-flake, actionlint clean, engine-strict legible EBADENGINE). Workbox adjudication: implementer RIGHT, original GR claim WRONG (GenerateSWOptions.json default globPatterns = **/*.{js,wasm,css,html}). Bundle guards ×3 + path-gate bash all mutation-verified red. Axe settle() fix SOUND (event-wait not clock; tokens.test pins resting contrast). Findings: w10-r-001 HIGH/GATING — R1.3 it is FILE-granular, DESIGN.md:1021-1029 requires SELECTOR-granular; probe: 5th unrelated var(--sig-red) selector in allow-listed file passes silently → per DESIGN fallback R1.3 stays tier D until fixed. w10-r-002 MED — none of the six it titles match DESIGN.md:889-905's prescribed grep-tripwire strings verbatim. w10-r-003 LOW — R2.2/R3.1 excludes ALL @media preludes so rogue breakpoints don't double-fire (R3.4 alone catches; defense-in-depth gap). MERGE-TIME PLAN (f), controller-owed at W10 merge on post-W0 tree: delete 5 of 6 shell scan lines + clear their daggers (R4.5/R4.7/R6.5/R3.4/R2.2/R3.1 promote to Tier A, mutation-verified); KEEP R1.3 scan line + dagger + tier-D fallback text UNLESS fix round lands selector-granular it; reconcile tripwire titles. FIX ROUND 1 dispatched to ORIGINAL implementer ade53e62bc60f2e28.
W1 REVIEW: NEEDS FIXES (reviewer completed post-resume, ~13:55). Full review persisted to .superpowers/sdd/2026-08-04-grand-review/review-w1.md (authoritative for the fix round). Gates all green in worktree (1555/1555, cal 5/5 byte-idempotent, goldens 6/6, e2e 15/15 on re-run after port-4317 collision with a concurrent probe). BLOCKERS: w1-r-001 scientific — null-day accounting1 ×3 locales inverts the measured decomposition (permutation probe over 188 accepted null days: confounding = 5.1% of hits, indistinguishable from zero; rejection sampler removes the confounded tail; reviewer supplied EN fix shape; NOT a ruling-(a) violation — ruling permitted naming the confound, not majority-claiming it); w1-r-011 stamp label clip ×3 locales (sub-probe finding confirmed independently by arithmetic; recommended fix textLength+lengthAdjust; new label-fit test must cover 9 verdict×locale strings; rect/scrollWidth blind to filter ink). SHOULD-FIX: w1-r-002 (wire-boundary pinning — 4-line protocol.test.ts assertion, do NOT touch types.ts; mutation stripping split counts SURVIVED 1555 green → NaN copy), w1-r-007 (scroll-snap = unsanctioned 10th motion site, R5.2 exhaustive+compiled; recommend DELETE snap keep padding — also removes the :root:has seam W7 would trip on), w1-r-003 ("single p-value" fix), w1-r-004 (conditional "if you followed the p-value"), w1-r-006 (doc comment 0.514→0.6136/2.72; backlog gr6-002's 51% superseded), w1-r-008 (land legend.significant → 'p < 0.05' ×3 OR revert accounting1; not as-is), w1-r-012 (768 trim criterion unmet — one piece with r-011/r-010), IT grammar HIGH + IT gender MED + ES accounting1 MED (reviewer supplied IT/ES texts). LOW: r-005, r-009, r-010. Deviations 1/3/4 accepted; 2 accepted w/ protocol test; 6 rejected. CONTROLLER NOTES: W11 must run cal AFTER W1 lands (else regenerates single-vector shape → assertPHitTable throws); W2's gr6-004 must be consistent with new accounting1; W7/7a flags (Stamp.css ph-stamp-slam keyframe, tests/ui overlap); EN accounting2Prereg alignment suggestion → W2. FIX ROUND 1 dispatched to ORIGINAL implementer af6476878ca0a52f6.
W6 FIX ROUND 1: DONE (468243f on task-w6, worktree clean; App.tsx + Published.tsx untouched — fold-in confirmed via empty diffs). w6-r-001 !practice conjunct + 3 tests (mutation 2 red); w6-r-002 error:null at all 4 dispatch sites, boot deliberately excluded (would stomp mid-init onCrash), new preregRetry.test.tsx drives reject-once-then-hang through real store+component, fixer CAUGHT AND FIXED its own vacuous unhandledrejection test (jsdom doesn't surface void-discarded rejections; now counts real dispatches at the seam), mutation 7 red; w6-r-003 two [INTERIM] tests in published.test.ts pinning the 3 seeds + byte-identity to pressForDay, retirement condition documented (W7's adoption commit DELETES them — mechanical trigger), severity confirmed by measurement (seed '#2'→'#two' moves same-outlet pairs 0.00%→55.05% over 180k cells), mutation 1 red; w6-r-004 hack-first tie-break documented arbitrary (mutation 1 red); w6-r-005 comment truth-restored w/ W12 pointer; w6-r-006 countdown suppressed when iso≠localIsoDate(), honestly scoped (app-shell-wide staleness = W7 re-read + W2 copy key; Summary has identical bug — noted), mutation 1 red; w6-r-007 14/14 throw sites + 12/12 STORE_ERR asserted; w6-r-008 renamed + companion; w6-r-010 TAKEN — per-field fallback to rebuildStatsFromHistory mirroring saveDay arithmetic, mirror kept honest by real-saveDay toEqual test, streak rebuilt as run-ending-on-last-played-day (no wall clock; honest caveat), mutations 5+1 red. GATE: tsc 0, eslint 0, vitest 1610 (58 files), build 0, e2e 15/15 (port-4317 leftover killed, re-run clean), spoiler property untouched since 7e417f4 and green. 32-day replay instrument re-run: identical numbers, scratch deleted. SCOPED RE-REVIEW dispatched to ORIGINAL W6 reviewer a9b50d35d8540e5e6.
W10 FIX ROUND 1: DONE (2927f40 on task-w10, one commit atop f69cb86, ONLY tests/ui/tokens.test.ts touched, worktree clean). w10-r-001: reproduced the gap first (5th-selector probe green pre-fix), then replaced line-scan with brace-tracking parseDeclarations walk attributing declarations to their actual rule, comma-list splitting, at-rule prelude skip; allow-list = DESIGN's ELEVEN selectors across four places; --sig-band matched by declared property in tokens.css and asserted the ONLY derivation; non-CSS files same parser (style-object var(--sig-red) has no selector → always red). Mutations: reviewer's exact probe RED, new-file RED, renamed-sanctioned-selector RED, second-derivation --sig-halo RED, in-test parser probe pins attribution+line numbers. w10-r-002: renamed tests to DESIGN's six verbatim strings (byte-for-byte grep -F vs build/v1's DESIGN.md via git show, em-dashes included; did NOT edit worktree's pre-W0 DESIGN.md copy) — all six FOUND. w10-r-003: took the narrowing + one step further — allow-list is the FOUR declaration shapes DESIGN names (not bare numbers), so it and the shell block accept exactly the same set; 1024px rogue fires on BOTH R3.4 and R2.2/R3.1 (2 failed/4 passed); padding:2px probe shows old value-set predicate would have passed it. GATE: typecheck 0, lint 0, test 1501+1todo, build 0 (14 precache), bundle 111.6/200KB, e2e 25/25 (axe zero serious/critical both themes), cal 5/5 + clean status, actionlint 0. Open items stand: engine-strict node flag, it.todo analytics tripwire → T25, gr6-120 git-add note. SCOPED RE-REVIEW dispatched to ORIGINAL W10 reviewer a62e8ea50188214de.
W10 SCOPED RE-REVIEW: APPROVED FOR MERGE (all three findings independently reproduced as fixed; reviewer re-ran its exact original probe RED + a novel JSX-inline-style probe RED; six tripwire titles byte-exact vs build/v1 DESIGN.md; 1024px double-fire confirmed on both its; gate re-run green; e2e/bundle/cal/actionlint not re-run — diff test-only, flagged explicitly). Item (f) superseded: ALL SIX daggers clear uniformly, R1.3 included, no follow-up row needed.
W10 MERGED -> build/v1 @ 0ff4209, PUSHED. Merge executed --no-ff --no-commit so the owed DESIGN.md edits landed IN the merge commit per §10's "same commit" clause: sh scan block deleted, all seven † cleared (table row + promotion block rewritten past-tense, six it titles kept as the grep index), tier-D fallback paragraph removed, dangling references re-tensed ("The retired scans…", "All six print nothing", point-1/2 tense fixes, closing "six scans of this section" reword). One auto-merge (scripts/simulate_calibration.ts) clean. npm install needed for @axe-core/playwright: run with npm_config_engine_strict=false on dev machine (node 22.22.1 one patch below jsdom@30's floor per .npmrc's own MEASURED CONSEQUENCE note; README nvm-use is the sanctioned path) — lockfile gained the root engines sync W10's committed copy missed (single hunk, staged into merge). GATE ON MERGED TREE: tsc 0, eslint 0, vitest 1444+1todo (53 files), build 0, bundle 112.1/200KB, cal verified + clean tree, e2e chromium 21/21. task-w10 worktree removed, branch deleted. W10 CLOSED. Note: §10 shell-block deletion obligation DISCHARGED. Remaining batch-1: W6 (fix round 2 done @ 0cb87be — 1-place !practice fix at definitions, dayFinished conjunct removed as redundant, 3 tests, mutations 3/3/1 red, gate 1612; re-verify dispatched to reviewer), W1 (fix round in flight).
W6 RE-REVIEW ROUND 2: APPROVED (residual probe re-run verbatim → fixed; all 3 mutation claims match exactly; deliberate behavior change judged RIGHT — fixes the mirror-image bug the reviewer would have filed next, practice player forced into hack mode with no prereg practice route; degrades correctly via preregAvailable achievement wall). Reviewer's ⚠️ environment blocker (undeclared @tailwindcss/vite import broken by controller's 14:04 npm install) adjudicated MOOT on merged tree: W10's merge already landed gr6-090 removing the import+plugin (vite.config.ts:57 note), W6 touches neither vite.config.ts nor package.json — reviewer was seeing task-w6's pre-W10 base. Historical note: @tailwindcss/vite was NEVER declared in package.json (undeclared artifact); gr6-090 resolved this permanently.
W6 MERGED -> build/v1 @ b183035, PUSHED. Predicted W5×W6 seam materialized exactly as booked: W6's new preregRetry.test.tsx payload fake lacked capExhausted → 9c1935d adds it (same shape as 565a2b6). GATE: tsc 0 (post-seam-fix), eslint 0, vitest 1562+1todo (58 files), build 0, preregRetry 3/3. task-w6 worktree removed, branch deleted. W6 CLOSED — all ten review findings resolved and mutation-verified across two fix rounds. BATCH 1 REMAINING: W1 only (fix round in flight with implementer af6476878ca0a52f6; then scoped re-review; then merge — watch protocol.ts seam vs W5's merged hunks and simulate_calibration.ts vs W10's write-flag).
SDD RECORD COMMITTED (gr6-120 discharge): d876d2e pushes all 76 .superpowers/sdd/**/*.md files (T1-T40 briefs+reports, this ledger, grand-review findings/backlog/reviews). Blocker found+fixed: a tooling-created nested .superpowers/sdd/.gitignore ('*') was overriding the merged root policy from inside the subtree — removed (was untracked/local; note: if tooling recreates it, already-tracked files stay tracked, but NEW record files would silently stop being trackable — check at final wave). Pre-publish name-scan run: real-name matches are the game's own shipped parody journals + About prior-art credits (already public in src/content) plus false positives ("passwordless" contains "wordle"; 538 = decimal fractions; dog-economist = profession slug). build/v1 @ d876d2e.
W1 FIX ROUND 1: DONE (5 commits on task-w1, tree clean). w1-r-001: fixer REPRODUCED the reviewer's permutation finding, rewrote accounting1 ×3 with three measured-true claims (count belongs to threshold, no effect exists, design still unclean); DELIBERATE deviation from reviewer's proposed shape — their draft repeated {total} (t() replaces only first occurrence, second would render raw, pinned by never-repeats-token test) and carried an em dash (IT/ES budget = 0); "about one in twenty" true across whole acceptance band where "about this many" is 3× off at band floor. IT/ES fold in all transcreation fixes (sesgo de confusión, no bare predicates, azar collision gone). w1-r-011: independent getBBox measurement (NULL REPORTED 370.4 / RESULTADO NULO 406.9 / RISULTATO NULLO 419.8 vs 344 region), fixed textLength={268}+spacingAndGlyphs (268 = RETRACTED's own advance; signature label moves 0.3%); all NINE rendered labels eyeballed; guards = structural shell.test + NEW e2e/stamp.spec.ts measuring 9 real strings via getBBox in real browser (in-file doc: rect returns filter region on filtered content, scrollWidth absent on SVG); w1-r-010 overflow:hidden second containment; w1-r-012 768 claim replaced with measurement (cannot clear without breakpoint: 145px card/64px title/25px interior; mark-drop collides subline at 553). Should-fix ALL done: r-002 protocol.test pins both counts both day types summing to sigPaths (previously-surviving mutation now KILLED), types.ts untouched; r-007 snap deleted padding kept; r-003/004/006/008/009/005 + IT gender/order + EN accounting2Prereg alignment. GATE: typecheck 0, eslint 0, vitest 1557/1557 (53 files), build 0, cal 5/5 checksum 3889152766 byte-idempotent, goldens 6/6 no regen, e2e 16/16 (+stamp.spec). Mutations 6/6 killed incl. compiling TEXT_W-drift. INCIDENT (self-flagged): mid-round `git checkout -- src` in mutation harness destroyed uncommitted src edits; reapplied all, re-ran evidence from scratch on committed tree; harness now refuses dirty worktree. Shots in scratchpad w1-stamp-shots/ + w1-labels/. SCOPED RE-REVIEW dispatched to ORIGINAL W1 reviewer abaa6ddab8c63966a.
W1 SCOPED RE-REVIEW: APPROVED FOR MERGE. Both blockers resolved+mutation-verified; reviewer ADJUDICATED the copy deviation (conclusion accepted, mechanism rejected: "t() replaces only first occurrence" is FALSE — t.ts:19 uses /\{(\w+)\}/g, both would substitute; but the no-repeat shape tests forbid repetition anyway; false claim PRE-EXISTING in seven places → w1b-007); reviewer CORRECTED ITS OWN SUB-PROBE (TEXT_W=340 / shrunk-region "gaps" are killed by shell.test.tsx at the gate — two-layer guard division of labour correct) and ITSELF (SHARED_WITH_EN roster is allow-list not requirement; w1-r-008 correct by authorship only → w1b-004). Checkout incident cleared by 3 independent checks. NEW FINDINGS BOOKED: →W2: w1b-001 MED (ES regla-6 comment certifies unimplemented lock; fix "así que está confundido con la edad y la renta" — also fixes sesgo-de-confusión preposition), w1b-002 MED (IT "confondimento con" calque; fix "quindi c'è confondimento da età e reddito"), w1b-003 MED (root cause: EN lock tested, IT/ES only commented — extend shape.test.ts:1017 to IT/ES catalogs), w1b-004 MED (notation-identity toBe(enCopy[k]) for legend.significant it+es), w1b-007 LOW (fix false t() first-occurrence claim in 7 places incl. it/copy.ts:19 es/copy.ts:16 headers), w1b-008 LOW (r-003/004 wording fixes lack regression guard), w1b-009 LOW (IT da-sola attachment + double-ne; IT "un minimo"→"una stima per difetto"; ES por-sí-solo attachment). →W7: w1b-005 MED (768 residual overlap 39.46px of label across question line 2, measured+documented but booked nowhere; frame top border crosses title first line; W7 owns the breakpoint decision). w1b-006 LOW noted (5/9 labels >4% glyph distortion via spacingAndGlyphs; judged absorbable by stamp idiom — craft note).
W1 MERGED -> build/v1 @ 5acfea0, PUSHED. Seams resolved: (1) simulate_calibration.ts UU conflict — kept W10's gr6-119 verify/--write mechanism, payload = W1's two-vector shape, report header BY DAY TYPE + tableStatus line; (2) R1.3 guard fired legitimately on gr6-059's subline move (sanctioned selector no longer used) — removed `.ph-stamp__mark--red .ph-stamp__subline` from tokens.test.ts PLACES + DESIGN.md place-1 wording → "two descendants (border and label; subline is horizontal HTML in --ink since gr6-059)". NO capExhausted seam (W1's fakes were current). GATE: tsc 0, eslint 0, vitest 1603+1todo (58 files), build 0, cal 5/5 VERIFIED byte-idempotent two-vector, e2e 26/26 ALL THREE ENGINES (stamp.spec cross-engine clean first try). NOTE: no "goldens" npm script exists — golden fixtures run inside day.test.ts within the full suite; agents' "6/6" figures were vitest-filter runs. task-w1 worktree removed, branch deleted. W1 CLOSED.
*** BATCH 1 COMPLETE: W0, W9, W5, W10, W6, W1 all merged into build/v1 (@ 5acfea0). ***
BATCH 2 OPENED (2026-08-06 ~14:45): worktrees task-w2 + task-w7 provisioned at 5acfea0. W2 implementer (OPUS, agent aa0eb5273441abfb6): copy catalog wave — backlog W2 rows + w1b-001/002/003/004/007/008/009 + W6's TODO-W2 keys + gr6-004 consistency constraint + rulings a/b/i; barred from src/ui. W7 implementer (OPUS, agent ad2b79d515b59cb15): screens/CSS wave — backlog W7 rows incl. 7a-alone sequencing + pressForDay adoption REQUIRED (deletes W6's two [INTERIM] tests same commit) + onViewStats + .ph-entered rename per lawbook + .ph-reveal bottom-pad ruling + gr6-063 dial band-change (ruling: AMPLIFY in-law, 6px/--dur-scene) + ruling (e) sticky lab actions + didBootRef comment + UseGameStore/TFunction dedup finish + countdown-staleness re-read + w1b-005 768-overlap decision + w0-r-002 selector re-read + gr6-090 App.css comment check; barred from src/content (consumes W2 keys by name), barred from DESIGN.md (if 7a would move a registered keyframe, leave it and record why). Both: PATH guard, private scratchpads, mutation on archive copies only, commit early. SEQUENCE AFTER: W3 (after W2) → W4 (gated on gr6-091, 6 staged press tasks T39c-1..6); W8 after W7; W11 (MUST run cal after W1 — now satisfied — regenerating two-vector shape; goldens regenerate exactly once; rulings a/d/f/h/k) and W12 (rulings i/j; same-day prereg reopening decision) last; then final verification wave.
W2 IMPLEMENTED (d1bb246, 4 commits on 5acfea0, worktree clean). Catalog 226→235 keys ×3 (+13 new, −4 deleted). gr6-004 about.mechanism ×3 discloses rejection sampler quoting only compiled constants (NULL_SIG_BAND, allSpecs().length), agrees with W1's accounting1, deliberately omits 42%/26% discard rates (move with W11's §1(d)). Ruling (b) Armitage "equally spaced" ×3 + α=.05→0.05 (carve-out was conditional on spec-verbatim; ES worked example moves too; master-spec docs/implementation_plan.md:98 still pre-ruling — documented at key, plan docs outside wave). gr6-026 deletions ×4 + summary.playPrereg found DEAD (W6 removed CTA) → rostered RETIRED. gr6-027..037/065-068/086/096 all ×3; gr6-028 extended 2→5 strings ("the reveal" contract would lie half-amended). w1b-001/002/007/009 done. Gate: tsc 0, eslint 0, vitest 1632+1todo (+29), build 0, e2e chromium 22/22 (new i18n.spec decimal-point law it/es). Mutations 8/8 killed incl. root-cause proof for w1b-003 (calque restored → pre-W2 suite green 280/280, lock was EN-only). Rendered-value audit through real t() caught 6 IT ’ escapes source-grep missed. 5 deviations documented (decimalNote placement per source finding; preregUpsell names moment not screen; +3 keys gr6-028; 3 UI tests retyped catalog English → key lookups, forced; ES gains exhaustive untranslated sweep). HAND-OFFS→W7: 14 rostered keys w/ call sites in ROSTER_PENDING (self-retiring roster), altmetricPercentile {n}→{pct} (Published.tsx:285), summary.playPrereg deletion (re-pin summary.test structurally first), About.tsx restructure + Stats.tsx empty state render halves. →W11: ruling (a)/gr6-009 stays (rate re-measured after §1(d)). REVIEW dispatched (OPUS, fresh agent) — includes em-dash budget adjudication (W2 claims budget = exactly one per locale at stats.noData; W1 round enforced 0 for new values — reviewer to check the compiled law).
(W2 reviewer agent id: a1479a3f99d17a7d7. Review lenses: scientific audit incl. Armitage equally-spaced-vs-N_SCHEDULE adjudication, transcreation ×2, roster mechanism, em-dash budget law, gate + 3 replayed + 2 novel mutations, 5 deviations, hand-off scoping.)
W7 ROUND 1 IMPLEMENTED (90ffb2b, 5 commits on 5acfea0, worktree clean, gate green: tsc/eslint 0, vitest 1628+1todo 59 files, build 0, e2e 26/26). Landed: gr6-007 boot-failure page (mid-day crash keeps additive banner; 4 test suites were structurally depending on the bug — jsdom no Worker — fixed w/ booted:true seam, documented), gr6-006 call overlay surface (3.64→16.24:1), gr6-012/013 (▦▦▦ was tofu in both faces → ☆+muted)/014/015/016/017/023 (BAND_LABEL_MIN_CHARS 12→20 = IT passivo-aggressività)/024 (16 unstyled not 10; W6's blocks were at UA defaults)/025, ruling (e) sticky lab actions (R8.1 containing-block trap: direct child of .ph-lab), gr6-060/061/074/075/080/083/088-dedupe/089-ownership/104, pressForDay + both [INTERim] tests deleted same commit, onViewStats via new src/ui/nav.ts, didBootRef comment CORRECTED (locale toggle changes content mid-session — ref prevents progress reset), gr6-090 App.css note. RULINGS w/ shots (controller scratchpad w7-private/shots/): w1b-005 resolved by GEOMETRY — 45% trim ≥768 (was: label crossed title 6.2px AND frame border inside volume-line box; now frame clears by 9.9px, both question lines still stamped, 47%/32% viewport = oversized survives); .ph-reveal bottom pad KEEP 40 (Act II ends on hairline-underlined control; 24 reads as truncation); R8.1 measurement: actions 67px + dial 153px = 34% of 640; gr6-076 CLOSED-RESOLVED (does not reproduce); gr6-121 already fixed; gr6-104 second half already 153. MUTATIONS 7 (6 killed first try; #5 ??-fallback SURVIVED → added real-harness test → red). gr6-014 honesty: overflow:hidden blocks gestures not programmatic scrollTo (not player-reachable, flagged). Countdown ruling: re-boot only at briefing/log.length===0 via clientRef; mid-play affordance needs W2 copy. w0-r-002 re-read green. NOT DONE: (1) gr6-050 .ph-entered half + gr6-087 + gr6-063 — BLOCKED on DESIGN.md R5.2 register cells; agent correctly stopped on brief's prohibition; CONTROLLER RULING: prohibition lifted NARROWLY for R5.2 register cells + named rows per the lawbook's own "same commit" mechanism; (2) gr6-050's four non-law utilities (.ph-focusable ×22, .ph-label ×20, .ph-page(--titled), .ph-close ×3) — largest remaining, budget; (3) gr6-088 split barred by law (R5.2 row 5 + per-file R5.6 association) — two real defects fixed, split stays; (4) W2-key stand-ins at TODO-W2 sites (errors.reload, a11y.skipToContent proposal, submit-status, peek CI sentence, errors.newDay) + 14-key roster wiring + altmetricPercentile {pct} + summary.playPrereg deletion + About/Stats render halves + ForkTrail key — ALL after W2 merges (controller will merge build/v1 into task-w7 then W7 wires). npm ci needed --engine-strict=false (known dev-machine case). W7 CONTINUATION ROUND dispatched.
W7 CONTINUATION: gr6-063 LANDED (3f4c04e — ph-dial-settle-band 6px/--dur-scene band-keyed, same-band -2px/--dur-quick, re-arm timer reads same token so R5.6 reaches both; register row 2 moved in exactly the law's four cells; RED-FIRST demonstrated — Motion-cell-only revert reds pairs check; sign-reversal mutation killed, the failure a value table can't catch; weight-snap kept out w/ a no-grow test). gr6-087 LANDED (0126b4d — six names → two bodies ph-enter-scene/ph-enter-quick defined once in App.css, fired from five sheets; six Motion cells only; pair-diff mutation still beats counts). Gate: 1632+1todo, e2e 26/26, tsc/eslint/build 0. ESCALATION RULED BY CONTROLLER: .ph-entered collapse — R5.6's unguardedHiddenRules is invoked PER FILE, but the three hidden base rules live in three stylesheets, so a single .ph-entered can't vouch for them; (a) triplicate declarations = the §9.1 defect recreated one level up, REJECTED; (b) corpus-wide iteration of the association check = APPROVED — the association stays class-name-based (extend-by-name or component co-write) and class names are global in CSS, so per-file locality was iteration structure, not substance; a stray .ph-orphan{opacity:0} with no association anywhere still reds. CONDITIONS: (1) demonstrate the stray-orphan probe reds under the widened check; (2) re-run T38's historical Reveal.css probe and show it still fails; (3) DESIGN.md's rule paragraph records the corpus-wide iteration and the one-declaration-cannot-live-in-three-files reason (within the existing narrow authorization). W7 proceeds with ALL FIVE §9.1 utilities in one sweep (.ph-entered + .ph-focusable ×22 + .ph-label ×20 + .ph-page(--titled) + .ph-close ×3).
W2 REVIEW: NEEDS FIXES (opus reviewer a1479a3f99d17a7d7; full review persisted to .superpowers/sdd/2026-08-04-grand-review/review-w2.md). Gate reproduced exactly. BLOCKER w2-r-001: Armitage footnote — RULING (b)'s PREMISE FALSIFIED: N_SCHEDULE [200,250,300,350,400] IS equally spaced (Δn=50); true distinguishing condition = equal fractions of total information; 2M-trial measurement: Armitage design 14.17%, THIS GAME 11.18% (8.67% at first render); max peeks=4 so "five peeks" unreachable under W2's player-facing gloss; IT/ES comments self-refute. CONTROLLER RE-RULING (within owner intent — keep figure+citation, make it true; TO BE FLAGGED IN FINAL T25 REPORT): adopt (a′) — name equal fractions/equal batches as the CITED DESIGN's condition; string must read as the citation's fact, not the player's action; comments must stop turning it toward the player. HIGH w2-r-002: About's closing inference false 38% of the time at n=400 (band binds at 200; measured 8/21 outside; sig 37→5 case) — scope the sentence to the opening sample of 200, drop the absolute. MED w2-r-003 (three self-authored locks uncompiled — N3/N4 survived), w2-r-004 (About guard containment-only — N1/N2 survived; pin relation), w2-r-005 (leading-zero regex two-digit-only — N7 survived; broaden, zero false positives verified), w2-r-006 (EN header's zero-budget claim false — em-dash adjudication: catalog PASSES compiled law, only the description wrong). LOW w2-r-007 (cut→subsample), w2-r-008 (effect-day gate half-disclosed), w2-r-009 (4th TODO-W2 record-decline or author), w2-r-010 (report's "new i18n.spec" claim wrong — noted, no code change), w2-r-011 (3 craft notes). All 5 deviations ACCEPTED. Roster mechanism proven both directions. Backlog note: W2 row lists About/Stats.tsx but dispatch barred src/ui — render halves are W7's (recorded here so no reader thinks W2 undone). FIX ROUND 1 dispatched to ORIGINAL implementer aa0eb5273441abfb6.
W7 .ph-entered LANDED (ee4c67f; 8 commits total on task-w7; gate 1635+1todo, e2e 26/26, tsc/eslint/build 0). All ten enumerated places one commit; descendant gate is .ph-fade.ph-entered .ph-stamp--animate (never bare flag) — w0-r-002 re-checked green through stamp/abandon/reduced-motion specs. ALL THREE controller conditions demonstrated on archive copies: (1) stray-orphan reds under widened check — landed as PERMANENT test; (2) T38 historical probe re-pointed at corpus still red (class-based association defeats it, not file boundary) + stronger variant (removing a real co-write reds ".ph-fade has no restoring modifier"); (3) widening predicate vocabulary reds the co-written-non-modifier guard. FLAGGED instrument change (accepted by controller): legacyRestorerCount — the retired per-file helper kept only so probe 2 can model the OLD check — matched '--' only, reported 0 restorers post-collapse (probe passing for wrong reason); vocabulary widened alongside predicate (--[\w-]+|\.ph-entered); test-local model, not a guarantee. COMPONENT_CLASS_PAIRS negative control moved (ph-press-card, ph-fade--in)→(ph-press-card, ph-fade) — rename made old negative true by construction; still a real negative. DESIGN.md scope held (R5.2 cells sites 4+9, R5.6 restorer list, rule status line, new corpus-wide-iteration paragraph; no other prose). NOT DONE: four non-law utilities (.ph-focusable ×22, .ph-label ×20, .ph-page/--titled, .ph-close ×3) — agent hit context exhaustion, stopped CLEAN (nothing half-applied), left inheritance notes: membership per §9.1 match-first (.ph-page covers about/legend/stats/summary/lab keeping lab's flex; --titled covers briefing/prereg; .ph-call NOT a page; .ph-reveal does NOT adopt — bottom-pad ruling recorded at site, restate beside --titled); .ph-close keeps margin-block-start at 2-of-3 sites (margin is layout); R6.6 .ph-screen NOT .ph-focusable (§9.1 constraint 4 exception); new adopters from this wave: .ph-boot-error__reload (+.ph-label), .ph-skip-link, .ph-summary__next-button. FRESH utilities implementer dispatched into task-w7 (original agent context-exhausted).
(W7b utilities implementer agent id: a17c397c7237c7571 — fresh opus in task-w7 @ ee4c67f; method: computed-style baseline dump before any edit, one utility per commit, diff-per-utility must be zero-or-named; barred from DESIGN.md entirely this round.)
W2 FIX ROUND 1: DONE (ba37d44, 5 commits on 5acfea0, worktree clean; gate tsc/eslint/build 0, vitest 1652+1todo (+20), e2e chromium 22/22; catalog invariants re-measured: 235 ×3, one dash/locale, zero commas, zero leading-zero misses under BROADENED regex, zero U+2019). w2-r-001: implementer independently REPRODUCED the falsification first (1M trials: Armitage 14.172%, game 11.174%, first-render 8.681%) then landed (a′) ×3 impersonal ("testing after each of five equal batches of data turns α = 0.05 into ~14%"); comments carry re-ruling + five-row table + don't-simplify instruction; BEYOND ASK: string compiled per locale (requires equal-batch condition, bans your/tuo/tus AND equally spaced/equidistanti/equidistantes, pins 14% + citation + α = 0.05; second-person ban is a correctness pin — addressed to the player "five" is unreachable). w2-r-002/008: band clause carries own n, drift has own sentence ("checked at 200 and nowhere else… the count moves, sometimes a long way"), effect gate names both halves, absolute gone, all compiled ×3, agrees-with-accounting1 assertion holds. w2-r-003: three locks compiled (retired-term ban over VALUES only, reveal. key prefix untouched; streak lock ×4 keys ×3; la clave scoped to definite NP w/ guards-the-guard case sparing "una pieza clave"). w2-r-004: ordered between-span from constants + toContain(legend.significant). w2-r-005: broadened; ZERO catalog false positives but fires on ONE CORPUS string/locale — Grantwell email "a p-value of .06 is just a p-value of .05 with poor time management" (character writing sloppily; index.ts is W3's file) — guard scoped to copy, reading in test comment, BOOKED FOR W3 (likely reasoned wontfix). w2-r-006: rule 10 states the real law + today's re-measured counts. w2-r-007 taken. w2-r-011 all three taken (IT fa pendere la bilancia; page-fills subject ×2 w/ EN divergence documented; decimalNote verb ×2; ES also → poner el dedo en la balanza). w2-r-009 DECLINED w/ recorded reasoning at published.careerPoints (agreement-by-identity is the stronger contract; gr6-018 renders career as own line; proposed wording carried a dash). w2-r-010 acknowledged. MUTATIONS 13/13 KILLED (N1,N2,N3,N3b,N4,N4b,N4c,N7,N8,N9,N9b,N10,N11). Backlog W2-row ownership amendment appended by controller. SCOPED RE-REVIEW dispatched to ORIGINAL W2 reviewer a1479a3f99d17a7d7.
W2 SCOPED RE-REVIEW: NEEDS FIXES on ONE item, everything else resolved+mutation-verified (all 6 previously-surviving probes now die; N9/N9b reproduced; novel N10 drift-clause-deletion killed; gate reproduced exactly). w2-r-012 MED transcreation — IT "cinque lotti di dati uguali" / ES "cinco lotes de datos iguales": post-posed adjective attaches to NEAREST noun, all masc-pl so morphology can't disambiguate → default parse "batches of IDENTICAL DATA", condition lost in 2 of 3 locales (same class as w1b-009 da-sola). Fix: transpose → "cinque lotti uguali di dati" / "cinco lotes iguales de datos" + move both armitageCondition regexes, one commit. EN adjudicated scientifically correct+unambiguous (pre-posed adjective = equal-sized = equal information fractions). Reviewer: "hold the merge for one commit… I do not need to see the rest again." Other adjudications: w2-r-005 corpus scoping CORRECT (grantwell[0] is the one string ×3; W3 disposition = keep+record, allow-list entry); w2-r-009 decline ACCEPTED (agreement-by-identity argument better than the finding); rule-10 char counts moved this round (re-measure at merge or drop); effect-gate "detectable" mild overstatement LOW non-blocking w/ exact alternative recorded. FIX dispatched to implementer; controller verifies + merges directly after (no further re-review round).
W2 FINAL FIX (ade465d): w2-r-012 transposition ×2 + regex pins moved same commit + load-bearing-order notes at the strings; rule-10 counts: took the DROP (dash counts kept 3/3/1, char counts removed w/ reasoning in-line; re-measured 36,021/41,368/41,423 matching reviewer exactly). Mutations N12/N12b red PER-LOCALE. Green: tsc/eslint/build 0, 1652+1todo, content suites 321.
W2 MERGED -> build/v1 @ bb8ad1c, PUSHED. Zero conflicts (10 files, +1456/−164). GATE ON MERGED TREE: tsc 0, eslint 0, vitest 1652+1todo (58 files), build 0, e2e 26/26 three engines. task-w2 worktree removed, branch deleted. W2 CLOSED. task-w3 provisioned at bb8ad1c. W7 wiring round PENDING W7b utilities completion (then controller merges build/v1 into task-w7).
W3 DISPATCHED (opus agent abed8c79af733547f, task-w3 @ bb8ad1c): content banks (corpus — scenarios/emails/press/journals in index.ts + banks), barred from src/ui + engine + game + reworking W2's fresh strings; carries the grantwell[0] adjudication (allow-list-with-reasoning disposition recorded by both reviewers); parody-only journals/outlets, plausible-but-fictional science.
W7b UTILITIES: DONE (4 commits ee4c67f→7700a98, DESIGN.md untouched, gate 1637+1todo → then; e2e 26/26). Adoption: .ph-focusable 23 rules/31 sites (+1 = gr6-007's new reload), .ph-label 22-in-21 (+watermark grouped pair), .ph-page 5+2 exact per inheritance, .ph-close 3 exact. Computed-style instrument: 19 states ×2 viewports ×2 arms (normal + CDP-forced :focus-visible so .ph-screen observable), ~76k observations/run, noise floor PROVEN 0 (byte-identical baselines), non-vacuity proven. Value-diffs 0/0/0/0 across four commits; INSTRUMENT EARNED ITS KEEP: caught grouped watermark rule de-casing (uppercase→none, 1.04px→normal on both cards ×2 widths ×8 states) that NOTHING in 1637 tests + 26 e2e asserts — fixed in-commit. classNames.test predicate: closed four-name map screen-root→utility, both ends pinned (+2 tests). Deliberate non-adoptions all §9.1-reasoned (.ph-screen constraint-4, descendant underline, emoji tracking, eyebrow T29 pin 6, .ph-call not-page, .ph-reveal recorded ruling restated beside --titled, .ph-legend__close REMOVED CSS+DOM — nothing addressed it). FLAGGED to controller: .ph-boot-error + .ph-published__cover are exact value-matches for --titled but outside §9.1's enumeration → handed to W7c. CONTROLLER: merged build/v1 (bb8ad1c) into task-w7 @ 8056886 (one import-union conflict in appNav.test resolved; auto-merge doubled enCopy import in shell.test → deduped 33d844c); post-merge tsc 0, vitest 1686+1todo (59 files). W7c WIRING ROUND dispatched (fresh opus agent ac7a2557fb483548a): 14 roster keys + TODO-W2 stand-ins + altmetricPercentile {pct} atomic w/ Published.tsx:285 + playPrereg deletion after structural re-pin + About/Stats render halves + gr6-029 check + the two value-match rulings. After W7c: FULL W7 review (fresh opus — covers rounds 1+continuation+.ph-entered+W7b+W7c incl. corpus-wide R5.6 widening, legacyRestorerCount instrument change, COMPONENT_CLASS_PAIRS negative-control move, classNames four-name map).
W3 IMPLEMENTED (12 commits on task-w3 @ bb8ad1c base, worktree clean; gate tsc/eslint/build 0, vitest 1690+1todo (+38, 58 files), content suites 359, e2e chromium 22/22). gr6-005 BLOCKER: measured FIRST — 71,680/71,680 valid paths round to 1 (floor did all the work) → all 19 token headlines re-cut token-free ×3 (deviation: 19 not the 6 supplied), rule 5 rewritten w/ measurement + reinstatement condition, pinned by regex AND real substituteEffect w/ live-substitution vacuity control. gr6-037 ten nullReportedSublines/locale (NOT wired — Reveal.tsx is W7's). gr6-038 seven slot-0 labels relative ×3. gr6-039 jigsaw outcome-0. gr6-040 all 15 income proxies (distinct 3→17, max rep 15→4, honesty constraint in authoring rules). gr6-041 five openers re-entered (gaps 16→12/20; cat-crypto+full-moon needed new closers — authorized for one, extended reasoned). gr6-069 EN-only in effect (IT/ES already conformant; 401(k)→PENSION; voice law re-anchored on fixture + live IT/ES accent assertions). gr6-070 grantwellSubjects 22/locale INDEX-PAIRED (NOT wired — Briefing.tsx is W7's). gr6-071 α 8→1 ordering. gr6-072/073 dosage trims (11→7/20). gr6-085 findParamFieldTokens in validateLocaleContent, 12 fixtures, headline-exclusion itself tested. GRANTWELL[0] ADJUDICATION: KEEP w/ reasoned allow-list (not narrowed regex — regex would spare the phrasing in copy catalog where it WOULD be a defect); self-retiring both ways + guards-the-guard fixture; CONTRAST CALL: pre-existing EN grantwell[15] U+2019 STRAIGHTENED not allow-listed (encoding accident ≠ characterization) — distinction now the written rule, apostrophe law compiled corpus-wide. AS-RENDERED re-read caught 2 unit-mismatch defects diff couldn't show (metrics recounted, units stayed — fixed ×3) + 3 own-prose transcreation nits. MUTATIONS 12/12 killed. 8 deviations recorded. BOOKED: →W7c/W7: Briefing.tsx:193 pickGrantwellEmail wiring + DELETE briefing.emailSubject same commit; Reveal.tsx:300 NULL_REPORTED sublines branch; →W7/W9: tests/ui/published.test.tsx:145 now VACUOUS (token-free corpus) — re-pin against token-carrying fixture or retire; →controller/W11: published.ts Math.max(1,…) floor unreachable from shipped content, "1 Minutes" if token returns — decision w/ gr3-001 alternative. Parody-masthead note recorded (pre-existing, unchanged, gr3-033 precedent). REVIEW dispatched (fresh opus).
(W3 reviewer agent id: a3b0ab5311e180efe — fresh opus; lenses: scientific reproduction of the floor measurement, comedy/writing craft w/ worst-3/best-3 quotes per locale, transcreation ×2, contract/guards re-runs, 4 replayed + 2 novel mutations, 8 deviations, 3 bookings, parody-note check.)
W7c WIRING: DONE (e35a653, 11 commits atop 33d844c, tree clean; gate tsc/eslint/build 0, vitest 1698+1todo (+12), e2e 26/26, ROSTER_PENDING EMPTY). All 14 keys wired w/ rendered evidence; roster mutations both directions red. altmetricPercentile {n}→{pct} atomic ×4 files, both half-renames red. playPrereg: re-pin FIRST (398a321, measurably stronger — different-label CTA reintroduction beats old pin 55-blind-pass, new shape pin catches 2-failed) then delete (478c93d). About render half: standfirst + four h2s W2's order + decimalNote under None-of-this-is-real; flat siblings not sections (landmark reasoning); .ph-about__standfirst weight+space-8 NOT muted (R1.2 list) NOT sixth size (R2.2); 4 mutations red. Stats: note under title at played===0, both success panels stay (§2.8 juxtaposition is the lesson); deliberately NOT reusing .ph-stats__empty (mono/tabular numeral stand-in); 3 mutations red. gr6-029: W2 did BOTH (reworded hint in place AND added lab.forkTrailKey) — one line repointed, mutation red. §9.1 RULINGS: ADOPT .ph-boot-error (postdates section, passes category test — whole viewport, sole main content, h1); DECLINE .ph-published__cover (passes values FAILS category — inner column of §7.3 full-bleed takeover, deliberately exempt from R3.5; adoption would couple the unframed screen to future shell changes; recorded at site). DEVIATIONS: (1) errors.newDay DOESN'T EXIST in any locale (W2 never wrote it) — midnight mid-play affordance re-booked in place, → W8 BRIEF (author key ×3 w/ transcreation care + wire); (2) DESIGN.md §9.1 "the two that spell 40-24-24" is now THREE — OWED AT W7 MERGE (controller edit); (3) SHELL_CONSOLIDATED gained ph-boot-error (pinned-list extension, predicate unchanged, both mutations red); (4) briefingFinishedDay countdown regexes re-derived from key; (5) W3-booked wirings correctly skipped (corpus not in tree; briefing.emailSubject still live); (6) one historical TODO-W2 substring in W2's declination note left; (7) two new CSS rules justified in-file. FULL W7 REVIEW dispatched (fresh opus, all four rounds).
W3 REVIEW: NEEDS FIXES (fresh opus a3b0ab5311e180efe; full review persisted to review-w3.md). Gate + all numeric claims reproduce EXACTLY (floor measurement byte-for-byte, +38 delta, invariants). BLOCKER w3-r-001 HIGH: 5/10 nullReportedSublines false on abandoned effect days (25% of days; reveal.ts:81 day-type-blind; same screen shows truthEffect β≠0 while sublines deny an effect; RETRACTED bank not precedent — spec-scoped) — bank is UNWIRED so nothing ships false today; fix day-typed variant or rewrite the five + caveat at Reveal.tsx:300 hand-off. MED: w3-r-002 IT negative concord, w3-r-003 apostrophe law scope→localeProse (N7 survived), w3-r-004 dosage ratchets (N1/N2/N3 survived; accept no-guard only for 038/039), w3-r-005 percentage-points unit ×3. LOW 006-013 incl. published.test.tsx:145 vacuity CONFIRMED STRONGER (N4: severed substituteEffect leaves whole suite green on head) and w3-r-011 floor-attribution correction (96.7%; deleting floor prints "0") → fold into W11 booking. Deviations: 7 accepted (grantwell[0] COMMENDED, survives harder M8b probe), 1 split (dosage). Craft verdict: re-cuts LAND; worst-3/best-3 per locale recorded in review file. FIX ROUND 1 dispatched to ORIGINAL implementer abed8c79af733547f. (W7 full review also in flight, agent a37a63fd1143ce8f3.)
W3 FIX ROUND 1: DONE (15 commits on bb8ad1c, tree clean; gate tsc/eslint/build 0, vitest 1708+1todo (+18 this round, +56 vs base), bundle 114.9/200, e2e chromium 22/22). w3-r-001 taken shape (b): premise reproduced first (15/60 effect days, 8/10 sublines reachable, drove every day printing subline beside truthEffect); five rewritten day-type-agnostic under written rule "say what happened to the REPORT, never what the day contained" (RETRACTED-not-precedent note at bank); second pass re-scoped [3] "Nothing in it was wrong" (also closes w3-r-010 + [0]/[3] duplication); [1] Grantwell callback; [7] CI-cannot-exist dropped; COMPILED FLOOR per locale (truth vocabulary + existential shapes; first draft caught 5/5 EN but 4/5 IT/ES — the exact asymmetry the check exposes — both extended; labelled floor not oracle); hand-off caveat: future branch keys on payload.dayType NOT the stamp, !isPrereg gate not precedent. w3-r-002 concord restored. w3-r-003 localeProse — law over corpus AND catalog, review's probe now the check's second half. w3-r-004 three RATCHETS (income≤4; device ≤ own-locale counts EN7/IT5/ES4 — argued; sense≤1); 038/039 no-guard reason recorded. w3-r-005 percentage points ×3. w3-r-006/007/008/012 all taken (telescope → plain comparative; ES señalar por su nombre; IT nominare stands). w3-r-009 both halves (3 index anchors/locale + residual-risk comment). w3-r-011 taken — CHANGES THE CONCLUSION: 96.7% floored-from-0 / 3.3% genuine; W11 booking now instructs NOT to open by deleting the floor. w3-r-013 DECLINED in writing at the ceiling (warmth ×2 never a finding; confidence differs by rater prefix — construction's varying element). MUTATIONS 10/10 killed incl. novel N8d ("The effect was never there to begin with") proving the guard generalizes. Invariants re-measured exact. SCOPED RE-REVIEW dispatched to W3 reviewer a3b0ab5311e180efe.
W3 RE-REVIEW: APPROVED (all 13 resolved-or-declined-with-accepted-reasoning; verified on the REAL path — drove three abandoned effect days through generateDay→enumerateCurve→buildRevealMetrics(…,null,…) and read block 1 against the landed subline; all ten now day-type-agnostic ×3 locales; N8a-d die in their OWN locale proving the asymmetry closed; novel N8e — an unlisted false phrasing — SURVIVED exactly as the docblock discloses, so "floor not oracle" is honest labelling; ratchets bite with ZERO slack, N2c proves the per-locale ceilings matter, N3c proves ratchet-not-equality; N7-REDUX + novel N7b die; N6-REDUX dies; round-1 guards M8a/M8b/M1 undisturbed; craft: [1] Grantwell callback "now the funniest line in the bank"). One comment-only nit: "four SELF-ratings" → three of four. W3 MERGED -> build/v1 @ 2d6f5dd + comment fix ec6f99b, PUSHED. GATE ON MERGED TREE: tsc/eslint/build/cal 0, vitest 1708+1todo (58 files), e2e 26/26 three engines. task-w3 removed. W3 CLOSED.
W7 FULL REVIEW: NEEDS FIXES (1 HIGH, 2 MED, 5 LOW; full review persisted to review-w7.md; gate reproduced exactly). CONTROLLER RULING on w7-r-001: take fix (a) — hoist the shared restorer into ONE App.css rule at sufficient specificity (.ph-fade.ph-entered, .ph-press-card.ph-entered, .ph-chyron.ph-entered, .ph-summary__unlock-item.ph-entered { opacity: 1 }), keep per-file animation/animation-delay (R5.2 requires them), update motion.test.ts:466. Reason: (a) is what the corpus-wide widening was approved FOR — it makes the ruling load-bearing rather than retroactively unjustified, keeps the law text true, and the reviewer measured it as order-independent and value-neutral; (b) would mean reverting an approved law change to preserve the very triplication that was rejected. ALSO: restore COMPONENT_CLASS_PAIRS' negative control to a pair whose second member IS in the vocabulary. w7-r-008 CONTROLLER RULING: ACCEPT AND RECORD — no stamp ink has ever touched the volume line at any width (134.79px horizontal clearance measured); the failing criterion is box-level only, and R3.4 forbids a second breakpoint; the comment must state the measured horizontal clearance so the criterion is honest rather than silently unmet. CONTROLLER merged build/v1 (W3 corpus) into task-w7 (tsc 0) so the two W3-booked wirings can land in the fix round.
W4 DISPATCHED (opus agent afd3facb288956e76, task-w4 @ ec6f99b): press system, six staged tasks T39c-1..6, gr6-091 gate satisfied (W6's audited picker merged, W7 adopted pressForDay as sole path); barred from src/ui (W7 fix round active), engine, game, DESIGN.md; carries W3's compiled laws + parody-only constraint + as-rendered discipline.
W7 FIX ROUND 1 dispatched to W7c implementer ac7a2557fb483548a: w7-r-001 per controller ruling (a) (single App.css compound-selector restorer, keep per-file animation/animation-delay, motion.test.ts:466 update, DESIGN.md:523-535 corrected, COMPONENT_CLASS_PAIRS negative restored to an in-vocabulary pair, reviewer's N1 must go RED to prove the widening load-bearing), w7-r-002 rendered-shape or corpus-wide unsubstituted-token guard, w7-r-003 rollover tripwire (e2e or makeWorker seam; N6 must red), w7-r-005 base-class regex + corpus negative (N5 must red), w7-r-006 written declines, w7-r-007 annotation correction, w7-r-008 per ruling ACCEPT AND RECORD w/ measured horizontal clearance stated, w7-r-004 cheap tripwire optional (gap pre-existing, fair control confirmed) PLUS the two now-available W3 wirings (Briefing.tsx:193 pickGrantwellEmail + delete briefing.emailSubject same commit; Reveal.tsx:300 nullReportedSublines w/ W3's day-type caveat read first).
W4 IMPLEMENTED (3 commits on task-w4 @ ec6f99b; all six staged tasks T39c-1..6 delivered; 102 new strings = 34 cells ×3 locales; NO new mastheads invented). MEASURED through shipped pressForDay (180,000 draws/locale): cells covered 26/60 → 60/60; first card bespoke 43.33% → 100.00%; same-outlet pairs 0.00% (held); repeated line on a page 0.00% (held); em-dash COUNTS unchanged 3/3/1 (zero new dashes written, so IT ratchet holds 3≤3 by construction not luck), density improved every locale. Distinct presentations 313→251 BY DESIGN (card 1 now pinned to the study, not rotating generics) — which is why distinctness is measured on outlets. GUARD: findUncoveredPressCells/pressCellCoverage in validators.ts wired into validateLocaleContent (all locales inherit) + 3,000-date simulation in shape.test.ts, shipped as a RATCHET (37→48→60) that becomes hard law at 60, kept because it prints WHICH cells are missing. MUTATIONS 8/8 caught (incl. emptied Italian cell proving the law reaches IT; inverted picker slot-0 preference caught in all 3 locales). DEVIATIONS: (1) three commits not six — IT/ES press is index-parity-pinned to EN so an EN-only commit is structurally red, and T39a's placeholder workaround would reverse W3/T39b's permanent no-byte-identical law; six authoring passes kept as work/reporting units; (2) review-w3.md WAS NOT VISIBLE in the worktree — CONTROLLER PROCESS GAP: reviews were written to the main checkout but never committed, so worktrees off build/v1 saw only review-w1.md; W4 reconstructed the laws from validators.ts + shape suites and verified by mutation (sound, but shouldn't have had to). FIXED: dfd1b70 commits review-w2/w3/w7.md + ledger; ALL FUTURE DISPATCHES can now cite review files by path. (3) tier-1 band shifted (pre-wave max 150/176/212; initial 206/234/244; trimmed four strings to 190/218/217) — reported not hidden; Published card is W7's surface. BOOKED: →W6 review two tests/game/published.test.ts fixture repairs (both premised on the defect this wave removes, neither fixable from W4's files; properties preserved exactly — generic-pair test now reads the follow-up tail, agnostic-fallback uses a synthetic bank with the binding filtered; both carry CROSS-WAVE NOTE comments); →W6/editorial T39a's "a tier is a VOICE not a volume knob" rationale is superseded by §1(g) — comment updated in place, POLICY changed not just data; →W3/editorial (pre-existing, W4 added none): tier-2 generic blurbs name real awards (Pulitzer/Premio Strega/Premio Nacional) — not outlets/journalists so no parody-rule breach, but the same real-world-specificity class W3 was reducing. CONTROLLER: real-awards question routed to the W4 reviewer for adjudication. REVIEW dispatched (fresh opus).
W7 FIX ROUND 1: DONE (5 commits atop the build/v1 merge, tree clean; gate tsc/eslint/build 0, vitest 1775+1todo (59 files), e2e 28 THREE-ENGINE (26 baseline + 2 midnight)). w7-r-001 fix (a) taken: single App.css compound-selector restorer, per-file animation/animation-delay kept per R5.2 File column each named against its own base class (no two byte-identical); VERIFIED IN CHROMIUM against production build — App.css rule matches FIRST in sheet order, computed opacity 1 on press cards and reveal fades, counterfactual bare .ph-entered gives 0 (permanently invisible), so the cascade constraint is real and compound selectors solve it by specificity order-independently. WIDENING NOW LOAD-BEARING: unguardedHiddenRules takes file-tagged sources and only the component-attested association crosses a file boundary (a -- name extension is BEM-local). TWO PRECISION FIXES the hoist forced, both mutation-found: restorer selectors split on commas; a restorer vouches only if its whole compound is satisfiable on the element. Negative control → (ph-press-card, ph-stamp--animate) + two non-vacuity assertions. Mutations N1→5 failed, N5→2, drop-one-compound→5. DESIGN.md:523-535 rewritten (narrow authorization honoured). w7-r-002: took the STRONGER option — corpus-wide {token}↔param-name reconciliation ×3 locales both directions, 163 sites parsed / 24 with params / 0 SKIPPED pinned by its own assertion BECAUSE the check shipped with a real parse bug for an hour (spread test scanned the rest of the FILE not the literal's extent, silently skipping nearly every site while passing loudly) — self-caught, self-pinned. Mutations: half-rename A→6, one-locale drop→1, unsupplied token→1, parse regression→3. w7-r-003 SURFACED A REAL DEFECT BEYOND THE REVIEW: the rollover HAD NEVER ONCE FIRED — boot() seeds a free VIEW_SPEC entry (§2.10) so log.length===0 can never hold; screen==='briefing' alone is complete AND stronger ('briefing' set only by initialState(), nothing navigates back); two e2e (destructive + honoured); mutations delete-guard→destructive failed, never-fires→positive failed, restore-dead-conjunct→positive failed. W3 WIRINGS: subject paired index-for-index (old constant was "Re: the deadline"), briefing.emailSubject deleted ×3; NULL REPORTED subline wired WITHOUT a day-type branch per W3's caveat + test asserting both day types get the SAME line; mutations own-seed→2, removed→2, FORBIDDEN stamp-keyed branch→1. w7-r-004 tripwire under three CTAs + both watermarks (both reviewer probes red + original re-declared-casing defect). w7-r-005/006 done. w7-r-007 re-measured independently with a Range (one rect per line box): THREE runs at 360, frame crosses the third, clears second by 6.31px (reviewer: 6.32). w7-r-008 accepted+recorded with measured 134.79px horizontal clearance. DEVIATION FLAGGED: the rollover guard change is a behaviour change the review did not ask for (dead code → live), documented. STILL OWED AT MERGE (controller): DESIGN.md notes 2+3 — §9.1:859-860 two→three, record .ph-published__cover decline, §9.1:830/866 staleness, and R5.6:486-492 which now says .ph-entered "currently ships as three names" (DOUBLY stale after this round). errors.newDay still unwritten → W8. SCOPED RE-REVIEW dispatched to W7 reviewer a37a63fd1143ce8f3.
W7 SCOPED RE-REVIEW: NEEDS FIXES on ONE item (w7-r-009), seven of eight closed and independently mutation-verified; gate re-run matches (1775+1todo, e2e 28). w7-r-001 CLOSED AND THE CLAIM IS TRUE — reviewer verified rather than accepted: N1 re-run RED naming .ph-ghost; PER-FILE COUNTERFACTUAL reds with ALL FOUR sites named, so the widening now pays for itself and "the law text is finally true of the tree"; both precision fixes exact in the safe direction (every attempted widening fails conservatively); negative control now genuinely exercises the mechanism w/ non-vacuity from both ends — "better than what I proposed"; DESIGN.md:523-548 matches what ships and opens by admitting the first version "described a tree that never shipped". w7-r-003 BEHAVIOUR CHANGE ADJUDICATED CORRECT — dead-code diagnosis independently verified (free VIEW_SPEC appended INSIDE boot() at store.ts:325 with its own comment "screen stays 'briefing'", so log.length===1 at a booted briefing and the old conjunct returned every time; only window where log.length===0 && booted coincide is a few ms of boot prefetch a 60s interval never samples); screen==='briefing' COMPLETE by exhaustive grep (assigned in exactly one place, initialState() at store.ts:205; every other assignment moves forward); reviewer drove all three cases incl. writing its OWN finished-day e2e probe (new day arrives, finished block clears, phackle.v1 survives intact — history score 80, careerPoints 80). w7-r-004/005/006/007/008 all closed and mutation-verified (w7-r-006's decline "a genuinely better argument than a rename", incl. the collision point the reviewer hadn't considered: .ph-stats__ach is already the row class). W3 wirings both correct (pairing pinned per-index not just count; gr6-037 no day-type branch, reviewer's dayType-offset mutation reds; reviewer also checked the axis the comment doesn't discuss — all ten lines are about the report's reception, none assumes hacking, so no !isPrereg gate is right).
w7-r-009 MEDIUM (new): the token-reconciliation key regex accepts exactly ONE dot, so multi-segment keys are never scanned and SKIPS-NOTHING-SILENTLY structurally cannot see them (they never enter allCallSites). Measured: shipped regex finds 164 literal sites, (?:\.[a-zA-Z0-9]+)+ finds 172 → 8 UNSCANNED (lab.explain.{outcome,subgroup,covariates,exclusion,transform,tails} in SpecControls.tsx; lab.howThisWorks.{title,dismiss} in Lab.tsx). PROVEN: {ghostparam} added to lab.howThisWorks.step1 ×3 leaves suite green (1775, tsc 0) and renders raw to the player — the gr6-086 class in the keys the check can't see. ZERO LIVE IMPACT (no multi-dot key carries a token today). Fix: widen the pattern AND add the class-closing assertion — every catalog value carrying a token is reached by >=1 scanned call site (reconciles from the catalog end where a regex gap cannot hide). Dispatched to W7c ac7a2557fb483548a; reviewer needs no further pass.
MERGE-TIME DESIGN.md LIST NOW SIX (controller work, not implementer): (1) §9.1 constraint 3 has TWO separate two→three counts — "--titled covers the two that already spell 40-above-24-around" (now three w/ .ph-boot-error) AND "Two sites are not on the shell" (now three w/ .ph-published__cover); neither class name appears anywhere in DESIGN.md; (2) §9.1:829-831 "the consolidation itself is a later commit" — landed; (3) §9.1:892 .ph-reveal "left open deliberately, to be ruled" — it WAS ruled (keep 40); (4) R5.6:486-492 "currently ships as three names" — trebly stale; (5) NEW: R5.6:526 says "one shared flag cannot live in four files" then names three — four SITES across three STYLESHEETS; (6) NEW: R5.6's "ten places" paragraph is an index of line numbers and this round's +315 lines to motion.test.ts invalidated ALL NINE (:307 now the w7-r-005 clause, :457/:474 bare `});`, :466/:501 comments, reveal.test.tsx:325 blank, summary.test.tsx:954 is `  );`) — re-point or re-tense as history and drop the numbers.
W4 REVIEW: NEEDS FIXES (fresh opus a06c4e0805eb0ef92; full review persisted to review-w4.md). Gate 1719+1todo, bundle 116.3/200. EVERY numeric claim reproduces, several byte-for-byte; the wave's real effect is BETTER than its report (globally distinct presentations 128→251 not 313→251; lived 20-day cycle 13/20→20/20 distinct pages, 7 exact repeats→0; and under a dedup bypass W4's content takes repeated-line from 38.33%→0.00%, i.e. strictly less fragile than base). BLOCKER w4-r-001 HIGH: the out-of-scope generic-pair fixture repair silently retired gr6-064's scenario salt — N4 (delete the salt) is KILLED by the ORIGINAL test on base and SURVIVES on task-w4 with all 1719 green; ledger's "properties preserved exactly" is FALSE; the matrix itself masks the salt's absence because a bespoke card 1 shifts the tail per scenario, and `signatures.size > 1` is far too loose. Fix (a) synthetic-bank technique (W4's own, from the OTHER repair) or (b) threshold >=4; N4 must go red. MED: w4-r-002 (bespoke text↔scenarioIds pinned by nothing — N3 swapped two cells' texts, suite green; exposed surface tripled 13→47), w4-r-003 (tier register laws compiled nowhere — N2 rewrote a tier-1 cell as second-person tabloid, suite green), w4-r-004 (IT horoscope "indovinare" inversion — the IT cover story three lines away has it right), w4-r-005 (three load-bearing comments describe content that isn't there, incl. two commit messages), w4-r-006 (four ES grammar defects). LOW 007-013. CONTROLLER RULING RECORDED — REAL AWARDS (Pulitzer/Premio Strega/Premio Nacional at press[26]): **KEEP, CLOSED WRITTEN DECLINE** per the reviewer's four grounds (the parody rule is about impersonation not reference; the specificity is load-bearing because the joke is aspiration and needs a real ceiling; removing it would break the best pre-existing transcreation, where each locale reached for its own culture's summit — the delta rule working; and the class is narrower than W3's, which reduced specificity mistakable for a factual claim). NOT a standing item; grantwell[0]'s allow-list is the precedent. Deviations 1/2/3 ACCEPT (3 with a MEASURED hand-off to W7: +1 rendered line, ES tier-1 tallest card 318px at 360, horizontal overflow FALSE everywhere, press block grows ≈64px worst case — give W7 this, not the character counts). FIX ROUND 1 dispatched to implementer afd3facb288956e76.
W7 FINAL FIX (f4f26b2): w7-r-009 closed in one commit, copyFreeze.test.ts only (+184/−8). Pattern hoisted to a shared KEY constant used by ALL THREE regexes — check 1's T_CALL_RE/BRACKET_RE had the identical hole, so a typo'd multi-segment key was invisible to the key-exists scan too. WHY THE MISS WAS TOTAL: against 'lab.explain.outcome' the body matches lab.explain, then the closing-quote backreference finds '.' and the whole match fails, so the site never entered allCallSites. Part 2 taught the parser the product's other binding shapes (interpolate(copy[k],{…}); copy[k].replace('{tok}',…); a bare copy[k] read = a site supplying NO params, the correct claim) rather than granting amnesty — with only t() scanned the assertion would have needed a twelve-key amnesty list including reveal.accounting1Effect (five tokens, most token-dense string in the product), "which is not a guard". Coverage 162→187 sites, 24→31 with params; of 32 token-bearing values 27 scanner-reached, 5 via DYNAMIC_KEY_GROUPS carrying an INVARIANT not an excuse (each member must carry exactly its one site's token set, so a token added to any diverges from its siblings; self-retiring like the dead-key roster). MUTATIONS: M1 (reviewer's exact probe) 3 failed; M2 multi-dot key WITH a scanned site 1; M3a regex hole alone green — honestly so, zero live impact; M3b hole + probe 3 failed = THE STRUCTURAL PROOF that the catalog-end check works where a regex gap cannot hide; M4 dynamic-group divergence 1; M5 scannable key parked in the enumeration 4. Gate 1784+1todo, e2e 28.
CONTROLLER VERIFICATION: independently replanted the {ghostparam} probe on a git-archive copy of f4f26b2 (first attempt's regex missed because the value is double-quoted — corrected, not assumed) → RED in all three locales.
W7 MERGED -> build/v1 @ 18aae1a, PUSHED. Zero conflicts. ALL SIX owed DESIGN.md edits landed IN the merge commit (two stale §9.1 counts w/ .ph-boot-error's category reasoning + .ph-published__cover's decline; .ph-reveal ruled keep-40; "later commit" → landed, with the cascade constraint that forces compound selectors written down; R5.6 "ships as three names" → past tense; "four files" → four sites across three stylesheets; "ten places" line-number index re-tensed as history w/ numbers dropped and a note saying why). motion+tokens suites re-run green after the doc edits. GATE ON MERGED TREE: tsc 0, eslint 0, vitest 1784+1todo (59 files), build 0, cal verified, e2e 28/28 three engines. task-w7 removed. W7 CLOSED. *** 9 of 12 waves merged. *** task-w8 provisioned at 18aae1a.
W4 FIX ROUND 1: DONE (all 13 findings; gate tsc/eslint/build 0, vitest 1740+1todo (58 files, up from 1719), e2e chromium 22/22; matrix unchanged 60/60, bespoke 100.00%, same-outlet 0.00%, em-dashes 3/3/1). w4-r-001: reproduced first (salt deletion survived, 1719 green), took the SYNTHETIC-BANK technique not the threshold — with every bound row filtered out all three slots share one agnostic pool and safePool/offset are identical across the twenty scenarios, so the salt is the ONLY route by which the scenario id reaches the index; unsalted = exactly 1 signature per tier, so `> 1` is now STRUCTURALLY EXACT rather than merely true; salt-deletion mutation RED. ALSO DELETED ITS OWN SECOND TEST from W6's file ("a scenario's whole page differs across nearby dates") — the design CONTRADICTS it (card 1 is deterministic per cell by design, T39a's guarantee), so it would have been a FALSE LAW; player-facing restatement moved to W4's own suite, shrinking its footprint in W6's file to the single corrected test. "Properties preserved exactly" claim CORRECTED. w4-r-002: findWeaklyAnchoredPress, 3 distinctive stems shared with the claimed scenario; STEM LENGTH MEASURED not guessed (at 4 departures/department collide and the swap survives — hit exactly that; at 6 ten legitimate rows drop below floor; at 5, 376/380 tier-1 swaps caught, only sub-floor row is T39a's cat opener); TIER 3 DECLINED IN WRITING with numbers (min 0, median 1-2 — a chyron avoids the abstract's vocabulary by design, so no floor both passes the tier and catches anything), and the decline is itself ASSERTED so it self-retires if a rewrite ever lifts tier 3. w4-r-003: both ratchets in, per-locale second-person lexicons covering VERB forms; TWO BUGS FOUND WHILE BUILDING THEM — "sei" is both "you are" and "six" (flagged "sei settimane"), and JS's ASCII \b never matches after "ú" (reported a Spanish tier-2 row as third person); ceilings not absolutes, every violation predates the law, none of the 34 new rows is on any list. w4-r-004/006 as specified. w4-r-005 all three corrected in place (the two errors also in commit messages 10963b8/52f12db: history not rewritten, corrected in the code comments). LOW all seven taken (007 keyboard rewritten ×3 using the cover story's two unspent jokes; 013 the simulation no longer skips uncovered cells — asserts coverage as a precondition, checks all 180,000 draws, COUNTS them to prove the loop ran). w4-r-010 restated player-facing and ASSERTED: lived 20-day cycle 20/20 distinct pages, bank yields 60 distinct pages for 60 cells; original framing was "wrong twice — it mixed a global count with a per-cell one, and it fell BECAUSE card 1 stopped rotating, which is the guarantee, not a cost". MUTATIONS 5/5 caught. BOOKED: (1) review-w4.md was on no ref — CONTROLLER PROCESS GAP AGAIN (dfd1b70 carried w1/w2/w3/w7 only); fixed, and STANDING RULE ADOPTED: persist AND COMMIT a review before dispatching the fix round that cites it; the one inferred finding was w4-r-008 (unspecified "four IT idiom fixes" — W4 did its own re-read and fixed five; re-reviewer to check the five against the four). (2) Pre-existing, not W4's: the IT cover story still carries "classificato … contro un elenco", the same anglicism W4 fixed in its two press rows. SCOPED RE-REVIEW dispatched to a06c4e0805eb0ef92.
W4 SCOPED RE-REVIEW: APPROVED (after two one-word IT corrections, applied by the controller and gate-verified). w4-r-001 VERIFIED BY DIRECT MEASUREMENT, margin LARGER than claimed: synthetic bank pool sizes 5/7/6, salted signatures = EXACTLY the pool size (full decorrelation), unsalted = 1/1/1; over 400 dates × 3 tiers minimum is 4 salted vs 1 unsalted, so `> 1` can only take 1 (no salt) or >=4 (salt) — structurally exact and NOT tied to today's pool sizes the way the threshold option would have been. Mutations 6/6 killed (incl. the reviewer's own N2/N3 verbatim, and IT+ES second-person strips proving the lexicons reach both locales). *** CONTROLLER LEDGER CORRECTION (the re-reviewer caught my error): NO TEST WAS DELETED. published.test.ts holds 47 tests at ec6f99b, at 5d6cc84 and at HEAD; the name-by-name diff is empty. W4 DECLINED TO WRITE a law and recorded why — a different act with a different burden than deleting one. My earlier entry said "deleted its own second test"; that was wrong. *** The decline was CORRECT and measured: 59 of 60 cells have ONE bound row, so card 1 is byte-identical on every date and the proposed law would have asserted the negation of T39a's guarantee for 98% of cells. The replacement assertions BITE (base 18/20 and 43/60 → fix 20/20 and 60/60). w4-r-002 decline numbers check out (tier-3 min 0/median 2/max 4, below floor 18/21 EN 17/21 IT 15/21 ES — real margin); guard sweeps ALL 380 ordered tier-1 pairs, not one swap. w4-r-003 both lexicon bugs confirmed real — the re-reviewer had hit BOTH itself in round 1 (sei-settimane false positive; ASCII \b after ú under-reporting ES); over/under-match checked both directions, zero false positives, zero false negatives; residual (closed sets, a NEW unlisted second-person form could slip at tier 1) accepted — it fails CLOSED in the safe direction. w4-r-006: reviewer WITHDREW one of its own findings ("tras considerable insistencia" is the ES cover story's own pre-existing wording — its error). w4-r-011: W4 found a THIRD occurrence the review hadn't named. w4-r-013: W4 added a draw-count assertion (checked === 180,000) the reviewer hadn't thought to require. w4-r-005 commit-message question RULED SUFFICIENT: rewriting three commits to correct two prose claims would invalidate every reference to those SHAs (ledger, backlog, both reviews) for no benefit — the errors are narrative prose, the CODE comments are what a future reader reaches for, and the fix commit is self-describing ("corrected in the report instead, since history is not rewritten"). RESIDUAL LOW, explicitly not warranting a third round: w4-r-014 two IT defects (APPLIED BY CONTROLLER as e51af8e+1: del canale, e poi del telescopio; tsc 0, 1740+1todo), w4-r-015 (w4-r-012's two named EN nits untouched — two DIFFERENT ones fixed instead, both real improvements, so "LOW all taken" was not accurate), w4-r-016 (007 keyboard overlap UNCHANGED at 95/100/100% — better row, overstated claim; 009 prefix repetition reduced but the twinned FRAME remains and the one-clause comment is untouched). PROCESS NOTE FOR THE RECORD (reviewer's, and it is fair): this wave twice reported a property as held that measurement showed was not ("properties preserved exactly", "LOW all taken") — both caught, both owned immediately, both fixed well, and round 2 is the strongest guard-building in the wave; but the pattern is worth naming because the fix was cheap both times.
W4 MERGED -> build/v1, PUSHED. GATE ON MERGED TREE: tsc 0, eslint 0, vitest 1816+1todo (59 files), build 0, cal verified, e2e 28/28 three engines. task-w4 removed. W4 CLOSED. *** 10 of 12 waves merged. Remaining: W8 (in flight), W11, W12, then the final verification wave. ***
