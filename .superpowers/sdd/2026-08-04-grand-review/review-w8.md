# GR6 W8 (PRACTICE MODE) — REVIEW
(Persisted by the controller from the reviewer's reply.)

Target task-w8 @ 91b4f1c, base 18aae1a. VERDICT: NEEDS FIXES — 1 MEDIUM, 6 LOW. Worktree never mutated; probes on git-archive copies.

## THE HEADLINE CLAIM — CONFIRMED INDEPENDENTLY ON THE BASE TREE
Reproduced end-to-end on 18aae1a BEFORE looking at the fix. EPOCH '2026-08-10'; puzzleNumber('2026-08-06') === -3; -3 % 14 === -3; both banks[-3] undefined. Booted the real store through the real flow (boot → openData → submit/abandon → makeCall) on a pre-EPOCH ISO and read the DOM: RETRACTED @ 2026-08-06 subline ABSENT; NULL_REPORTED @ 2026-08-06 ABSENT; RETRACTED @ 2026-09-01 (control) PRESENT. Head: 4/4 pass. **W8 found a real, live, silent shipping bug and fixed it correctly.** bankIndex is the standard euclidean form, exact for every integer of either sign, and length<=0 → 0 avoids the 0%0 → NaN subscript.

## GATE REPRODUCED EXACTLY: tsc 0, eslint 0, vitest 1825+1todo (60 files), build 0 (bundle 115.9/200 KB gz), e2e 28 three engines.

## MUTATIONS — 17 runs (5 novel; NULL control green so the harness is honest)
N1 bare % at the Reveal call sites RED ×2 · N1b Math.abs(n % len) RED (euclidean pin) · N2 `practice: practice || dayType === 'effect'` at the dayComplete call site RED — the practice/spoiler boundary is guarded AT THE CALL SITE, not only by shareString's signature · m11 sever substituteEffect on head RED / m12 same on base 18aae1a GREEN (W3's reviewer's control reproduced exactly; vacuity genuinely closed) · mA issueLabel never em-dashes RED ×5 · mB DOI raw RED ×2 · mC share line always numbered RED ×3 · mD/mE notice raise/retire RED ×5/×1 · mG countdown never suppressed RED ×2 · mK marker never rendered RED ×2 · mL .ph-new-day-notice loses its rule RED (gr6-024) · mI2 client guard restored to its exact original first line GREEN → w8-r-002 · mM Summary uses a second independent localIsoDate() GREEN → w8-r-005. Re-ran the spoiler property with a DIFFERENT seed (0xbadbeef) and 900 draws — green.

## FINDINGS
[w8-r-001] MEDIUM — the mid-play notice is routed to the FINISHED-DAY screen, where its sentence is false and its own no-affordance argument does not hold. Drove a rollover on every screen: the notice renders identically on lab, call, reveal, published AND summary, always reading "The one you are playing still counts as the day it started; today's puzzle is waiting WHEN YOU FINISH." On the Summary the player HAS finished. Worse, W8 has just (correctly) suppressed that screen's countdown, so the Summary now offers NO ROUTE AT ALL to today's puzzle. And W8's decline rationale is by its own terms scoped to mid-play ("mid-play nothing is persisted until the Summary, so a reload IS the destructive action") — SummaryScreen's first-mount effect (Summary.tsx:409-436) has ALREADY run persistAndComputeSummary by the time that screen is on-screen, so a reload there loses nothing. The decline is right for lab/call/reveal/published/prereg and wrong for exactly one screen. FIX (a, preferred): exclude 'summary' from newDayPending and give the Summary its own line WITH errors.reload — safe post-persist, and the only route to the new day. (b): reword errors.newDay ×3 so it is true of a finished day too, and record the missing affordance in writing.
[w8-r-002] LOW — the recorded reason for moving the clientRef/content guard is measurably false. "No behaviour change" TRUE (permutation of side-effect-free conjuncts; mI2 leaves the full suite green, appMidnight.test.tsx included). "That is what closes w7-r-003's unreachable-in-jsdom" FALSE — what makes the effect reachable is the createEngineClient mock (the router.test.tsx idiom the file itself cites), which sets clientRef.current regardless of guard order. Also stale: e2e/midnight.spec.ts:11-13 still says in prose that checkRollover "returns on its first line". FIX: correct the App.tsx comment (call the reorder a readability/ordering improvement) and update the e2e header; or revert the reorder, which costs nothing.
[w8-r-003] LOW — the %-over-a-pool sweep's ENUMERATION is incomplete; its CONCLUSION is right. Reviewer's own sweep: published.ts:124/289/297 (THREE not two), briefing.ts:16, RadioGroup.tsx:54/55, Call.tsx:76 (UNMENTIONED), engine/seeds.ts:82/136, engine/day.ts:445 (UNMENTIONED). Every one verified safe and why (fnv1a32 returns h>>>0; pickPress's offset is an fnv1a32 value or a slot index 0-2; the roving-focus sites do the +length dance and their worst input findIndex → -1 stays non-negative for any pool length >= 2). "No other site is exposed" TRUE; "the other four" is not the count. RESIDUAL: generatePractice(seed, scenarioCount) does an unguarded seed % scenarioCount on an EXPORTED entry point with no stated non-negativity precondition, and it cannot borrow bankIndex (eslint.config.js:159-183 bars src/engine/** from importing src/game/**) — wants a local guard or a written precondition.
[w8-r-004] LOW — fakeDoi's widened `string | number` leaves the exact defect typechecking: fakeDoi(-3) still returns 10.1337/phk.-3; only the two behavioural tests stop the regression. Narrowing issueLabel to string (practice ? NO_ISSUE : String(puzzleNumber)) and fakeDoi(issue: string) would make the old call a COMPILE ERROR, closing the class rather than the instance. Cost: String() at three test call sites. The rest of the reasoning is sound — src/game/** genuinely acquires no src/ui/** import (all hits are comments), and keeping the rule beside the masthead formula is the right single-source call.
[w8-r-005] LOW — the "one clock read" discipline in SummaryScreen is unguarded (mM green). The shipped code IS correct and internally consistent (suppression and countdown both read the same `now`) — verified — but the claim "can never be answering different questions" has no tripwire. Also "cannot contradict the shell notice" is EVENTUAL, not instantaneous: the shell re-checks on a 60s interval, the Summary refreshes `now` every 30s. Bounded and self-correcting; recording it would be honest.
[w8-r-006] LOW — two booked-out items name a CLOSED wave (W10 merged/closed). The docs half already says CONTROLLER WILL CARRY AT MERGE; the e2e half does not and needs routing to the final verification wave. The e2e gap is NOT material: appMidnight.test.tsx covers the notice in jsdom and both its guards are load-bearing.
[w8-r-007] LOW — the wrapped-masthead middot booking's width is EN-scoped. Measured in chromium against the built bundle at ?practice=1: masthead 74px (two lines) at 320/360 for EN, at 320/360/414 for IT and ES; 45px (one line) at 414+ (EN) and 768+ (IT/ES). The leading "·" persists to 767px in IT/ES, not "below ~600px". No horizontal overflow in any of the 15 width×locale cells.

## ADJUDICATIONS
gr6-078 deviation JUSTIFIED, measurement reproduced exactly: backlog literal → stored stats.streak=7 but invoice "Forks: 0 · Streak: 0"; W8 as shipped → invoice "Streak: 7". state.stats.streak IS the figure Stats.tsx:102 prints (both read loadState()). Take the deviation.
errors.newDay's declined "reload" — ACCEPT for mid-play, REJECT for the Summary (see w8-r-001).
Three locales vs the compiled laws CLEAN AND MEASURED: em-dash density base→head EN 12366.3→12410.0, IT 14179.0→14236.0, ES 42648.0→42787.0 chars/dash (floor 2500); counts unchanged 3/3/1 so the IT ratchet holds at 3<=3 with the same zero slack; zero U+2019 in localeProse in any locale; no tokens in either new key; vocabulary consistency per locale (IT rompicapo, ES puzle — both match their own errors.workerCrash); register tu/tú throughout. NO_ISSUE = '—' sits outside the catalog by design; if counted, EN would go to 4 dashes / 9307 chars per dash, still 3.7× the floor, and the IT ratchet would stay level since it is one shared UI constant — the exemption is harmless either way.
Practice-mode UX at 360 — YES, unmistakable without being obtrusive. Two craft notes, neither a finding: at wide widths "No. — · PRACTICE RUN" puts an em dash and a middot side by side at similar weight (slightly muddy); the wrapped line's leading "·" reads as a bullet more than a separator.

## VERIFIED CLEAN
Guard reorder behaviour-neutral. No pre-boot flash of "No. -3" (app-loading until booted). src/game/** imports nothing from src/ui/**. Both briefing.vol sites go through issueLabel. .ph-header__practice and .ph-new-day-notice both caught by gr6-024's no-class-without-a-rule check, including through the grouped selector with an interleaved comment. Summary countdown suppression uses the same `now` as the countdown it suppresses. Ledger's W4 press-number and header-measurement claims consistent with independent measurement.
