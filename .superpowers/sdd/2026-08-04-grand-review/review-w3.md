# GR6 W3 (CONTENT CORPUS) — REVIEW
(Persisted verbatim by the controller from the reviewer's reply.)

Target task-w3 @ e11edd8, 12 commits on bb8ad1c, 8 files. VERDICT: NEEDS FIXES — one HIGH blocks; rest LOW/MEDIUM and cheap. Every numeric claim reproduces exactly; contract work best-argued in the round; as-rendered discipline real.

GATE (re-run): tsc 0, eslint 0, vitest 1690+1todo (58 files, +38 exactly), build 0, bundle 114.9/200KB, e2e chromium 22/22.

SCIENTIFIC: gr6-005 floor measurement REPRODUCED BYTE-FOR-BYTE (71,680/71,680; 5-day probe 17,920/17,920; runSpecCore regresses transformedY not z-scores so beta is raw-unit — premise right; fixed-frame argument independently correct). ATTRIBUTION CORRECTION (w3-r-011 LOW): floor did it for 69,336, plain rounding for 2,344 → 96.7% not 100%; deleting the floor prints "0" — the floor is not the bug, the raw-unit beta is; fold into W11 booking. gr6-038 CORRECT ×3 (7 complete, jigsaw subsumption verified; compiled guard genuinely unavailable — regex false-positives on 4 legitimately-signed labels). gr6-039 coherent ×3. gr6-040 plausible+honest ×3, numbers reproduce (3→17 distinct, maxrep 15→4).

[w3-r-001] HIGH scientific — 5 of 10 nullReportedSublines FALSE on an abandoned EFFECT day; hand-off carries no caveat. reveal.ts:81 `if (published === null) return 'NULL_REPORTED'` is day-type-blind; 15/60 days (25%) are effect days; abandoning one is the honest path §1(j) is about. Same screen renders reveal.truthEffect ("True effect on {outcome}: β = 0.29") in block 1 while sublines [0][1][2][3][7] deny an effect exists. RETRACTED bank NOT precedent (RETRACTED implies published spec; its claims are spec-scoped and true). Fix shapes: (a) day-type the bank (nullReportedSublinesEffect variant or day-safe subset); (b) rewrite the five day-type-agnostic ([4][5][6][8][9] prove the register compatible); (c) minimum: constraint into the Reveal.tsx:300 hand-off + book vs §1(j)(2). Nothing ships false today (bank unwired) — HIGH not tree-blocker.

[w3-r-002] MED transcreation — IT jigsaw opener loses negative concord: "hanno prodotto quasi nessun bagaglio" needs pre-verbal non → "non hanno prodotto quasi nessun bagaglio"; also "ed è la lacuna" → "ed è questa la lacuna". (ES got the same sentence right.)
[w3-r-003] MED contract — apostrophe law corpus-only; N7 (U+2019 in en/copy.ts lab.coefPlotAxis) SURVIVED. Fix: run the describe over localeProse; verified 0 typographic quotes in all three catalogs today (green on landing).
[w3-r-004] MED contract — dosage numbers compiled nowhere: N3 (all 19 income reverts) SURVIVED, N2 (one relative label revert ES) SURVIVED, N1 (jigsaw outcome-0 revert) SURVIVED. Accept no-guard for 038/039 (semantic); REJECT for dosage rows: three one-line ratchets (maxrep(income)<=4; count(^Longest|^Length of)<=7; count(sense-head)<=1) or one recorded decline.
[w3-r-005] LOW — label-maker slot 0 relative label kept absolute unit "% of arrivals" → percentage points ×3 (sibling jazz-spreadsheets is the model).
[w3-r-006] LOW craft — 3 EN headlines read as token-ectomies: full-moon ("Now…Measurably" — worst; fix "Meetings Run Longer Under a Full Moon, Analysis Finds"), horoscope ("Save Minutes"→"Save Time"), telescope (IT is the model).
[w3-r-007] LOW — ES cat-crypto "ganan más" drops the noun → "obtienen mejores rendimientos, según un estudio".
[w3-r-008] LOW ×3 — jigsaw gap clause awkward all three (ES worst: "cubrir al aeropuerto"); restructure second half.
[w3-r-009] LOW contract — pairing guard pins length not pairing; N6 (swap last two bodies) SURVIVED. Fix: 2-3 index-pinned content anchors or a comment naming reorder as residual risk.
[w3-r-010] LOW craft — nullReportedSublines[3] opens "The dataset was fine." byte-identical to retractionSublines[6]'s first sentence; vary ("The data held up. So did the analysis.").
[w3-r-012] LOW — "no subject"/"senza oggetto"/"sin asunto" read as rendering bugs; parenthesize like index 9's idiom.
[w3-r-013] LOW process — gr6-072 took 4 of 5 with no recorded decline ("confidence" ×2 remains); take or record.

CRAFT VERDICT: the 19 re-cuts LAND (12 pure subtraction read MORE like real press; 4 better than replaced; 3 padded = w3-r-006). Best EN: sock-folders badge-data line; number-13 mortgage; terms-readers "Quietly". Best opener: "There is a folding table at gate 14 and, on it, someone's holiday." Sublines best: "Nobody will cite it, and it will still be true next year." Worst: [0][1][6] same shape, [0]/[3] near-duplicates (dosage wobble). IT best: citation line better than EN; telescope line better than EN. ES best: "El hallazgo es que no había nada que hallar." ES worst: ganan más; cubrir al aeropuerto; "poner nombre" inverts the joke (fix: dar los nombres de / señalar por su nombre; IT nominare is fine). Three self-caught nits verified fixed. grantwellSubjects 22/22 pairing CORRECT ×3 meaning-wise (RV:/Fwd:, ERC echo, mail-client terms; Re: Re: Reviewer 2 exempted in both anti-alias tests).

MUTATIONS: 9 replayed 9 killed (incl. M7/M7b proving IT/ES inheritance via validateLocaleContent; M8a stale-entry; M8c second-exemption). 7 novel: M8b (same sentence verbatim in retractionSublines) KILLED; N1/N2/N3→w3-r-004; N4→booking; N6→w3-r-009; N7→w3-r-003. Invariants re-measured exact (476 rows/locale; 1 leading-zero hit = grantwell[0] only; 0 quotes; zero dashes added, density improved; glossary definitions byte-identical).

DEVIATIONS: 1 ACCEPT strongly (6→19); 2 ACCEPT (8→15); 3 ACCEPT (two closers, verified); 4 ACCEPT verified (EN-only + net-strengthened voice law); 5 SPLIT (accept 038/039, reject dosage → w3-r-004); 6 ACCEPT (apostrophe law; scope under-drawn → w3-r-003); 7 ACCEPT AND COMMEND (grantwell[0] allow-list; survives harder probe M8b); 8 ACCEPT w/ note → w3-r-013.

BOOKINGS all correct: Briefing.tsx:193 exact (two-line wiring + same-commit emailSubject deletion); Reveal.tsx:300 exact — w3-r-001 caveat MUST attach there; published.test.tsx:145 vacuity TRUE AND STRONGER (N4: severing substituteEffect in Published.tsx leaves ENTIRE suite green on head, reds on base — coverage genuinely gone; re-pin against token-carrying fixture, don't just retire); Math.max floor → W11 with w3-r-011 correction.

PARODY: clean; zero new outlets (10/locale set-identical); journals.ts untouched; only real-world-specificity REDUCTIONS; ERC echo pre-existing; gr3-033 undisturbed.

SUMMARY: Blocking w3-r-001. Should-fix w3-r-002/003/004/005. Low 006-013. Nothing needs re-measuring at merge.
