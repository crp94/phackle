# W7 FULL REVIEW (four rounds) — VERDICT: NEEDS FIXES
(Persisted verbatim-in-substance by the controller from the reviewer's reply.)

Target task-w7 @ e35a653, own work = build/v1...task-w7. 1 HIGH · 2 MEDIUM · 5 LOW. Gate reproduces exactly (tsc 0, eslint 0, vitest 1698+1todo 59 files, build 0 PWA 14, e2e 3-engine 26). Mutations: 9 replayed (8 as claimed), 7 novel, 1 fair control.

## w7-r-001 HIGH — .ph-entered ships TRIPLICATED: the REJECTED option (a) landed together with option (b)'s law change
Declared byte-identical three times (Reveal.css:78, Published.css:137, Summary.css:198); the other four §9.1 utilities are each declared once in App.css. The controller rejected triplication; DESIGN.md:523-535 (added by the SAME commit) asserts triplication "would recreate the exact defect" and "the iteration widened and nothing else did" — both false of the shipped tree. §9.1:829-831 also says utilities are "declared once in App.css"; .ph-entered is not in App.css at all.
MEASURED: reverting R5.6 to per-file leaves motion.test.ts 35/35 green — the escalation + 3 conditions + law amendment buy the shipped implementation nothing. COST (novel N1): hidden .ph-ghost{opacity:0} in Reveal.css whose only restorer sits in Summary.css, never co-written, now PASSES (suite 1698 green); per-file would have red.
IN FAIRNESS (constraint real, unrecorded): R5.2's File column pins each site's animation: to its own stylesheet, and App.css lands at byte ~6,000 vs the three bases at 25,062/33,075/39,106 — a bare .ph-entered{opacity:1} in App.css loses the cascade at equal specificity and would make all three entrance sites permanently invisible. No rendering defect ships today (minifier dedupes to one rule at 39,258, after all bases).
FIX (either, not both): (a) keep per-file animation/animation-delay (R5.2 requires), hoist the shared restorer into ONE App.css rule at sufficient specificity — `.ph-fade.ph-entered, .ph-press-card.ph-entered, .ph-chyron.ph-entered, .ph-summary__unlock-item.ph-entered { opacity: 1 }` — order-independent, values unchanged, makes corpus-wide iteration load-bearing; update motion.test.ts:466's summaryCss restorer assertion. (b) keep triplication, REVERT R5.6 to per-file, rewrite DESIGN.md:523-535 to record the two real constraints.
The three controller conditions themselves WERE met (stray-orphan permanent test; T38 probe re-pointed and still red; predicate-vocabulary guard RED under mutation). legacyRestorerCount widening = honest port. BUT COMPONENT_CLASS_PAIRS negative moved to (ph-press-card, ph-fade) — ph-fade can never vouch under any predicate, so the control no longer exercises the association mechanism; use a pair whose second member IS in the vocabulary (e.g. ph-stamp--animate).

## w7-r-002 MEDIUM — altmetric {pct} rename guarded ONE direction only
Half-rename B (call site) RED as claimed. Half-rename A (all three catalogs {pct}→{n}, call site unchanged) GREEN — full suite 1698+1todo, tsc 0. Renders raw: "Top {n}% of all research outputs, all time" ×3 (t.ts:33-35 leaves unmatched tokens by design). Root cause: the assertion derives its expected string from the same catalog the component reads — tautological. This is the exact defect commit 2f6cb83 existed to fix. FIX: assert the RENDERED shape using this wave's own COUNTDOWN_SHAPE idiom (escape key, substitute \d+, anchor), or one corpus-wide guard that no rendered string contains an unsubstituted {…}.

## w7-r-003 MEDIUM — midnight-rollover effect is new, destructive, zero coverage
App.tsx:218-237 adds 60s interval + visibilitychange calling boot() behind four conjuncts. grep tests/ e2e/ for visibilitychange|ROLLOVER → zero matches. Unreachable in jsdom by construction (createEngineClient's new Worker throws → clientRef null → checkRollover returns first line), so only e2e could cover it and nothing does. MEASURED (N6): deleting the guard `if (state.screen !== 'briefing' || state.log.length > 0) return;` — so midnight wipes a half-hacked spec/open reveal/finished summary — leaves tsc 0, vitest 1698, e2e chromium 22/22. TODAY'S BEHAVIOUR IS CORRECT (probed: mid-play and finished-day return early, state kept); the finding is the missing tripwire. FIX: one e2e (boot → Lab → advance pinned clock past midnight → fire visibilitychange → assert log survives); engineClient.ts:39's makeWorker seam allows a unit test instead.

## w7-r-004 LOW — the instrument's one catch landed without a guard
Watermark de-casing correctly fixed and verified live (both cards, both viewports, uppercase + 1.04px identical to base). But removing ph-label from Published.tsx:117 leaves 1698 green; removing `ph-focusable ph-label` from all three .ph-briefing__cta buttons (R6.1 ring + R2.7 casing off the primary CTAs) ALSO leaves 1698 green. FAIR CONTROL: the same loss expressed pre-consolidation on build/v1 also passes (1652 green) — the gap is PRE-EXISTING, not introduced by gr6-050 (R6.1/R2.7 are compiled as value laws, never coverage laws; tokens.test.ts mentions neither utility name). Scoped to W7: a catch was made, documented as invisible to the suite, and no tripwire left behind.

## w7-r-005 LOW — R5.6's "never bare as a descendant gate" clause is uncompiled
motion.test.ts:307 matches the bare form as readily as the scoped one. MEASURED (N5): rewriting both Reveal.css gates to bare `.ph-entered .ph-stamp--animate` leaves 1698 green and e2e 22/22 (reduced-motion and stamp specs included). Today's tree is correct and w0-r-002's re-read was green; nothing holds it there. FIX: require the base class in the regex + a corpus negative.

## w7-r-006 LOW — gr6-089 shipped 1 of 3 named sub-items, no recorded reasoning for the other two
Only ph-spec-group ownership split landed (both sides reasoned). Both abbreviation mismatches survive in base AND head (.ph-stats__histogram beside __hist-*; .ph-stats__achievements beside __ach-*); the round's two new classes extend the abbreviated family. Ledger's "089-ownership" is honest shorthand but not the written reasoning A4 requires.

## w7-r-007 LOW — the w1b-005 ruling record contains an annotation its own numbers refute
Reveal.css:286-289 records "the frame crosses the title's SECOND line only" at 360, but 469.6−373.0 = 96.6 = exactly 3× the 32.2px line box the same note uses at :291. Re-measured at 360: three glyph runs; the frame crosses the THIRD only, clearing the second by 6.32px. The next comment (:333-337) says the title "stops wrapping to three lines" above the breakpoint — the file contradicts itself. THE RULING'S CONCLUSION IS SOUND AND FULLY REPRODUCED (768: frame clears volume line by +9.95px vs comment's 9.9; both question lines stamped 28.0px each; R8.2 oversized survives at 47.2%/31.7%). Every other number reproduces to ±0.1px.

## w7-r-008 LOW — the trim leaves the same defect across 530–767px by the ruling's own criterion
Sweep (frame-top minus volume-line-bottom): +40.54@500, +5.50@510, +2.64@520, −0.22@530, −3.08@540, −4.21 flat 600→767, +9.95@768/@1088. −4.21 at 767 is identical to build/v1. The note's own indictment ("a width the rule never looked at") applies verbatim to 530–767. MITIGATION (measured): the crossing is BOX-LEVEL at every width — the volume line's glyph run ends at x=141.53, the frame's left edge is x=276.32, 134.79px clear — so NO STAMP INK HAS EVER TOUCHED the volume line at any width tested. No player-visible defect; what fails is the box-level criterion the ruling chose. Controller's call: accept and record, or move a breakpoint R3.4 does not have.

## VERIFIED CLEAN
gr6-006 reproduces exactly (prompt 3.64→16.24:1 paper, 2.45→13.98:1 dark; eyebrow — unmentioned in the backlog and WORSE than the prompt — 1.14→5.09 / 1.12→5.08; base backdrop had 482/493 distinct colours through the scrim, head's is one). Four utilities' zero-diff claim HOLDS (0 differences in 12,915 property comparisons across 4 screens ×2 viewports; focus ring identical on 14 observations; instrument non-vacuous four ways; the 20 tree-wide diffs are all gr6-013's deliberate display:block→flex on an element carrying none of the utilities). No unsanctioned motion site (6 keyframe bodies, 12 animation declarations across R5.2's 8 sites, exactly one transition = PValueDial colour exception; register reconciles both directions). The four booted:true seams do not mask a boot defect. pressForDay adoption complete (Published.tsx:315 sole path; both [INTERIM] tests deleted). ROSTER_PENDING empty, self-retiring both directions. Both §9.1 value-match rulings SOUND (adopt .ph-boot-error; decline .ph-published__cover — reasoning at Published.css:25-41 genuinely good, consistent with the .ph-call precedent). gr6-014/015/017/023/025/029/060/061/074/075/080/083/088/104 and §1(e) sticky actions all check out (153+67=220px=34% of 640 reproduced).

## MERGE-TIME NOTES
1. CONFIRMED OWED: §9.1:859-860 "the two that already spell '40 above, 24 around'" → THREE (.ph-boot-error).
2. NEW: §9.1 enumerates only .ph-call and .ph-reveal as off-shell — should also record the .ph-published__cover decline.
3. NEW: R5.6:486-492 self-contradicts (status line "landed by GR6 gr6-050" vs next sentence "currently ships as three names"); same staleness at §9.1:830 and §9.1:866 (.ph-reveal "to be ruled when the consolidation is done" — it WAS ruled: keep 40).
4. Carry w7-r-001's disposition explicitly — it decides whether the R5.6 corpus-wide paragraph stays in the book.
5. errors.newDay never authored in any locale → mid-play midnight affordance defers to W8; in the ledger, not in backlog.md.
6. Harmless, disclosed: historical TODO-W2 substring at src/content/en/copy.ts:849; tokens.test.ts:466's it.todo is W10's tripwire.
