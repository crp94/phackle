# GR6 FINAL WHOLE-BRANCH REVIEW — build/v1 @ fb2b8ed
VERDICT: **SHIP IT, AFTER THE CHECKLIST.** The branch is sound. One player-facing false claim, and it is not about the game.
(Reviewer started at 8d7dffb, noticed e2c0545 and fb2b8ed land mid-review, re-archived at the real HEAD and re-ran the entire gate. Both post-merge fixes verified and folded in; neither alters the report.)

## 1. GATE (run by the reviewer at fb2b8ed)
tsc 0 · eslint 0 · vitest 61 files / 1937 passed / 1 todo · build 0 · bundle PASSED 118.6 KB gz of 200 · cal PASSED 5/5 bands, p_hit verified (checksum 3889152766), 0/1000 cap blowouts, attempts p99 6 / max 11 · playwright 28 (chromium 24, firefox 2, webkit 2). ZERO .skip, ZERO .only. The single it.todo is tokens.test.ts:466 — the deliberately disabled analytics tripwire, i.e. finding #1 wearing a green hat.

## 2. THE ONE THING THAT IS FALSE
about.dataDisclosure ×3 locales (en:1449, it:584, es:560), rendered unconditionally at About.tsx:89, discloses an analytics vendor. VERIFIED TWO WAYS: the package is in neither dependency block and there is no inject() call site anywhere; and `grep -rn "fetch(|XMLHttpRequest|sendBeacon" src/` returns ZERO HITS — the app makes no network request of any kind at runtime. No vercel.json exists either. It OVER-discloses, so it is harmless in the direction that matters (the rest of the string, "never sent anywhere", is true precisely BECAUSE nothing is sent) — but a disclosure the product cannot honour is the exact failure mode this game is about, and it is the one claim on the site with legal weight. tokens.test.ts:418-471 documents it and writes the fix verbatim behind the it.todo.
EVERYTHING ELSE CHECKS OUT, several true only because carefully written: allSpecs().length is 1792 as quoted; preregFalsePositive's "about 8%" measures 7.71% (independent second pass 8.14%); "about one in five ... plain unadjusted default" measures 21.7% (independently 24.7%) — both inside the [0.10,0.35] guard band; the 1969 optional-stopping footnote is stated as a fact about the CITED five-equal-look design, impersonal and conditional, never as the game's rate (the game's own 8.7-11.2% is documented at the site and deliberately not printed; re-simulated from scratch at 400k trials, reproducing the in-file table to two decimals: five equal batches 14.12%, this game 11.15%, single look 4.95%). Numeric parity across locales EXACT — 237 keys each, zero drift. sigOtherOutcome = sigPaths − sigTrueOutcome by subtraction in one loop, so the Act II partition cannot go negative or desync.
FOUR SMALLER COPY ITEMS, none a blocker: (1) lab.explain.tails names an act the game does not model ("or only in the predicted one" ×3; analyze.ts:254 is ALWAYS the upper tail and the control is a bare One-tailed radio with no direction input) — defensible as textbook framing, arguably the joke, OWNER'S CALL; (2) about.priorArtOptionalStopping IT ONLY uses "addebitato" (is debited to you) implying arithmetic that does not exist — `grep -c peek src/game/scoring.ts` = 0; EN and ES correctly scope it to Act II; ONE WORD; (3) about.mechanism's sampler disclosure IS NOW BEHIND THE ENGINE — it omits §1(d)'s lab-default conjunct and the MAX_ATTEMPTS=20 fallback; nothing is contradicted, the filter is simply LARGER than described; this is wave-ordering fallout (W2 deferred the discard rates to "move with W11's §1(d)" and W11 landed without the copy following) and the About page's whole claim is candour about the sampler — WORTH ONE SENTENCE BEFORE LAUNCH; (4) "One attempt, one mode" is false in a practice session (Briefing.tsx:158-162 makes modeSpentToday unconditionally false under practice) — unreachable before EPOCH, reachable after via ?practice=1 with history; LOW.
Cosmetic: about.mechanism's "every player in the world analyzes the same numbers" is absolute where the engine hedges (documented <=1 ULP cross-engine divergence through Math.exp/log); the three-engine e2e matrix is what actually backs it, and it is green.

## 3. THE THREE HIGH-CONSEQUENCE CLAIMS, VERIFIED INDEPENDENTLY
(a) W8's negative-puzzle-number bug — HOLDS AND THE CLASS IS CLOSED. bankIndex is euclidean, both Reveal call sites use it, and the reviewer swept every % in src/: every remaining one is over an fnv1a32 result (unsigned) or already euclidean/guarded, INCLUDING generatePractice's seed % scenarioCount which W8 fixed in the same pass. NO EXPOSED MODULO-OVER-A-POOL REMAINS. Regression coverage is real (reveal.test.tsx:431/439 drive both banks at a negative puzzle number and assert the subline is present AND drawn from the bank; daily.test.ts covers negatives, zero-length, both live banks).
(b) W11's acceptance predicate — REPRODUCED AND STRONGER THAN CLAIMED. Base vs HEAD over 1000 consecutive real dates from 2026-08-11: lab default significant at N=200 on the accepted day 147/1000 (14.7%) → 57/1000 (5.7%); on NULL days 59/754 → **0/754**; cap exhaustions 0/1000 both. ALL 57 SURVIVORS ARE THE DOCUMENTED EXEMPTION (effect days whose true outcome IS the lab default's — a true positive on the day's own truth, the one case the gate is deliberately blind to). The residual is not leakage; it is the exception, exactly. DOC NIT: day.ts:139 says "120 dates ... 5/91 null, 9/29 effect (11.7%)"; that window is actually 89 null / 31 effect and measures 15/120 (12.5%) — direction and magnitude hold, the breakdown belongs to some other window. One-line correction, no behavioural consequence.
(c) W12's four fail-open predicates — CONFIRMED, with a correction in EACH direction. The merge message OVERSTATES one thing: pre-W12, `stamp !== 'NULL_REPORTED'` was CORRECT for a three-member union, and at three of the four sites the values come from the engine, so the fail-open was LATENT — it would have gone live the instant a fifth verdict was added BESIDE the old one. W12's choice to REMOVE the member rather than add beside it is what converted four silent wrong answers into four TS2367s; that is the real achievement. It UNDERSTATES another: at storage.ts:218's readRecord the fail-open was ALREADY LIVE — it reads rec.stamp off a blob validated only as "is an object", so pre-W12 ANY string other than 'NULL_REPORTED' (garbage, hand-edited, a future value) counted as published and inflated careerPoints through the repair path. That one is a genuine fix, not a prophylactic. NO MIGRATION DRIFT: legacy 'NULL_REPORTED' reads undefined → false, which is what that verdict meant.

## 4. THE HAND-RESOLVED W12 MERGE — ALL FOUR CONFLICTS RIGHT
share.ts line 1 composed correctly (practice drops the number, keeps the tagline; spoiler-safe because `practice` is not day type, verdict or direction). Reveal.tsx WAS THE REAL HAZARD, not line 1: W8's bankIndex fix sits INSIDE the hunk W12 rewrote, so taking W12's side wholesale would have silently re-broken every subline bank on every practice day; the merge kept both and the naive resolution reds. scoring/summary tests: the outcomeFamilies rename crossed with the verdict split, mapped by dayType — and scoreDay branches on (published, dayType) and (preregSig, dayType), NEVER on stamp, so the split cannot reach scoring at all.
THE SEAM THE REVIEWER WENT HUNTING FOR AND DID NOT FIND: W12's earlier prereg unlock against W6's finished-day guard. Widening the unlock should be able to un-finish a day with an unspent prereg attempt. It cannot — !modeSpentToday is a conjunct of BOTH arms, so widening preregAvailable is inert against a finished day, and the controller's preregUnlockedBy tightening only narrows it further. The Summary agrees in words ("Tomorrow you can choose it"), so the player is never invited to a door that is shut. Also confirmed: the memoised scenarioIndexFor walk is ORDER-INDEPENDENT (ascending vs descending query order byte-identical, 0 mismatches, 0 fourteen-day repeat violations over 386 windows × 3 scenario counts).

## 5. WHAT IS ACTUALLY WRONG (nothing blocks)
1. ScoreDayInput.stamp (scoring.ts:33) is declared, required, and NEVER READ — dead field on the scoring API; callers must pass it and it does nothing. Delete it.
2. day.ts's determinism header claims the engine has "no shared mutable state" — NOW LITERALLY FALSE: seeds.ts carries module-level caches since the recursion was made iterative. The memoisation is sound (verified), but determinism now rests on cache correctness rather than statelessness and the comment should say so.
3. Stale comments naming the retired verdict (dayComplete.ts:267,269; store.ts:134; Stamp.tsx:90; e2e/stamp.spec.ts).
4. e2e/harness.ts:537's SHARE_LINE1_PREFIX(n) has no practice arm — no e2e exercises practice line 1 (unit-covered at share.test.ts:163). Matters more than it looks — see checklist item 0.
5. capExhausted is on the wire with no reader (protocol.ts:172; nothing in src/ui consumes it). Measured 0/1000 on both trees — dead data, not a fail-open.
6. e2e/harness.ts:532's SHARE_LINE2 regex does not pin what its comment claims. DOWNGRADE of w12-r-003: share.test.ts:406 pins the exact rule across k ∈ {0,1,4,5,6,10,11,43,60}. Comment accuracy, not a coverage hole.

## 6. T25 DEPLOY CHECKLIST (updated — see the ledger/report for the owner-facing copy)

## 7. LOOSE ENDS
egregiousnessTier/TIER_FORKS deprecated: does NOT matter for correctness (zero production call sites; the deletion instruction is written at published.ts:21-28). registry.t*.patch.md: does NOT matter (~22 KB of design scratch, NOT in the bundle — `find dist -name '*.md'` is empty). --assist-green for CONFIRMED_NULL: MATTERS, as a design question. The token is fully registered (DESIGN.md:716); what is missing is a THIRD R1.5 carve-out row (DESIGN.md grants exactly two named exceptions). Stamp.tsx declines to add one and says why, which is right for a content wave. But §1(j)(2) exists BECAUSE the honest path had no positive moment, and the honest player's WIN AND LOSS CURRENTLY RENDER IN THE SAME INK, distinguished only by the word. THE OWNER QUESTION TO PUT FIRST.
